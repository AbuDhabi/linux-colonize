# Init `FUN_521d_20e6` fog / dir hang — Brave `(47,53)`

**Status: PARKED.** Overlay reloc made prior hang EXEs unreliable. Prefer the
coarse-fog port + `SEED100` goldens instead of new `EB FE` stubs.

## Settled without a hang

| Fact | Evidence |
|------|----------|
| DOS ends **NW** `(46,52)` | `SEED100.SAV` / `SEED100_UNREVEALED.SAV` (identical Brave XY) |
| Complete Map irrelevant | Viewpoint flag only (`unknown42[2]`); same `seen[]` |
| Far ocean/land | `probe_far_ocean_4753` AGREE Linux↔SAV |
| Fog `+8` flip under all-unseen | Phase 7 score dump (stale assume) |
| Live explore cell after `6a09` | Linux `s_ai_coarse_fog`: farW explore≠0 → no `+8` → quiet picks NW |

Phase 9: dual-index plane `DS:0x9faa` — see
[`original_sources_annotated/ai/move_scoring.md`](../../original_sources_annotated/ai/move_scoring.md)
and [`docs/seed100_brave.md`](../../docs/seed100_brave.md).

## If you still need a hang (last resort)

Overlay caution: `521d` reloc may trash a blind file patch. Prefer patching the
**loaded** overlay / verifying opcodes in Memory. Do not put stubs in the
FAR-pointer null table (`midturn_465b.md` bad trampoline note).

### A — Chosen dir

```
521d:58a5  MOV [BX+0x314f], AL    ; unit last_dir
```

Filter unit XY `(47,53)`. **AL** = dir (`6`=W, `7`=NW).

### B — Fog `+8` applied

```
521d:5730  ADD word [BP+local_28], 8
```

Noisy without unit + `local_50==6` filter. After tribe place, explore-byte at
far land may already be non-zero → this ADD may not run.

## Parallel (unchanged, also parked)

Spent: `VR_B465R` (`BX==0x1F8`) → AL. See [`midturn_465b.md`](midturn_465b.md).

# `VR_1816` — hang at `FUN_4d56_1816`

Prove whether DOS enters the Indian nation-turn entry and capture the far
caller (missing from the Ghidra export). Rebuild:

```bash
python3 tools/brave_dump/patch_1816_hang.py
# → COLONIZE/VR_1816.EXE
```

| Item | Value |
|------|-------|
| File offset | `0x485F6` (`4d56:1816` ENTER) |
| Patch | 24 B: ENTER + log ret IP/CS + `param_1` → `DS:7000` + `EB FE` |
| Neighbor check | `14fe` @ `-0x318`, `1b3a` @ `+0x324` still stock |

## Scratch after hang

| Addr | Meaning |
|------|---------|
| `DS:7000` | Far return **IP** |
| `DS:7002` | Far return **CS** |
| `DS:7004` | `param_1` (indian slot `0..7`) |

Stack (ENTER ran): `[BP+2]` IP, `[BP+4]` CS, `[BP+6]` slot.

Map return `CS:IP` against `viceroy_unpacked.asm` / thunk bank (`281f_*`).

## Run (prefer normal DOSBox-X, `COLONIZE` = C:)

Overlay caution — same class as `VR_B465*` (`midturn_465b.md`). After load,
confirm Memory still shows `A3 00 70` … `EB FE` near the live `4d56:1816`
site before trusting a no-hang.

### A — Mid-game year loop

1. `VR_1816.EXE` → LOAD `TURN2` / `TURN3` (or seed-100 campaign save).
2. End Turn / wait for AI.
3. **Hang** → dump Memory / save state → read `DS:7000`..`7004`.
4. **No hang** but stub visible in Memory → `1816` not entered on that path
   (still does not prove globally dead).

### B — Init / post-`6a09`

NEW WORLD with known seed (`VR_SEED` / customize). If hang fires before first
human Move Pieces, init (or early year loop) reaches `1816`.

## Negative / corrupt results

| Observation | Reading |
|-------------|---------|
| Stub bytes gone / garbage in Memory | Reloc/EMS thrash — inconclusive |
| Hang, `7004` in `0..7`, return CS is a `281f`/`130d` thunk | Call site recovered |
| Full EOT + init, stub intact, never hangs | Unreached on tested paths |

Hypothesis (not XREF): caller in `FUN_130d_0290` after `1b3a` or after Euro
act — [`mid_pass_indian_rank.md`](../../original_sources_annotated/turn/mid_pass_indian_rank.md).

## Result — `dosbox-x-dumps/dump_1816` (2026-08-10)

**Hang fired.** `FUN_4d56_1816` is **live** (not dead code in this path).

| Item | Value |
|------|-------|
| `IP` | `0x182C` (`EB FE` in stub; entry `0x1816`) |
| Live CS | `0xCC89` (patched body) |
| Stack ret | **`1930:1554`** (SS=DS=`0x2385`, BP=`SP+0xC`) |
| `param_1` | **`0`** (first Indian slot → nation 4) |
| `DS:5394` | still `3` (hang before `param+4` write) |
| Braves spent | all 0 — pulse not started yet |
| Parser | `tools/brave_dump/parse_1816_dump.py` |

Scratch `DS:7000` = IP `1554` / CS `1930` (matches stack). Slot word
reloc-trashed (`8B 46 06` → `8B 69 0E` in live stub); trust stack `param`.

Return sits in the **overlay “Return Vector”** segment (`"Return Vector"`
string @ `1930:15AF`), not a direct `130d` / `281f` thunk return. Nearby
static `CALLF 1930:15CF → CC81:1816` targets a **different** paragraph than
the patched load (`CC89`); treat as overlay-manager dispatch, then peel who
invokes that vector (still open — not a Ghidra `FUN_*` XREF yet).

#include <stdio.h>
#include <stdint.h>
#include "src/core/col1_bridge.h"

int main() {
    printf("I need to link against the engine to parse the savefile, but I can just grep the unit test!\n");
    return 0;
}

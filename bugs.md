This is a user-maintained list of bugs. User puts reports in. Agents may annotate with a resolution (Status FIXED + one-sentence resolution) and then MOVE the row to [docs/archive/bugs_fixed_pending.md](docs/archive/bugs_fixed_pending.md) so this file stays small (OPEN rows only). User verifies rows there, then they are moved to [docs/archive/bugs_closed.md](docs/archive/bugs_closed.md).

IDs in the `#` column are permanent and never reused; the archive holds #1-#951. **Next free ID: 952.** Cite rows as `bugs.md #NNN`. Look an archived row up by grepping `| NNN |` in the archive file; never read `docs/archive/bugs_closed.md` whole.

Status: OPEN = no resolution yet (this file). FIXED / REFUTED = agent claims a fix or refutation, awaiting user verification (docs/archive/bugs_fixed_pending.md, full resolution text there). CLOSED / REFUTED = verified, archived.

| # | Status | Bug Description | Resolution |
|---|--------|------------------|------------|
| 530 | OPEN | The AI first-colony opening is golden-fitted scaffolding, not DOS: `ai_euro_recover_landfall_from_ship` hardcodes seed-100 ship coordinates, `ai_euro_act_ship_first_colony_course` / `_arrival` / the dispatcher wake rules key on `goto == (4,13)`, `fx == 45 && fy == 52`, `c->x + 2, c->y + 6`, and `ai_euro_colony_goals_ship_found` writes primary FOUND goals on LAND tiles at prio `6 + urgency/2`, which no DOS producer does (FOUND code-1 writers are only raw 87983 and 88049, both ocean tiles). Deleting `ship_found` alone breaks TURN3-5. Real fix: drive the opening through the DOS 20e6 ship path (LAB_3558 unload mask, LAB_4d2e ship steps incl. land-target unload, `FUN_521d_5b66` goal walk) and delete the fit layer (`src/core/ai_euro.c`). Side lead: the 5d04 hire-tail `+0x3150` read (`ai_euro_0a60_holds_occupied`, ~raw 93040) still counts passengers. | PARTIAL 2026-09-19 (still OPEN): DOS-literal hull path built behind `AI_SHIP_DOS=1` (default off): `ai_euro_act_ship_dos` = `FUN_521d_5b66` (asm-read: a 0x0b transport with MP spent skips 20e6; switch default exhausts MP) + the 20e6 hull order (LAB_3558 unload mask, newest-first unload loop with per-passenger `FUN_521d_06ae`, colony-sail, delivery/4393, 457e, pre-4d2e gate via DOS `FUN_1427_09dc` which counts villages, far roam, 4d2e, LAB_589e 0x0c commit, LAB_5a78 tail) + `FUN_479b_0972` walk; landers keep act state 1 and their goal bytes; real `+0x314f` facing and `DS:0x9faa` coarse plane feed the scorer; invented FOUND producers off; #528 1-3 clear on. Findings: DOS TURN2's `(4,13)` 'D' course is the far-roam random tile, not a scripted west-explore. With it the Dutch opening matches DOS exactly, the French only with 3 extra draws after the 6d8e reseed (no static DOS site found; suspect the 6d8e UI pump / music-picker `randomize`), the Spanish at no offset tried (it must take N-W-SW to avoid unloading early). Blocker: a live DOSBox-X RNG trace of one AI nation turn under VR_SEED. The fitted default stays until then. Side lead closed 2026-09-22: the 5d04 hire-tail hold compare (`ai_euro_0a60_holds_occupied`, the last passenger-counting copy) is now the goods-only `ai_euro_0a60_goods_holds_used` shared with `ai_euro_0a60_ship_full`, matching every `+0x3150` read at raw 93020-93039; tests 67/67, golden_ai_turns 6/6. 2026-09-22 static + headless pass (no live trace used): (a) the DOS 0a60 turn-top clear (raw 87560-87564) zeroes any act-state ≥10 course whose order code is not '1', so the 'D' far-roam course is dropped and DOS scores at the ship's start tile exactly as the port does — the pre-4d2e gate is not the difference; (b) the only RNG sites in the whole 20e6 hull path are the far roam (raw 88635-88659) and the per-water-direction scorer draw (raw 88733); the colony-sail rng(0,8) (raw 89635) is gated on no military aboard, 90108/90320 are land-unit arms, and aboard passengers draw nothing (their hop-slot byte hold[2] stays 255 through TURN3); (c) of the pre-loop phases only 5d04 draws, and its ship-buy ladder gate `ship_cargo_totals <= census_pop_proxy/2 + colony_counts` is closed for every AI nation in TURN2 (2 <= 1, AI gold 0), so no draws there either; (d) the French residue is a pure RNG phase: drawing exactly 3 extra values at the French turn top reproduces the DOS TURN3 state tile-exact (ship (51,39), goto (50,39), soldier ashore (50,38), pioneer aboard), offsets 0/1/2/4/5/6 do not — and the Dutch result is RNG-insensitive (identical for every offset), so "Dutch matches at 0" was never evidence of stream alignment; (e) a second, independent divergence: the raw 89431-89446 founder fold — DOS demotes the French pioneer to a civilian (local_4a→0 when 053c(rep)+0494 < 1 and no FOUND goal sits on the ship tile), which is why only the soldier lands and the pioneer stays aboard, while the port keeps pioneers=1 and raises mask 0x40; (f) DOS's tile reveal stamps the revealing nation's nibble onto every unclaimed tile it uncovers, water included (TURN2/3 layer3 grids show 0x11/0x21/0x31 blocks around each AI ship) — the port has no such writer; statically inert for the ship scorer (06dc only gates the attack arm) but any later owner-nibble reader on water sees a difference. Remaining leads (no trace needed): find the 3-draw source (unmodelled 5d04 branch, a draw between the King step and the 6d8e reseed if DS:0x83a6 is re-captured, or a per-step scorer draw-count difference), and port the fold inputs (FUN_521d_0656 representative passenger, 052c/0492 scores) so the 0x40 bit matches. Continued 2026-09-22b: FUN_1427_0d38 case 3 confirmed byte-exact as "count type-2 units" (ndisasm 0x7420: `cmp [bp-2],2 / jz inc di`), so the French pioneer does count (local_4a=1); the founder fold cannot demote it either (FUN_521d_03d0 returns 8 with 0 colonies, 052c(soldier rep) = −2, local_58 = 6 → promote arm), and every 0x40 setter in the raw 89485-89520 mask block is true for continent 10 with the saved census tables (0x94a6/0x94e6 all zero) — yet DOS left the pioneer aboard, so a DOS input to that block is still unidentified (candidates: −0x6b5a/−0x6b1a recomputed by FUN_4962_0018 mid-turn, or the DS:0x1740 recall latch that zeroes the whole mask). FUN_465b_0000's timer reseed (raw 75650) fires only on the partial-MP gamble (cost > MP left with MP already spent) — never for full-thirds ship steps — so it is not the 3-draw source. LAB_3558 placement is FUN_465b_0c1e → FUN_465b_0000, whose tail (raw 75830) zeroes +0x314c on every completed move: consistent with the French soldier and both Spanish passengers reading orders 0 at TURN3, but the Dutch pair reads orders 1 on land, so the Dutch reached shore by a different path than LAB_3558 — a lead worth a look before touching the port's unload code. 2026-09-22c: the "3 extra draws" is NOT evidence of a missing draw site — placing the same 3 draws after the ship's first scorer call reproduces DOS just as well, and the French step-2 pick at (53,38) is decided by a 1-point margin (port: W 29 vs SW 28; with any 3-shift W 30 vs SW 30 and the strict `>` keeps the earlier dir 5 = SW, which is DOS's choice). So the real residue is a deterministic scorer term off by ≥1 between W and SW at (53,38); candidates by size: the +8 coarse-fog far-probe term (DS:0x9faa cell of (49,38) vs (49,42), the port restamps that plane itself), the ±2 per-neighbour unseen/presence counts around the far probes, and the water `0x2f77<<2` / 0x1a −16 terms. Next step is arithmetic, not a trace: hand-compute DOS's dir-5 and dir-6 scores at (53,38) from TURN2.SAV (seen plane, layer3, coarse plane reconstruction) and diff against AI_4D2E_TRACE. 2026-09-22d: scorer exonerated — hand-reconstructing DOS's LAB_4d2e terms from TURN2.SAV (facing −2·diff², far-probe +8 on land with a zero DS:0x9faa cell, +4 west lean, +2 per unseen far-probe neighbour via the 0x10<<nation bit of the 4th plane, −2 per FUN_137f_0314 unit-presence neighbour; all 12 RNG sites of the OVL14 20e6 body enumerated from the overlay asm, none hidden) matches the port's AI_4D2E_TRACE totals minus the rng(1,5) draw at every checked step for both France and Spain, and the offset sweep shows the French step-2 flip at k = 3, 7, 11, 13, 17, 21 (any phase works) while no k in 0..24 reproduces Spain. Spain's DOS path must be W,W ((50,53)→(49,53)→(48,53), the only 2-step route the scores allow) and DOS did NOT unload at (49,53) although (48,54) is a valid landing there, then unloaded both passengers from (48,53); likewise France sailed past (52,38)/(51,37) and landed only the soldier from (51,39). So the residue is the LAB_3558 unload gate, not movement: the port raises mask 0x50 at the first coastal tile, DOS raises nothing there and something at the next one. Position-dependent inputs to that block are only the goal probes at the ship tile (FUN_521d_0116 on the 0a60 table, i.e. an ocean FOUND/MIL_EXPAND goal sitting exactly on (48,53)/(51,39)) and the nearest-colony distance; a Spanish/French FOUND goal on those ocean tiles would also explain the 0x40 via `iVar16 != 0` and, with none at (51,39), the pioneer staying aboard there (mask 0x10 alone). The G-table stance (−0x6790) gates the whole block and is 4 in the port for both continents; DOS's value is unknown. Next: dump the port's 0a60 goal table for nations 1/2 at TURN2→3 and compare the FOUND ocean tiles it produces with (48,53)/(51,39); if they differ, the 0a60 beachhead producer (raw 87983/88049) is the real #530 residue. 2026-09-22e: goal-table check done — the port's 0a60 beachhead producer writes the same nation-independent FOUND ocean list for every AI nation, including (50,39) = the French ship's DOS TURN3 goto, and nothing on (48,53)/(51,39), so the 0116 ship-tile probe is 0 in both DOS and port there; DS:0x1740 is set only by FUN_5bfb_13b0 (Euro treaty tick, unmet peers → inert); no colonies exist in TURN2 so the `local_74 < 7` term is dead. Re-deriving the DOS French path from the scores gives (54,38)→(53,38)→(52,39)→(51,39): (52,39) has no land neighbour, so DOS never saw a coast before (51,39) and France is a pure RNG-phase residue after all (the port went (52,38) on a 1-point rng margin). Spain remains the real anomaly: W,W is the only score-admissible 2-step route, (49,53) is coastal ((48,54) free land, same continent 13 as the eventual landing) and DOS did not unload there; the Spanish TURN3 ship reads state 0 / goto = own tile / plan '9' (not the 0x0c+neighbour a same-call unload leaves, cf. France), i.e. the unload happened in a separate 20e6 call after the arrival tick. Also unexplained: French soldier and both Spanish passengers read orders 0 ashore while the Dutch pair reads 1 (LAB_3558 via 465b_0c1e keeps 1). Leads: the (−2,−2) passenger-parking sentinel (FUN_1427_10be) vs the save's ship-tile coordinates may change which 0a60 / LAB_3558 gates see the passengers; and whether 20e6's unload block is entered only on a specific act-state/MP-spent combination. 2026-09-22f: LAB_3558 entry (raw 89380-89384) is unconditional for a hull outside its own colony (`cargo cap != 0 → || nearest-own-colony dist != 0`), and inside it the passenger count `local_a8 = 0d38(ship,2) − 1` is taken BEFORE `FUN_281f_0948` (stack_set_xy) re-homes the chain onto the ship tile and AFTER `FUN_281f_0920` (FUN_1427_10be) — so whether the count sees parked passengers depends on 10be's parking behaviour on a hull that has already moved this turn; that ordering is the one static lead left for the Spanish (49,53) non-unload and is where a DOSBox-X watch on unit+0x3144 of a passenger during an AI hull's multi-step act would settle it in minutes. 2026-09-22g: guess A ("passenger count precedes the 0948 re-home, so the first 20e6 call after a step sees no passengers") ported and rejected: DOS runs two 20e6 calls per tile anyway (the 0x0c arrival call, then the state-0 call after the 0972 tick), so the second call still unloads at (49,53); in the port it changed nothing for France/Spain and moved the Dutch hull off (48,13) because the start-tile passengers were already homed. Reverted. Every remaining mechanism that would leave Spain aboard at (49,53) but landed at (48,53) contradicts a byte-exact reading somewhere; this row now needs the DOSBox-X watch (passenger unit+0x3144/+0x314c during the Spanish TURN2→3 hull act). Still OPEN; the fitted default stays. |
| 665 | OPEN | REF audit (L, record only): demote arm calls `units_sync_equip_after_type_change` after the type write (units.c ~3220); DOS `FUN_5fef_0352` raw 99437-99464 writes only `+0x3146`. Harmless for Cavalry->Regulars (sprite/name derive from type); needed by the port's armed-colonist body model for Dragoons->Soldiers. Leave unless kit bytes ever feed a REF display. | |
| 711 | OPEN | Colonist audit (L, record only, ties #530): `ai_euro_found_with_unit` tails (ai_euro.c ~6070-6110) release ships latched on (fx,fy+2) to (fx+2,fy+6) and send a Pioneer on (fx,fy+1) to (fx−3,fy+3), cited to test-saves-ai/TURN5 — seed-100 trajectory fits with no DOS counterpart. | |
| 712 | OPEN | Colonist audit (L, record only, ties #530): `ai_euro_unload_settle_first_landfall` recognises only Pioneer/Soldier hulls; a colonist-only wave lands nobody there and the stage runs in front of the DOS LAB_3558 unload mask. | |
| 715 | OPEN | Colonist audit (M, cross-file task): parked (Sentry/Fortified) units get `moves = 0` at refresh and a `park_nights` refund on wake; DOS never zeroes them (raw 6357) and the rotation filter (raw 3745/8757/15778) is `order != 1 && order != 6 && spent < max`. Changing it needs an order filter at every `moves > 0` consumer (~40 gates in ai_euro/ai_contact/ai_king, only 5 test `units_orders_skip_turn`), so it was not attempted in this wave. Also `units_orders_skip_turn` includes FORTIFY/CLEAR_PLOW/BUILD_ROAD where DOS tests only 1 and 6. | |
| 780 | OPEN | Master Planter audit (L, record only): 5952 ARM 1 guards the profession write with `if (elected >= 0)` (ai_euro_colony_jobs.c:2429-2434); DOS raw 95908-95911 writes unconditionally, so an idle-occupation pick gets profession 0xff. Rare (ARM 1 runs after all placement passes); the literal write would put a -1 profession into the port's colonist record, which other readers treat as unset. Leave unless a DOS save shows 0xff colonist bytes. |  |
| 781 | OPEN | Master Planter audit (L, record only, PLAUSIBLE non-issue): Live-Among learner set admits @JOB 0x13 alongside 0x1c/0x19 (ai_contact_actions.c:830-832); DOS `thunk_FUN_1000_a618` (overlays.c:77796) tests exactly 0x1c || 0x19 and sends 0x13 to @LEARNMASTER. Compensates for the port's own pool convention (0x13 where DOS stores 0x1c; `FUN_38fd_46d4` remaps 0x13->0x16 so DOS never has a live 0x13 unit). Do not "fix" toward DOS without first retiring the 0x13 alias. |  |
| 878 | OPEN | Man-O-War audit (L bundle, record/leads): (a) DONE 2026-09-23: `ai_euro_land.c` bVar7 parity now uses the pool SLOT INDEX (`u - ctx->units->units`), per raw 88577 `param_1 & 1`. (b) `ai_king_ref.c:721` 5×5 reveal centred on each landing tile; DOS raw 74217-74219 centres on the target colony (`colony.x-2, colony.y-2, 5, 5`). (c) `ai_king_ref.c:790-800` garrison-need decrement tests each unit's nation; DOS raw 74033-74041 tests only the first stack unit's nation then decrements for every unit with a non-zero attack byte; candidate gather uses `map_tile_is_coastal` where DOS reads colony `+0x1c & 0x40`. (d) `ai_king_war.c` header says `+0x314b = 0x45`, code writes `UNITS_ORDER_AI_SAIL` (=11). (e) `ai_king_crown_ships_in_europe_lane` (ai_king_war.c:180-205) counts crown ships on high-seas map tiles; DOS `DS:0x9456[n]` counts hulls at the Europe columns `x = 240+n / 244+n` (raw 78182-78185) — over-restrictive proxy. (f) `ai_king_ref.c` ~1230 typed fallback "Foreign" for the ally adjective (no-DOS-text rule). (g) `ai_king_offers.c:224-228` offer-time `ai_king_weakest_port` precondition absent from 2022 (burns draws then aborts); payload `hy` is 6 bits (caps 63, maps are 70 rows) — dead today since hx/hy unused by the hire. (h) DONE 2026-09-23: the invented `: 4` repair-threshold fallback is gone; `units_ship_enter_repair` reads the @UNIT 0x5235 column raw (raw 99622-99631). (i) DONE 2026-09-23: the ship-slow inner loop no longer filters by nation — it walks the whole tile chain filtered on hull type only (raw 98530-98588). (j) `ai_king_merc_offer` `ai_king_independence_declared` gate has no counterpart in 2022's body (lead: needs the `FUN_2a1f_0126` dispatcher call site). Leads: `switchD_2000:da9f::caseD_1(unit, nation)` after every 095c spawn in 10f0/0982 (raw 74170/74196/74381/74437) unidentified; `FUN_281f_0e08(x,y,0)` raw 74382 ported as a reveal without cite; `FUN_281f_02d0` slide substituted by `units_try_move_w` (extra LCG draw possible on cost>3 tiles); Naval Power MoW exclusion (reports_military.c:503) rests on `.asm` only; `FUN_281f_06dc` in the 0982 disembark tile test (raw 74222) read as "empty or crown-held", unproven. **2026-09-23: (b), (c), (d), (f) and the first half of (g) are DONE** — (b) the 0982 5x5 reveal now centres on the target colony (raw 74217-74219); (c) the 0982 garrison-need decrement tests only the stack head's nation then decrements for every unit with a non-zero attack byte (raw 74033-74041), and the candidate gather reads the colony record's `COLONIZE_COLONY_FLAG_COASTAL` (+0x1c & 0x40, raw 74001-74003) instead of `map_tile_is_coastal`; (d) the `+0x314b = 0x45` comments in ai_king_war.c now say the port models the goal with its own `UNITS_ORDER_AI_SAIL` (0x0b) and that the DOS order byte is unmodelled; (f) the typed "Foreign" ally-adjective fallbacks are empty strings; (g) the offer-time `ai_king_weakest_port` precondition no longer aborts the merc offer (re-read of 2022: its merc body has no colony pick), hx/hy payload unchanged and the 6-bit `hy` note still stands. Remaining: (e), (j) and the leads.  | 2026-09-24 (e) DONE: `ai_king_crown_ships_in_europe_lane` now also counts crown hulls with `UNITS_ORDER_AI_SAIL` (DS:0x9456 bump timing at raw 89717-89720 = sail-home decision, not arrival), so the one-hull-in-lane gate fires instead of being defeated by same-turn despawn; test case_878e_sail_home_one_at_a_time. Remaining: (j) and the leads. |

| 908 | OPEN | Craft audit (L bundle, leads, from #900 fix): DOS colonist jobs menu FUN_2f2b_348c also (a) lists the leave-as rows @JOB 0x13..0x18 in the same list (port keeps them in the eject popup only, colonies_eject_row_offered), (b) prints a production estimate `(n)` on indoor rows 9..0x11 from the aiStack_12c table filled at the head of 348c (port shows name only), (c) pages the list past 16 rows (`local_62 = 2, local_d4 = 0x10`, 0x62 "More" button; port stops drawing at the frame edge). | 2026-09-24 (b)/(c) NOT DONE, findings only: (b) raw 50762-50775 shows the printed string is not just "(n)" — it's `FUN_1d1d_07a4(local_5e,0xca8)` + (for field rows <9, an extra "n each"-style fragment via resource 0xcac) + the number (aiStack_12c[row]) + a goods-name suffix looked up via `aiStack_c8[row]*2-0x6840`; aiStack_c8 for indoor rows (>=9) is itself filled by `FUN_281f_0cd6` (raw ~50726), not a simple constant, so porting needs that good-id resolution plus a goods-name catalog lookup, not just the existing colony_yield_for_worker number — bigger than the row note at colony_screen_popups.c:334-345 assumed; deferred, left OPEN. (c) confirmed real feature: DOS reflows the same job_ids list at a `local_6` page toggle (0/1) with a literal "More"/page-2 label; port's ColonyJobsView (src/core/colony_screen.h:304-308) has no page-offset field and game_loop_colony.c has no extra-row click handler for it — needs a `jobs_page` int in the view struct, a 17th pseudo-row hit-test in the click handler, and reflowing job_ids[] by `page*local_d4` in colony_screen_popups.c's draw loop (~line 311-318); no code changed, left OPEN. |
| 909 | OPEN | Craft audit (L, leads, static-unresolved): the origin of the skill-match flag `[bp-0x1c]` is unlocated. (c) Train dialog row order for the 1000/1100/1200 ties (FUN_291f_0ed0 = 210d:0d91 sort stub unread). (d) University-without-College seating of a level-2 teacher: DOS tests 8bec(0xd) only; port's colonies_school_owned_tier allows it; unreachable by normal play. (e) @TRAINFAIL %STRING0 in DOS is whatever token 0 held from a previous phase (0416 set only inside the graduation arm); port fills the current colony. | 2026-09-24 (a) doc labels swapped; (b) not surgical — DOS's Custom House sale is interleaved per-cargo inside FUN_364b_0688's single Phase B loop (raw 57238-57300) alongside the produced-mask bit and net-stock bookkeeping for every cargo including food, while the port already applies food consumption at turn_production.c:784 (well before the separate Custom House pass at :1655) and the surrounding Phase B comment block (:1710+) shows the port's produced-mask logic was deliberately reworked to reproduce the same net numeric effect without replicating DOS's single-loop interleaving; moving the port's Custom House call earlier would require re-deriving that interleaving, not a line move, so left as a documented lead only. |
| 915 | OPEN | Blacksmith/Gunsmith audit (L bundle, leads, no fix): (a) Train dialog FUN_38fd_41ce raw 64316-64451 greys rows by `jobid+2` and commits by `rowid-2` straight into the @JOB cost table; self-consistent only if list row id == jobid+2. Port (europe_pool.c ~538-575/700-721, europe_dock.c ~277-313) keeps job_index with the sorted row, the consistent reading; needs a live capture only if a Train price ever looks off-by-two. (b) village teaching a618 draws over bid[0..0xf]; slots 14/15 are reachable in the loop but never fire because FUN_4d56_2154 never writes bid[14] and writes bid[15]=0: do not "fix" the loop bound to 13 (ai_contact_demand.c ~1097-1180). (c) 5952 indoor pass `avail` carry-over seed 0x7fff (ai_euro_colony_jobs.c ~1015) still needs a live capture. | |

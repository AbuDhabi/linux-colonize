This is a user-maintained list of bugs. User puts reports in. Agents may annotate with a resolution. User will verify. Old bugs left in for historical reasons.

| Bug Description | Resolution |
|-----------------|------------|
| Colony UI top bar has text misaligned by a pixel. | CLOSED |
| Starting a game on the Original Americas map has the whole map revealed, no fog of war. Same for Amer2, I think that's the same map. Either way, all game starts need fog. | CLOSED |
| Initial Indian contact popup mentions how many braves they have. It should say how many villages/camps/cities they have. | CLOSED  |
| Founding a colony right next to an Indian settlement is legal (if a poor choice usually). It should not be forbidden. | CLOSED  |
| Food stores depleted popup fires when colony food is zero, even when food production is non-negative. It really shouldn't unless the colony is actively losing food. | CLOSED  |
| Pressing B while having an active unit on a colony joins the colonist to the colony, and the colonist is visible on the population tab, but isn't automatically assigned a job - missing from settlement and minimap. He should be put to work on something. |  CLOSED |
| Transports entering colonies (wagons, ships) should forfeit the rest of their movement points for that turn. They can currently still move after entering a colony. | CLOSED  |
| In colony transport view, cargo loaded onto ships is about 10px too high and 3px to the left of where they should be. |  CLOSED |
| In colony transport view, the transports (wagons, ships) are placed some 10px too high. Move them downwards. | CLOSED  |
| Cargo in transport view (ie. in ship or wagon holds) needs to be bumped 4px up. |  CLOSED |
| Game Options aren't saved if you click away from the popup. They seem to be saved on pressing Enter while in the popup. Both should work. | CLOSED  |
| Map topbar menu dropdown on-hover highlight is blue. It should be light brown. Also the highlight is too low, move them all up 1px. |  CLOSED |
| AI merchantman attacked my caravel. That class of ship can't attack and shouldn't have done that. I could attack a Privateer with my Caravel, too. | CLOSED  |
| The popup following ship combat is messed up. Hard to tell who sunk/damaged who. No indication where the losing ship, if damaged, retreated to for repairs. | FIXED: SHIPDAMAGE/SHIPSUNK only ever filled 2 of the 3-4 GAME.TXT tokens (ship-type names, no nation names, no repair destination) — now fills nation + type for both sides, and a damaged ship actually relocates to its nearest own colony (used as the repair-destination token) instead of sitting at the combat tile doing nothing. |
| The King raising tax is always a choice, whether to Kiss the pinky ring (accept) or have a Cargo party (refuse). On refusal the king forbids trade in the relevant Cargo. | FIXED: when no Europe-bid-eligible/non-boycotted cargo was found the King fell back to a silent "taxes raised" OK popup instead of a choice; now retries with the boycott/price filters relaxed so a cargo is (almost) always nameable and the real choice always shows. Refusal already boycotted the picked cargo correctly. |
| I can see units moving around that I have no units or colonies of my own near. It seems that once fog is lifed, it is permanently lifted. In the DOS game, your units/colonies need to have sight of the tiles for you to detect AI movements, even if those tiles aren't under fog of war. | FIXED: foreign-unit rendering (main map + minimap) only checked the permanent "ever explored" flag; added a live-sight check (own on-map units radius 1 / colonies radius 2, same radii used to grow the explored set) that now gates foreign units specifically — terrain stays permanently revealed as before, only who's currently standing on it requires live sight. |
| Colony food production on the minimap tiles in the colony UI is mismatched with food production in the population view. I had a 1 pop colony making 2 food in the town center, but was shown in the population view to produce-and-consume 2 and 1 surplus. When I added another colonist to produce another 3 food, the food production showed 5 producd-and-consumed 1 surplus. This makes no sense - the colony had no horses to eat extra food. | NOT REPRODUCED (2026-08-28) — needs a screenshot. Checked against your own save `test-saves-play/COLONY09.SAV` (Jamestown, pop 1): town-commons badge 2 food, sum of tile badges 2, population view produced 2, eaten 2 (pop x 2), surplus 0, and the end-of-turn tick moves food by exactly 0. No horse breeding (needs >= 2 horses in store; the colony has none) and nothing else touches the food total, so tiles and the People band cannot disagree here. Two things that may explain what you saw: (a) at surplus 0 the surplus column is not drawn at all, so the "1" right of the food icons is the **crosses** meter (cross icon, 1/turn base), with bells "1" after it; (b) the 2-colonist reading is self-consistent — 5 produced, 4 eaten, 1 surplus. Like DOS, the People band shows produced and surplus but never the eaten amount, which makes the pair look unbalanced. Screenshot of the mismatch (or a save taken at that moment) and I will chase it further. |
| Celebratory SFX should not be played every time the colony UI is opened. That's only when the colony is founded. | FIXED — DOS `FUN_2f2b_6cd4` plays event 0x54 only when `DS:0x34a >= 0`, i.e. when a building has just finished and the screen runs its "new building appears" reveal. Ported as `ColonizeColony.pending_build_reveal` (set by `colonies_try_complete_building`, consumed on colony open); founding keeps its own 0x54 (`FUN_479b_076e`). |
| There should not be a cannonade when the prices fall popup is shown, or when the immigration popup appears. WTF. | FIXED — the cannon was AI-vs-AI combat resolving during end-of-turn, landing on top of whatever popup was on screen. DOS `FUN_5fef_1b0e` only plays the fire/win sounds when its `param_4` visible flag is set (`FUN_465b_0000` passes 1 for the viewport nation or a human side; the AI move scorer at `521d:52aa` passes 0). Port now gates both the event SFX and the military music sting on human involvement. |
| A go-to order should be possible for a ship into the high seas / sea lane tile. In which case the ship should automatically return to Europe on landing on that special tile. (Normal manual movement by arrow keys should not trigger automatic returning to Europe.) In fact, it seems that I can't move a ship onto a sea lane either way. | FIXED — the port denied any eastward step onto a lane tile; DOS `FUN_4720_015c` denies it only when the ship is **already on** a lane tile and steps further east without a Go To / Trade Route order. Entering the lane from ocean now works, Go To accepts a lane destination, and a Go To that ends on a lane tile sails the ship to Europe with its passengers and holds. Manual arrow-key steps onto the lane still just move. |
| Price of processed goods (eg. coats, cloth) falls very rapidly, nearly once per turn. It should not fall this fast. | INVESTIGATED, not changed: traced the EOT market formula (europe_tick_market_prices) — it's byte-exact against 2 captured real-DOS turn pairs (golden_market_prices01). Per the actual DOS disassembly (FUN_38fd_0058), Rum/Cigars/Cloth/Coats use a pressure delta scaled ×100 (`sign*mid*100`), while raw goods (sugar/tobacco/cotton/furs) use an unscaled delta (`mid*sign`) against the same ×100 rise/fall threshold — so manufactured goods correct in ~1 qualifying turn where raw goods take ~20-30. That asymmetry is in the original EXE, not a porting bug: once a manufactured good is being sold steadily its price crashes fast and recovers slowly (attrition is small, ~11-13/turn vs the 400 threshold), same as real Colonization folklore. If you're seeing something DOS itself wouldn't do (e.g. a save where the price never recovers at all even after selling stops for many turns), send the save and I'll compare against a live DOS run. |
| Turn processing advances even when user is in a popup. In DOS, just about everything except animations pauses until user deals with the popup. | FIXED: the per-frame unit goto-pacing / auto-activation-cycle in game_update (game_loop.c) only checked for full-screen overlays (report/menu/Europe/colony/pedia/debug atlas) before letting wall-clock time step units — it didn't check for an open modal popup/dialog (king tax, ship sunk, options, name entry, etc.), so units kept sliding along queued goto orders and the turn kept auto-selecting the next unit while a popup sat on screen. Added `!game_modal_open(game)` to that gate. |
| ↳ (earlier pass) Missionary action to Establish Mission is missing from enter village popup. | Partly addressed — could not reproduce the disappearance itself. The row gating in `ai_contact_enqueue_village_meet` matches `indian_actions_menu.md` row 3 and the existing unit test still shows the row for a Missionaries-type unit at a mission-free village. Two real defects in that gate were fixed: (a) the menu re-derived *which* village it was describing from "some village of this tribe next to the unit", so with two villages adjacent, or none, it could read a different village's mission state — callers that already know the settlement now pass its index (`ai_contact_try_village_meet_unit_at`); (b) the "has a mission" test was `!= 0xff`, while DOS tests the byte as signed (`+5 < 0` = none). If you still see it missing, the village/tribe state at that moment would pin it down. |
| ↳ (earlier pass) Indian alarm isn't shown. In DOS, it's a number of exclamation marks of varying colors drawn on top of the village graphic on the map. | Implemented. `map_panel_render_tribes_on_map` now draws 1–3 exclamation marks above each visible village. The tier and colours are DOS's (`FUN_112b_0790`): the DS:0x54f6 grudge word `>> 5` clamped 0..3, forced to 3 once `alarm_by_player` passes 0x4a, coloured 10/11/14/12 by tier. Since few writers feed that table in the port yet, the lower tiers are also graded off `alarm_by_player` at DOS's own 0x19/0x32/0x4b cuts. Mark shape and placement are the port's, not decoded from DOS — worth a look against the original. |
|Missing ability to select which unit is active (blinking) with the mouse.| Fixed. A press-and-release on the same map tile used to only re-centre the view when a unit was already selected, so there was no way to activate a different one. Both click paths now share `game_map_click_dispatch` (game_loop.c): own colony → open it, own units → activate one (stack chooser for several), else move the tile cursor. |
|On colony founding, a large swath of fog is revealed. It looks like Coronado's bonus is applied even without him.| Fixed — it *was* Coronado's bonus. DOS `FUN_364b_1dd6` runs `for n in 0..3: if has_ff(n, 6) then FUN_13f1_00a6(colony, n)`; the port called the ±5 reveal unconditionally. `colonies_reveal_founded` now takes the col1 save and reveals only for nations that own FF 6, which is also every Coronado owner, not just the founder. Unit-tested both ways. |
|Units loaded onto ships are shown in the transport view in colonies. They should not be; no non-transports should be in that view. Two more problems: 1. Units are only actually loaded when a ship leaves. While in the colony, it's not yet decided whether a unit will leave with a ship. 2. On bringing a loaded unit into a colony, they are automatically disembarked and have no orders (and can move if they had any moves left to begin with).| Fixed. The colony hold strip no longer draws passenger sprites — goods only. Docking now puts everyone ashore on *every* arrival path, not just an explicit player move: `units_try_move` disembarks a transport's passengers whenever it enters a friendly colony, with no orders and their remaining moves intact. Loading stays a departure-time decision — the pre-existing `units_board_sentries_from_tile` already boards the sentried units on the tile when a ship leaves. |
|Population view does not display food shortages properly. It just colors the food production number red. What it should be doing is putting the shortage in the same resource counter as the food surplus (only surplus or shortage is ever shown, never both, often none if all food is consumed). | Fixed. The net-food counter now appears whenever `food_net != 0`: a surplus as before, a shortage as the same counter with the grey food icon and a red number. The fish/grain production pair is no longer recoloured and no longer swaps to grey — production reads as production. Nothing is drawn when the colony eats exactly what it grows. |
| Missionary action to Establish Mission is missing from enter village popup. | Diagnosed from `port_saves/missionary_bug.SAV`: the unit at (46,23) is a proper Missionaries-type unit, and the village to the north (tribe 50 at (46,22)) already carries `mission = 0x00` — **the human's own mission**. Tribe 37 at (47,27) too. With the mission already yours, DOS lists neither Establish Mission (needs no mission) nor Denounce Heresy (needs a *rival's*), leaving exactly the Incite / Cancel pair you saw — so the menu is right and the save data is wrong. Those two are stale: they were written by the pre-fix build, whose first-contact arm and human missionary-adjacency pulse both handed missions out for free. Verified there is no live writer left — 80 Indian turns on a mission-free save with the human as nation 0 create none — and that the save round-trip preserves the byte exactly, so nothing is corrupting it now. The converts arriving at Jamestown follow from the same stale data: tribe 50 genuinely holds your mission, so the (correct) @INDIANSCONVERT event fires. Its rate was also too high and is now capped at one roll per tribe nation per turn — DOS rolls it on the Brave's *move* next to the colony, so a Brave parked there was re-rolling every turn. The two bogus missions are baked into that save and the port has no "abandon mission" action; say the word and I'll clear them into a copy. Earlier findings below still stand. |
| ↳ (earlier pass on the same row) | Not a defect, as far as this can be shown. Driving the real menu against `COLONIZE/COLONY01.SAV` (a live village, met tribe, no mission anywhere in that save) gives `Establish Mission / Incite Indians / Cancel Action` for a **Missionaries-type unit**, and `Live Among The Natives / Cancel Action` for a colonist whose *profession* is Jesuit Missionaries. That split is DOS's own: `indian_actions_menu.md` row 3 gates on unit **type** 3, so a Jesuit Missionary bought in Europe is a colonist specialist and has to be blessed first — colony screen, pick the colonist, eject role "Missionary" (needs a Church or Cathedral). Two real defects in the gate were fixed on the way: (a) the menu re-derived *which* village it described from "some village of this tribe next to the unit", so with two adjacent, or none, it could read another village's mission state — callers that know the settlement now pass its index (`ai_contact_try_village_meet_unit_at`); (b) the "has a mission" test was `!= 0xff` where DOS tests the byte as signed. Villages are not created with a mission (`ai_tribe_add` writes 0xff) and that save has none set; the one path that used to hand them out for free — first contact — was removed with the conversion-popup fix. Missions are also now drawn on the village, so a pre-existing one is visible rather than inferred. |
| Indian alarm isn't shown. In DOS, it's a number of exclamation marks of varying colors drawn on top of the village graphic on the map. | Fixed, and the real cause was upstream of the drawing: **nothing was raising village alarm at all.** `ai_indian_152e_best_threat_nation_stub` — DOS `FUN_4cc6_03f8`, the "which European threatens this settlement, and how much" scan — was a PARKED stub returning "no threat", so the encroachment arm of the village tick never ran and every settlement's alarm word stayed 0 no matter how much you settled, built or cleared next to it. Ported for real (`ai.c`): the 20-tile military-pressure ring off DS:0xc8/0xde, then every colony within distance 7 scored on population, buildings (scaled by difficulty for a human colony), difficulty and distance, halved across continents, halved for the French, then rescaled by the village's own mission. Its output feeds `t->alarm[e]`, which the bump now carries as DOS's int16 instead of truncating to a byte. Verified against `COLONY04.SAV`: villages near colonies climb, distant ones stay flat, French earn about half — guarded by a new `unit_ai` case. On the drawing side, `map_panel_draw_tribe_chrome` reads what DOS reads: the addresses in `FUN_112b_0790` resolve onto the settlement record itself (base 0x54ec, stride 0x12 — the same 18-byte record, tech-at-+2 confirms it), so 0x54f6 is `tribe.alarm[nation]`, not the separate grudge table the first attempt used. Tier = that word >> 5 clamped 0..3, forced to 3 above alarm 0x4a, colours 10/11/14/12, tier+1 marks. The mission flag DOS draws from the same function (tribe+5 ≥ 0, owner's colour from DS:0x848 = {12,9,14,13}, dimmed unless Jesuit) is drawn too. Mark shape and placement are the port's. |
| When a new Founding Father joins congress, after the initial popup, view should change to the second page of the Continental Congress report, and after that to the colonizopedia entry on that founding father. | Fixed. The arrival OK now carries the elected index (`nation_b`); dismissing it opens the Continental Congress report on page 2, and closing that opens that father's Colonizopedia article. |
| "Iroquis accept conversion at Jamestown" is a bizarre popup I've never seen. Conversion is done by establishing a mission in a village, and then sometimes indians come to a colony and spawn a convert in the colony. Also, a village with a mission that is attacked can spawn converts. Check DOS for details. | Fixed. That popup came from an invented per-turn pulse where a human's Missionary standing next to a village silently established a mission and announced it; first contact also handed out a free mission. Both removed for the human — the pulse is now the AI's stand-in only, and the player establishes missions through the @ACTIONS menu. The real event is ported from `FUN_5bfb_022e` (viceroy_unpacked.c:96989): a Brave from a settlement holding your mission walks up to one of your colonies and, at `rng(0,15) < tech + 2` (doubled for a Jesuit mission), an Indian Convert appears in that colony with GAME.TXT @INDIANSCONVERT naming it. Attacked-mission converts (@INDIANSLAVES) are still unported. |
| When SoL membership reaches 50% there's a persistent popup with a choice to rebel or not. I've not seen this in DOS, except maybe at 100%. That popup is normally only shown when the player selects game->Declare Independence. It should not be shown unless you can show that DOS spawns it spontaneously at some threshold of SoL. | Fixed — could not show it, because DOS does not. `FUN_43f7_2564` has no call site anywhere in the decompile; it is reached only from the MENU.TXT @GAME "DECLARE INDEPENDENCE" command. The per-turn check no longer raises the confirm for a human at any SoL; the SoL≥min auto-declare is kept only for the headless/AI path, where nobody can answer a dialog. |
| Clicking anywhere while in a multiple-choices popup selects the current one. It should do nothing instead. Only clicking one of the two options should be considered a choice. | Fixed. On a CHOICE dialog only a click on an option row decides anything; clicks on the body, the chrome, or outside the dialog are swallowed. ESC and right-click still cancel. |
| "Tea Party" never actually happens the "Tea" part is always substituted by some actual cargo Colony models. | Fixed. GAME.TXT's `{%STRING3 Party}` is built by `FUN_38fd_3dc8` as colony name + " " + cargo name, so it reads e.g. "Jamestown Tobacco Party!"; the port hardcoded `%STRING3` to "Tea". Now built the same way and used for @TAXOPTIONS' "Hold '… Party.'" row, the @TEAPARTY body, and the popup title. |
| Clicking the map, even a colony, ends the turn if the End of Turn marker in the sidebar is active. Only clicking the sidebar should then be considered an end of turn press. Clicking a colony should almost always be considered as an instruction to open up the colony UI. | Fixed. The map-viewport End-of-Turn confirm is gone; a click there keeps its normal meaning (open a colony, activate a unit, start a go-to). Only the right-hand sidebar click confirms the turn. |
| "Throwing away excess cargo" should never happen just from filling up from production. If a colony produces more goods than its stores allow, that's normally ignored. Every 100 units of a cargo is reason for a "colony has a new load of <cargo> available" popup instead (gated behind the appropriate notification setting). | Fixed, and it matches DOS exactly. `FUN_364b_0688` reports a loss only for the part of the overflow that predates this turn's production (`if (production < overflow)`); either branch ends clamped at capacity, so production overflow is a silent clamp with no @SPOIL. Losses under 2 tons are dropped and Food is exempt. @CARGOREADY* is now its own event rather than an else-branch of spoilage, fires on every 100-unit crossing (not once per campaign), skips Food, and is gated only on the "report new cargos available" option — the once-per-campaign latch belongs to the separate @TUTORIAL6 hint, which is now emitted too. |
| Indians begging for food ask for 0 food. They never do than in DOS. | Fixed. GAME.TXT's accept row is `"We offer you {%NUMBER0} of our {%NUMBER1 food} as a sign of friendship."` and the port set neither number, so both rendered 0 — and the choice rows were handed to the popup straight from the catalog without any token substitution at all. NUMBER0 is now exactly what accepting hands over and NUMBER1 the colony's whole store, both read from one `ai_contact_beg_food_gift` helper so the label and the effect cannot drift apart, and the rows go through `popup_msg_apply_tokens`. A `unit_ai_contact` case drives the real trigger and asserts the row reads 25 of 100 with no stray token. |
| Converts are shown with their proper graphic in colony UI, but as free colonists on the overland map. | Fixed. DOS has one icon rule for both views: `FUN_112b_0060` routes *every* @UNIT type 0 (Colonists) unit through `FUN_112b_0002(profession)`, whose jump table (CS:0x14, case 8) sends profession 0x1b to icon 0x43 — sprite 66 here, exactly the one `units_job_icon_sprite` already returned for the colony screen. `units_map_sprite` skipped that branch and fell through to the bare @UNIT icon. It now consults the profession table for an unequipped Colonists-type unit; the equipment cases still come first, standing in for DOS's own `type != 0` overrides. Guarded by a `unit_units` case. (Two entries in that table disagree with the same DOS jump table and were left alone as out of scope: profession 23 should be 104 not -1, and 24 should be 61 not 77.) |
| Cargo Party ie. boycotting a good should always pick a cargo the selected colony has. You can't dump 0 tons of something in protest. | Fixed, and DOS agrees. `FUN_38fd_3dc8` fills `aiStack_cc[c]` with the largest stock of cargo c across the human's colonies and skips every cargo whose entry stayed 0, both when summing the roulette weights and when walking them. The port had keyed the candidate mask off Europe's *bid* instead, so it could name a good no colony was storing — and its last-resort fallback then widened to literally every cargo. The mask now comes from actual colony stock; the Europe price stays the weight; the fallback drops only the boycott exclusion, never the stocked one. `unit_ai_king` now stocks the warehouse for the roulette cases and adds one asserting that empty stores mean no boycott and a standing hike. |
| Mission icon on Indian villages should be a cross, of a color dependent on nation and missionary skill. Check DOS. It's not an exclamation mark. | Fixed — it is a cross, and the colour rule is nation plus Jesuit. Ghidra drops the register arguments to `FUN_1b9e_000a` (the rect fill: colour and height on the stack, x in AX, y in DX, width in BX), so the geometry came out of the raw instruction stream at CODE_5:112b:0afd..0b8c — a 5x6 black pad, a 1x4 vertical bar at pad+2 and a 3x1 horizontal bar at pad+1 one row down. Colour is the owning nation's from DS:0x848 = {12, 9, 14, 13}, less 8 for a plain mission and full brightness for a Jesuit one (`CMP CX,1; SBB AL,AL; AND AL,0xf8` is -8 exactly when the 0x10 bit is clear). The cross sits just right of the alarm marks, on the same baseline. |
| Indian alarm icon, the exclamation mark, should be centered, and I'm pretty sure the colors are a bit wrong vs DOS. | Fixed; the whole thing now comes off the disassembly rather than a reading of the decompile. Position is DOS's fixed anchor — first mark at tile_x+6, baseline tile_y+4, each mark stepping 2px (they overlap, being 3px wide) — instead of the previous centred row whose width drifted with the count. Each mark is a 3x7 surround, a 1x5 coloured bar inset (1,1), then one surround-coloured pixel four rows down splitting it into stem and dot. The **count** was wrong too: it is not the alarm tier but the threat score — the loop seeds a counter with `FUN_281f_0316`'s out value and subtracts 4 per pass while it stays >= 0, so `score/4 + 1` marks — and nothing at all is drawn when no nation scores. Colours 0x0a / 0x0b / 0x0e / 0x0c by tier were right, but the final mark is dimmed by 8 whenever that counter has fallen to 2 or less, which was missing; and when the threat is a *different* European, DOS draws a single mark in that nation's own colour (DS:0x848) instead, telling you who they are angry at — also missing. `ai_indian_village_threat` is now public so the renderer reads the same scan the turn tick does (single-pass over the unit pool, cheap enough per visible village per frame). `unit_map_panel` asserts the exact pixel geometry of both the mark and the cross. |
| Name new world popup fails to stop execution until it is resolved, like other popups. | Fixed together with the turn-processing row below. DOS runs every popup as a blocking call, so the code behind one simply does not run until it is answered; here dialogs are asynchronous, so `game_after_unit_action` kept going after `game_try_prompt_landho` opened the @LANDHO name entry — handing control to the next unit (and, with the End of Turn option off, ending the turn) from behind the prompt. Every automatic hand-off now asks `game_turn_flow_allowed` first and parks itself instead (`game_defer_turn_flow`); `game_update` replays the parked hand-off once the map is back on screen with nothing over it. |
| Returning a ship to Europe immediately opens the European Status. It should not. Ships ARRIVING in Europe should open the status screen. Check DOS for details. | Fixed. ORDERS → Return to Europe had its own copy of the sail-away code ending in `game_try_enter_europe`; the H command and the Go-To-onto-a-lane path (`game_ship_sail_to_europe`) never did that. All three now share that one helper, so ordering a ship home only puts it in the Expected lane. The screen opens on *arrival*, where DOS opens it: `europe_tick_voyages` moves the ship Expected → Harbor and raises `open_on_dock`, which `game_finish_end_turn` acts on. |
| Founding Father pedia pages are missing background (should be wood), and the pallette is messed up too. | Fixed, and the two halves had different causes. **Background:** the Colonizopedia *article* view cleared to black — only the category list blitted `WOODPANL.PIK` — so every article, not just a Father's, drew on bare black; the panel is now blitted for articles too. **Palette:** the article view fell through to the map palette. `CC-00..24.SS` are all indexed against `CCBKGD.PIK`'s palette, which differs from WOODPANL's on 45 of the 96 indices a portrait uses (measured) — flesh went black and the outlines magenta. A Father's article now takes its palette from the loaded `CC-nn.SS` itself. That is safe for the rest of the screen: the two palettes agree exactly on 0/7/12/14/15 and on `@COLORS` basic 68 / hilite 149, and differ only in the wood browns at 128-138 (a slightly warmer plank). |
| The only place that time/turn processing is allowed to proceed is the overland map, and only if there ISN'T an active popup. Yet another example of this bug is turns apparently being able to process while in European Status. Things are allowed to happen in places other than the overland map, but then they are always in response to the player doing something. The game should pretty much never proceed with time/turn processing while player is just viewing another screen or a popup. | Fixed structurally rather than case by case. New `game_turn_flow_allowed`: the unit cycle and the end-of-turn processor may only run with the overland map on screen (not menu / Europe / colony / report / pedia / atlas / Hall of Fame / exploits / new-game wizard) and nothing over it — `game_modal_open` **and** `ai_popup_busy`, since a queued-but-not-yet-presented popup is shown at the top of the very next frame and the action that queued it must not race ahead of it. `game_do_end_turn`, `game_wait_next_unit` and `game_after_unit_action`'s hand-off tail all consult it and park themselves (`game_defer_turn_flow`) when it says no; `game_update` replays the parked hand-off after the modal gate clears, and `game_finish_end_turn` drops a stale one so it cannot skip the unit turn.c already selected. This is the async stand-in for DOS returning from a blocking dialog and carrying on where it left off. |
| Colonists embarked on ships arriving from or traveling to the new world on the European Status need to be visible alongside ships. Check DOS. | Fixed. `europe_render_transit_box` drew only the ship icon; the Expected Soon / Bound For slots already carry `cargo_types` + `cargo_professions` per passenger, so each one is now blitted right after its ship in the same wrapping flow, with the dock queue's icon rule (profession sprite for a plain Colonists-type unit, the @UNIT icon otherwise) and the `-2` Artillery tag resolved the same way arrival does. Passengers are cargo, so they get no orders chrome and no selection frame — the ship stays the click target. |
| Colony population numbers missing from colony icons on overland map. Check DOS for where, what's counted and what color. | Fixed from DOS `FUN_112b_0c64` (Ghidra drops that function's register arguments, so the geometry was read off `CODE_5:112b:0ddb..0e79`). **Where:** digits at tile+(7,7); the name label moves to tile+(2,16) from the same code. Both are drawn only in the full-size 16px tile set — the caller passes the name/pop toggles as `(zoom == 0 && DS:0x890 == 0)`, so the smaller zooms show the icon alone. **What's counted:** your own colonies show live population (colony +0x1f); a foreign one shows what *you* last saw (colony +0xba per viewer, floored at 1), so the badge never leaks a rival's growth. **Colour:** white 15, bright green 10 once the Sons of Liberty ≥50% latch is set (+0x1c bit 0x04), bright cyan 11 when the 100% latch (0x02) is set on top of it. **Font:** the two labels use two different fonts, from two separate DS pointers — the badge is FONTTINY (DS:0x89e) and only the name is FONTINTR (DS:0x268a); both are loaded at startup at `CODE_153:75c2:2e5b`/`2e78` from the DS strings `fontintr`/`fonttiny` (EXE 130345 / 130354). The first pass drew the badge in FONTINTR (9px tall, 6px digits) instead of FONTTINY (6px / 4px) — player-caught as "too big"; `colonies_render_on_map` now takes both fonts. |
| If you have a ship with a loaded unit, and you, via the popup menu cancel the orders of that loaded unit, and the unit has remaining moves, that unit should be available to move. This is particularly important in coastal tiles, since this allows disembarking units on board a ship that's out of moves (and hence incapable of triggering the landfall popup). | Fixed. Boarding parks a passenger at `moves_left = 0` as a "don't offer this one" flag while DOS's own spent byte is still zero (a full allotment) — `units_unload_passenger` already knew that and restored the allotment for its move charge, but the tile-stack picker woke a passenger with a bare `orders = 0`, leaving it awake with no MP so every move gate (`game_try_unit_move`, the arrow-key handler) refused it. It now wakes through `units_wake`, which clears the order *and* restores the allotment, so the passenger can step ashore from a ship that is itself out of moves. ORDERS → Activate Unit also reaches a loaded unit now: `units_id_at` only sees units standing on the map, so it falls back to the current selection when that is a passenger whose ship is on the cursor tile. Guarded by `unit_units`. |
| Colonists working buildings in colony UI stand too far apart. If the building is wide enough, have them stand centered horizontally on the building, right next to each other as per their sprite widths. If the building is too narrow for that, squeeze them as needed. | Fixed. `colony_screen_icon_strip_layout` spread the icons across the host rect's whole width (first at the left edge, last at the right). The row is now packed shoulder to shoulder — one sprite width per step — and the block centred on the rect; when the sprites do not fit, the step shrinks to `(w - sprite_w) / (count - 1)` so the last one still ends inside, and the caller's count badge still appears once the step collapses to ≤1px. Applies to building workers and, being the same helper, to the outside-units row on the fortification. |
| Indians begging for food should ask for half of the colony stores. The popup seems to read the amount of food at the colony wrong too. | FIXED, and the two branches of the choice were also the wrong way round. Re-read of DOS `FUN_5bfb_022e`: the choice returns `local_c`, and it is `local_c == 2` — GAME.TXT's second row, "We offer you {%NUMBER0} of our {%NUMBER1 food}" — that subtracts `colony->food >> 1`. So the half **is** the voluntary gift, and NUMBER0 names exactly it (NUMBER1 stays the whole store, which was already right). The port asked for a quarter, and on top of that had accept and refuse swapped: accepting handed over the quarter *and* scaled the settlement's alarm up, while refusing seized half and calmed them. Now DOS-exact — accept: food −= half, the settlement's alarm word toward that European zeroed, nation alarm walked down 5 at a time from −5 (−10 off a capital) until it sits below 0x47 (that is the "floor-loop" an earlier pass left unreconciled); refuse: no food moves at all, settlement alarm word ×1.5, nation alarm up by `((difficulty + 1) >> 1) + 1`, doubled off a capital. The settlement alarm is now read/written as DOS's int16 (friction | attacks<<8) rather than the friction byte alone. `unit_ai_contact` asserts the row reads 50 of 100 and both branches' effects. Still not modelled: DOS halves the refuse bump when its upstream "calm enough" roll passed, and DOS begs at the colony the Brave actually walked next to where this port sweeps per Indian nation and takes the first colony over 74 food. |
| Foreign Affairs report seems to think that the player nation was withdrawn from the new world. | FIXED. The report is right — DOS prints @MISC 190 "(Withdrawn from New World)" for whichever block matches `head.crown_nation_id` (DS:0x53d2) — but new games were writing that field as 0, i.e. England, so an English player's own block claimed to have withdrawn. It is a "no nation yet" sentinel: both surviving original saves carry `ff ff` there and in the two rival slots beside it (head+98..103, verified by hexdump), and DOS resets all three together at new game in `FUN_75c2_235c`. `col1_save_reset_nation_slots` now does the same from `col1_save_init` and the new-game bridge path, and the report additionally refuses to treat the viewer's own nation as the Crown slot — the Crown is by definition a peer — so saves already written with the zero-filled head read correctly too. Same fix clears the peer-column skip, which was dropping England from every other nation's War/Peace line. |
| I think the sound when you assign someone to work a town hall is meant to be when you assign them to a church/cathedral. | FIXED, and it explains the silver-mine row below too. DOS `FUN_2f2b_2f3e` — the whole assign-colonist path — plays exactly one sound, the `0x8024` chord, and it is gated (2f2b:33dd..33fc): the occupation being handed out must be @JOB 0x10 Preacher or @JOB 0x18 Missionary. It is the *church* chord. The port fired it on all three assign paths (building drop, jobs list key, jobs list click), so every workplace and every field job sounded like a church. Now only Church/Cathedral work rings; field jobs are @JOB 0..8 and can never qualify, so they are silent, as is a Town Hall. |
| Why can't I fortify a ship in coastals waters? Seems to happen only sometimes. Investigate; I might be wrong. | FIXED, and you were right. DOS's Fortify command is `FUN_2b5a_1112`: its only type test is `type < 0x0d || type > 0x12` — the six ship types — and that gate guards the *adjacent-foreign-power war prompt* the command runs first, not the order. After it, `orders = 5` and `turns_worked = 0` are written unconditionally. There is no water condition and no harbour condition anywhere in it. This port refused ships outright on the F key (`units_set_orders` rejected any sea unit) and routed the ORDERS row to a separate `units_order_anchor` that required an own colony on or within one tile — which is exactly the "only sometimes". Both paths are now plain fortify (Anchor is a thin wrapper, kept because MENU.TXT really does list ~Fortify twice and the port names the ship one Anchor to tell them apart), and the DOS `turns_worked = 0` tail is ported too. `unit_units` now fortifies a ship in open water with no colony pool at all, through both entry points. |
| Paul Revere is absent from Continental Congress second page even when he's supposed to be present. Francis Drake, too. Double check all the FFs that weren't in the golden save for the reports. | FIXED, and there was nothing to measure. DOS `FUN_4345_01a6` (4345:01b5..0246) is a plain 25-iteration loop: take the next father id from the draw-order table at DS:0x123a, skip it unless this nation owns it, build "CC-" + a leading "0" below 10 + the id (DS strings 0x1234 / 0x1238), load that sheet, and blit it at the position the sheet itself carries (`ES:[BX+0x46]` / `+0x48` — the per-sprite anchor `ss_load` already parses out of the header). So every father's position lives in his own CC-xx.SS. The previous pass template-matched 10 of them off `continental_p2.png` and skipped the other 15 as "no known position", which is why Revere and Drake never showed. All 25 now draw from their anchors, and the four earlier positions that had matched cleanly (De La Salle, Washington, Jefferson, Franklin) come out byte-identical. The draw order is DS:0x123a verbatim — it matters because the portraits are photo cutouts with opaque margins, and as a sanity check it tracks anchor baseline ascending, 118 to 199, exactly like a hand-authored depth order. |
| Clicking a lone fortified (or sentried) unit on the map (not on a colony tile) should cancel the order. Verify against DOS when clicking on units cancels orders. There's doubless a bunch of gates. | FIXED. DOS's click-on-tile handler (2b5a:1b9b..1dda) ends every path that makes a unit active with `unit[active].orders = 0`, and its single-unit branch clears the order before that as well — clicking is how you take a unit off its post. The gates are the ones this port's `game_map_click_dispatch` already applies before it gets that far: the tile must hold a unit of the nation being played (`units_collect_tile_stack` filters by nation), and an own colony is consumed by the open-the-colony branch first. All three activation paths now go through one `game_click_activate_unit` — lone unit, the `game_owned_unit_at` fallback, and a row picked out of the stack chooser — and the no-moves-left branch clears the order too, since DOS makes the unit active either way. Uses `units_clear_orders`, not `units_wake`: waking also refills the allotment, which is right for a passenger parked at 0 MP on boarding but would hand a free turn to a unit that moved and then dug in. Not modelled: DOS's `FUN_281f_0966` corner, which routes an *already* order-less lone unit through the one-row stack list instead of straight to select. |
| Units on European docks should be printed 2px higher. Also, they should never have the "multiple units on stack" allegiance-orders presentation. Always singular. | FIXED. `EUROPE_DOCK_Y` 140 -> 138 (the hit-test in `europe_dock_menu_hit` shares the constant, so clicks moved with the art), and the dock row no longer passes `show_stack`. The dock lays every immigrant out side by side, so nobody is ever hidden behind anybody — the "more units here" tab is a map-stack device and was always wrong there. |
| Orders-allegiance chrome, when it uses the "multiple units here" presentation is incorrect for common units, like colonists and soldiers. The background rectangle indicating more units should be upper-left not bottom-right. | FIXED, and you had the direction exactly right. DOS `FUN_112b_01ba`'s corner switch is on the raw instruction stream (Ghidra drops the rect call's register args): the three named corners all place the stack rect with a -2/+2-family offset, and the fall-through corner — the one every plain Colonists/Soldiers unit takes — is `112b:0568..057c`, `AX = DX; DEC; DEC` for x and `AX = y - box_h + sprite_h; DEC; DEC` for y, i.e. **up-left**. The port had +2/+2 with a comment claiming it peeked "away from the figure". Corrected; the other three corners were already byte-right. |
| Units loaded onto transports in European Status view should be listed as having Sentry orders on their chrome, not "no orders" like now. | FIXED. `europe_render_transit_box` passed `UNITS_ORDER_NONE` for passengers on the reasoning that cargo has no orders. It does: DOS `FUN_38fd_0718` stamps `+0x314c = 1` (Sentry) on every unit it puts on the dock, and boarding preserves it. Passengers now draw the Sentry letter; they still get no selection frame, since the ship stays the click target. |
| Ships arrive in Europe from the New World way too fast compared to DOS. Same for reaching the New World from Europe. | FIXED against your 2-turn observation (order / End Turn: nothing / End Turn: arrives). The counter itself was already right: `FUN_48d3_0002` returns 1 for an ordinary crossing (2 on a ~10% roll that needs >2 ships and no Magellan), both set-sail paths write it to the unit's `turns_worked`, and `FUN_48d3_03d0` decrements once per tick and advances the lane at 0. What the port was missing is that the departure itself happens inside the same end-of-turn pass that has already run its decrement step, so the counter's first tick lands on the *next* turn — one turn at sea before the count even starts. `europe_tick_voyages` now burns that departure turn (a runtime `departed_this_turn` flag on the lane slot, cleared by the first tick instead of decrementing), which leaves `turns_left` holding DOS's own 1/2 for the save's `turns_worked` byte rather than inflating it. Both directions, since the same tick drives Expected and Bound. Restoring a save does not re-grant the grace turn. Ordinary crossing is now 2 turns, the slow roll 3, and `unit_europe` asserts the ship is still at sea after tick 1. |
| Something is very wrong with the Foreign Affairs report regarding Rebels vs Tories numbers. Despite having a colony with 2 pops and 100% SoL, I had 0 rebels in Foreign Affairs. Foreign affairs seems not to count units loaded onto ships as actual colonists for Rebels vs Tories either, but does appear to count those on European docks. | FIXED, two separate causes, both confirmed against DOS. **0 Rebels:** the report's formula was already right — DOS `3f41:2902` reads `[nation*0x13c + 0x8821]` (that is `nation + 0x19`, `rebel_sentiment`), IMULs it by `census_pop_proxy`, IDIVs by 100, and Tories are the remainder — but nothing in the port ever *wrote* that byte, so it sat at 0 for the whole campaign and everyone was a Tory at any SoL. `FUN_43f7_2424` (43f7:2478..2492) writes it for whichever nation it is ticking, human or AI, before the human-only DS:0x53d0 copy; the port only did the DS:0x53d0 half. Now all four nations cache their SoL there each turn. **Who counts as a person:** DOS `FUN_4962_0018` (4962:0183) counts a unit toward DS:0x9410 when `FUN_281f_0b78(unit) >= 0`, and that resolves to `DS:0x30e[unit_type]` — the per-TYPE profession slot, not the unit's own profession byte, which is what the port was testing. Now shared as `units_type_has_profession_slot` (the map sidebar's private copy of the same DS:0x30e table folds into it). DOS scans the whole unit array, and the port's loop already walks every active unit, so a passenger in a ship's hold now counts exactly like one standing on the map. |
| Ships docking into colonies should auto-disembark their loaded units. This does not always happen when you sail a loaded ship into a colony. They keep being loaded in overland map, but they should be in no-orders mode in the colony fence, possibly with moves if they had any remaining that turn. I don't know why it sometimes happens but other times does not. | Partly fixed; the "sometimes" half is not reproduced. Two real defects found and fixed. (a) The auto-disembark shared a condition with the wagon/ship MP-forfeit rule: it was gated on `units_is_transport`, which for a ship answers "does this hull have goods holds" — a different question from "is anyone riding in it". Ungated: a ship carrying passengers now puts them ashore on docking whatever its hold count says. (b) They were landing unable to move: boarding parks a passenger at `moves_left = 0` as a "don't offer this one" flag while DOS's own spent byte is still zero, and `units_disembark_all` cleared the order but never restored the allotment, so an arrival could not walk on the turn it landed — your "possibly with moves if they had any remaining that turn". It restores it now, the way `units_unload_passenger` already did for its own move charge. What I could not find is a path where a loaded ship enters a friendly colony and the disembark is skipped: player move, arrow key, and Go-To all reach `units_try_move` with a live colony pool. If you hit it again, a save from that turn would pin it. REVIEW 2026-08-31: the ungated disembark went too far the other way — a De Witt trade dock at a *foreign* colony would put your passengers ashore in someone else's town; now gated on the colony being the ship's own nation (friendly-colony behavior unchanged). |
| Hardy Pioneers who appear on docks should always have 100 tools on them. Now they appear without tools. | FIXED, and the port was dropping more than the tools. DOS `FUN_38fd_0718` is the one harbor spawn behind every dock arrival, and it picks the @UNIT **type** from the @JOB profession — 0x14 Pioneer -> Pioneers, 0x18 Missionary -> Missionaries, 0x16 Scout -> Scouts, 0x15 Soldier -> Soldiers or (on `rng(0, bound+4) == 0`, bound = difficulty for a human) Dragoons, anything else Colonists — then stamps orders 1 (Sentry), the profession, and `+0x3159 = 100` Tools when the type is Pioneers. The port spawned every dock immigrant as a bare Colonists-type unit with no kit. Worse, boarding looked the type up by the dock entry's *name*, which is the expert plural ("Hardy Pioneers") and matches no @UNIT type, so every specialist boarded as a plain colonist and landed as one. All of that is now one helper (`europe_spawn_dock_mirror_unit` / `europe_dock_unit_dos_type`), used by both dock-mirror spawn sites, by boarding as the fallback when the name lookup misses, and again on arrival in the New World so the Tools travel with the passenger. |
| Putting a colonist to mine silver produces the church sound effect. WTF. | FIXED with the town-hall row above — same root cause. The `0x8024` chord is DOS's *church* chord, gated in `FUN_2f2b_2f3e` on the new occupation being Preacher (@JOB 0x10) or Missionary (@JOB 0x18); the port played it on every assignment. Field jobs are @JOB 0..8, so a silver mine is now silent, as DOS has it. |
| Incomplete cargo stacks in ships in European Status view suggest they have a full stack. Ie. they are colored, but should be grey. | FIXED. ICONS.SS carries both sets — coloured at 22, grey at 38 — and the map sidebar and the colony transport pane both already switch on a full 100. The European Status hold strip was the one place that always blitted the coloured icon regardless of amount. |
| European Status is missing "equip with tools"/"horses"/"arm with muskets" options when clicking a colonist on the docks. Check DOS.| FIXED — the whole @ARMOPTIONS menu is in. The port's four invented rows (None / Don't board / Board next / Move to front) are replaced by GAME.TXT's twelve, titled from `@EUROPEARM`, transcribed from DOS `FUN_38fd_37xx`: the price/quantity setup at 38fd:3745..3830 (Muskets @CARGO 0x0f at 50, Tools 0x0e at 100, Horses 0x08 at 50, priced with the buy accessor or the sell one by unit type), the per-row enable switch at 38fd:388e..3a04, the add/grey tail at 38fd:3a32..3a7b, and the action switch at 38fd:3ade..3c49. DOS **omits** a row it has disabled rather than greying it, and greys one the treasury cannot cover, so both behaviours are ported, along with the boycott test each arm row runs and the rule that an Indian Convert (@JOB 0x1b) can be neither armed, equipped nor blessed. Type transitions are DOS's own: Muskets take Colonists→Soldiers and Scouts→Dragoons (and back), Tools Colonists→Pioneers, Horses Colonists→Scouts and Soldiers→Dragoons, Bless Colonists→Missionaries; each buy/sell also runs the volume ledger (`europe_apply_volume_price`, DOS 6881/6818) and the sounds are DOS's 0x58 arming / 0x5c horses / 0x8024 blessing. The representational blocker from the last pass is gone: a dock entry now carries a `dos_type`, which drives the dock sprite, the type it boards a ship as, the kit it lands with, the D-key deploy, and the (236,236) mirror unit — and it round-trips through the save, since the mirror is what gets written and the loader reads the type back. `unit_europe` covers the row set for a plain colonist, the buy-row → sell-row flip after arming, the gold charge, and the Soldiers+Horses→Dragoons / Dragoons−Muskets→Scouts transitions. One deliberate difference: DOS leaves the Tools byte set when you sell Tools (the type alone stops it mattering); this port zeroes the kit from the type, which is equivalent in play and cleaner in the save. REVIEW 2026-08-31: the "Cancel Missionary Status" row's gate was inverted vs the raw asm (38fd:39ec..3a09: enabled when type is Missionaries AND profession != 0x18) — DOS lets a *blessed* ordinary colonist cancel and refuses a born Jesuit Missionary; the port had it backwards. Fixed, with both directions asserted in unit_europe. The rest of the 12-row enable switch re-verified byte-exact against the extracted OVL05 disassembly. |
| Recruit popup on European Status can't be operated by mouse, only keyboard. Neither can Purchase and Train popups. Fix that. | FIXED. The Europe input path returned early for every event while a menu was open, so only the key handlers below it ever ran. Popup geometry is now one `europe_menu_layout` shared by the renderer and a new `europe_menu_row_at` hit-test, so the row drawn and the row clicked cannot drift. A left click on a row selects and confirms it in one go, a click off the rows closes the menu, and a right click cancels — the arrangement the rest of the port's lists use. Covers the dock-orders menu too, which had the same problem. |
| Train Popup on European Status view should list the professions in the same order as DOS. | FIXED. `FUN_38fd_41ce` collects every @JOB with a positive hire cost in job order — which is what this port was displaying — and then hands the cost array and the parallel job-id array to `FUN_291f_0ed0` -> `FUN_1cf8_000a` before drawing a single row. That routine is an ascending sort by cost: it scans for the first descending step, lifts that element out, shifts the tail left, finds the first slot whose key is >= the lifted key, shifts right and drops it in. So the list is cheapest-first, opening on Expert Ore Miners at 600 and ending on Veteran Soldiers at 2000. Transcribed rather than replaced with a qsort because the tie order is observable and is this algorithm's own (an element only moves on a strictly descending step and re-enters *before* every equal key): Carpenters before Fishermen at 1000, Farmers before Distiller at 1100, Pioneers before Tobacconists at 1200. Costs come from NAMES.TXT, so a modded table re-sorts the same way. `unit_europe` guards both the ordering and the first row. |
| The three European popups use the wrong fonts. Check what DOS uses. | FIXED — FONTINTR, not FONTSMAL. All four of those menus go through DOS's one generic list dialog (`FUN_291f_0182` -> `FUN_6f74_32a4`), which takes its font from the far pointer at DS:0x1f9e/0x1fa0. That global's standing value is DS:0x268a, the FONTINTR handle: startup writes it there (75c2:2306), and the single place that swaps it to FONTTINY (DS:0x89e) restores FONTINTR immediately after. The port already loads FONTINTR as `intro_font` (its own comment calls it the "title/dialog default") but drew these popups in `menu_font` = FONTSMAL. One `europe_menu_font` getter now feeds both the layout and the renderer, so the row pitch the mouse hit-test uses is the row pitch actually drawn. |
| Orders-allegiance chrome uses incorrect color for the orders symbol. It should be normally black, at least AFAIK. It is a dark orange kind of color instead. | NOT CHANGED — the port already does what DOS does, and I could measure it rather than argue it. DOS `FUN_112b_01ba` (112b:06e6..0708) sets the letter ink: orders 1 (Sentry) or 6 (Fortified) take the nation's @COUNTRY colour **minus 8** for a European (`local_2a < 4`) or a flat 8 for a native; every other order takes 0, black. So black *is* the normal case, and Sentry/Fortified genuinely are not. Confirmed pixel-for-pixel against `original_screenshots/europe/main_with_caravel_and_two_colonists.png`: the two sentried dock colonists' orders boxes are filled pure red (255,0,0) with the 'S' drawn in (170,0,0) — England's DS:0x848 colour 12, less 8, is 4, exactly what `unit_chrome_letter_color` computes. The dark orange you are seeing is that same subtraction on a different nation: Spain's 14 - 8 = 6, a brown/dark orange; the Dutch get 13 - 8 = 5, France 9 - 8 = 1. If you saw it on a unit that was *not* sentried or fortified then something is passing the wrong orders byte and I would want to know which screen — say the word and I will chase that instead. Two later DOS overrides this port does not implement are noted for later: `local_20` (a foreign ship's hold-count digit) forces 15, and `local_1a` forces 12 or 15. |
| I'm pretty sure that the multiple-units chrome should also apply to player units carrying passengers. Check DOS. | FIXED. DOS decides the tab with `FUN_1427_0002` / `FUN_1427_004a` (112b:01de..01fa): seek to this unit, ask for the next one, draw the tab if there is one. That walk is over the unit chain — the same next/prev links that tie a ship to the units riding in it, not a tile-only scan; it is why `units_collect_tile_stack` already has to pull passengers out of ships standing on a tile. The overland draw was computing the flag from `units_count_on_map_tile` alone, and a passenger is not on the map, so a loaded transport alone on its tile showed no tab. Now `on_tile > 1 || cargo_count > 0`. |
| When a unit is activated (blinking) on a colony tile and there's more units there (not in the colony; just on the tile), the port shows the first unit of the rest of the units when the active unit is in its off-blink. It should not. Only one unit should be shown, or none (off-blink). It should not blink between two units, because then it is confusing which one is active. | FIXED, and generalised past the colony tile. `units_top_on_map_tile` skipped the selected unit on the blink-off phase and then fell through to the rest of the stack, so the tile alternated between two units. The active unit now owns its tile outright: drawn on blink-on, tile left empty on blink-off, whatever else is standing there (a Go-To unit does not blink and so never yields). A tile with nothing selected still draws its top unit as before. The colony-tile case already returned nothing because of its own `on_colony` skip, which is why the existing test passed; the plain-ground case is the one that was wrong, and `unit_units` asserted the old behaviour explicitly — that assertion is updated. |
| The selection rectangle in the colony transport view is too large. Units should have selection rectangles closely hugging the sprite presentation. Looks to me they're 2px too far each direction. Almost sure the rectangle size should be the same as for the units in the multipurpose-military view.  | FIXED, and your 2px was exact. `unit_chrome_selection_frame` padded by `UNIT_CHROME_STACK_PAD` (2) on top of its own 1px margin and measured from the black shadow rather than the colour sprite, so the frame sat 3px proud on every side. Measured the real thing off `original_screenshots/europe/main_with_caravel_and_two_colonists.png`: the selected dock colonist's green frame is 18x18 at (232,137) around a 16x16 icon — a 1px margin on the sprite alone, with the shadow and the orders badge inside it. The frame is now exactly that, which also makes it the same size as the plain-icon frames the colony strips draw (`colony_screen_draw_icon_selection`), so transport, Units pane and the outside strip all match. |
| If there are units in the multipurpose-military view, one of them should always be selected. Same for the population view and transport view. Check DOS for exact "which one is selected" rules. | FIXED. The transport strip auto-selected only when exactly one transport was docked, so two ships in port left neither highlighted; the Units pane and the People band never auto-selected at all. All four selections (`transport_unit_id`, `multi_unit_selected_id`, `selected_outside_unit`, `selected_colonist`) now fall back to the first entry whenever they are unset and the list is non-empty, and drop back to none when the list empties — done in the same `colony_screen_refresh_transports` / `refresh_outside` passes that already re-validate a stale selection, so it holds after every change rather than only on open. The "which one" rule is first-in-list; I did not find a DOS routine that picks differently, so if the original has a preference (highest defence, say) that is still open. |
| Units in the multipurpose-military view are drawn too high up. 20px or so too high. Check DOS. It's also inverted; bottom row is first units, upper row is further units. The lower (first) row should have up to five units, the upper as many as there are (squeezed if necessary). | FIXED to your spec, and the offset works out to 21px. The layout filled top-down from the pane's content top (py = 139 after the 8px title), where the bottom row belongs at `py + pane_h - row_h` = 160 — the 20-ish you saw. It now grows upward from the bottom: the first five units on the lower row at their natural width, everything past that on the row above with the step shrunk to `(pane_w - icon_w) / (n - 1)` so the last one still ends inside the pane (the same squeeze the building worker strips use). One layout function serves both the draw and the click hit-test, so they cannot drift. The five-per-row figure and the bottom-up order are from your description, not from DOS — I did not find the routine; the 160 baseline is arithmetic from the pane rect. |
| Transport view in colony UI selection rectangles now too narrow. Multipurpose-military view selection rectangles too narrow, too. Should be square, I think, just like transport view rectangles. And should be generalized everywhere ships or military units are selectable EXCEPT a) the settlement view in colony UI, b) population views inside colony UI and c) overland map. Do check how does draws these squares/rectangles for selection and where it draws them. | FIXED. You were right — square. DOS draws the selection around the fixed 16x16 icon *cell*, not the sprite: the military view's box (asm 2f2b:1f7f..1faa) spans x-1..x+16 by y-1..y+16 — 18x18 — whatever unit is in the slot, and the Europe screenshot shows the same 18x18 around a dock colonist whose colour sprite is only ~8px wide (ICONS.SS unit sprites vary 6..16px wide, which is why the sprite-hugging frame from the earlier pass looked narrow). `unit_chrome_selection_frame` now returns a fixed 18x18 at (x-1,y-1); that one helper serves the transport strip, the military view and the Europe dock/harbor frames. Settlement view, People band (`colony_screen_draw_icon_selection`, tight boxes) and the overland map are untouched, per your exception list. |
| Too many selections in settlement view and too many selections in population view - should be 1 and only 1 each. May have something to do a ship-loaded unit brought in, not properly cleared from being loaded. | FIXED. Not stale loading state — a regression from the earlier "always have one selected" pass: `colony_screen_refresh_outside` auto-filled BOTH `selected_colonist` and `selected_outside_unit` whenever either was unset, and both the settlement view and the People band draw both groups, so two boxes showed at once. The pair is now one mutually exclusive selection: the click handlers already cleared one when setting the other, and the auto-fill fallback now only fires when *neither* is set (preferring the colony population). The Units pane and transport strip keep their own independent single selections. |
| Population view food-crosses-bells line can overflow out of its box. Check DOS how wide it can be. | FIXED. The per-column 14px minimum could push the row past the People box: widths are proportional with a floor, the rounding difference was absorbed only by the bells column and then re-floored, so e.g. one huge column plus three floored ones summed past the 102px band and painted into the transport pane. Widths are now shaved back (widest column first, never below the floor) until the row fits — 4 floored columns are 68px, so it always terminates inside the box. DOS-wide: the whole row lives inside the People band (COLONY_PEOPLE_W=118). |
| Resource counters in colony UI always display numbers. Clicking on resource view interior should toggle whether that should be the case (I think this toggle is saved in saves somewhere, and is game-wide, applies to all colonies), or if only some should be shown (check DOS for what counts as subitizable or not). | FIXED, and the DOS rule is now exact. DS:0x336 is the persistent, game-wide numbers toggle — colony key 'N' (2f2b:667c) or a click (2f2b:609e; here: the area view's non-assignable centre tile) flips it and refreshes. Each pane draw copies it into DS:0x70, and the shared icon-row drawer FUN_1097_0174 prints the count when DS:0x70 is set OR when the per-icon step collapsed to 1px with count>1 — that is DOS's subitizable cut: with numbers off, 5 corn icons at a readable pitch stay bare but 14 fused ones get the label anyway. Both states golden-confirmed against new_amsterdam_production(.numberless).png (the numberless capture keeps 14/22/16, drops the 5). People band stays unconditionally numbered (its DOS drawer never consults the flag). Not persisted to saves — DS:0x336 is a plain DS global in DOS too; it survives between colonies in one session. |
| English Expeditionary Force on Continental Congress page 1 is empty. Did only Dutch get implemented? | FIXED. Nothing nation-specific — the Dutch golden came from a real DOS save that already carried REF values; the port never seeded the force at NEW GAME, only at the Declaration, so any port-started campaign showed empty bars. DOS seeds it in the new-game init (75c2:360b..3643, diff = DS:0x53a6): regulars 8*diff+15, dragoons 5*(diff+1), man-o-wars 3*diff+2, artillery 6*diff+2, then the tax events grow it. Ported into the new-game bridge; the declare-time re-seed (which DOS does NOT do — FUN_43f7_1a26 writes only the backup_force pool) is demoted to a fallback for old zero-REF saves so an accumulated force is no longer shrunk at declare. |
| Recruit/Purchase do not properly show which options are affordable. | FIXED. Train already greyed; Purchase rows now grey (colour 8) when gold < cost, and Recruit greys the candidate rows when gold < the passage price. Verified against original_screenshots/europe/train.png (gold 1000: rows at cost 1000 still bright, 1050+ grey — the cut is cost > gold) and purchase.png. |
| Recruit/Purchase/Train use improper fonts and styling. Check DOS. | FIXED against the three DOS screenshots. Styling: the invented one-line titles are replaced by the GAME.TXT prose (@RECRUIT with the passage in %NUMBER0, @KINGRECRUIT, @PURCHASE), word-wrapped, drawn green with the {braced} words in the highlight ink; row text is tan (14); costs are right-aligned as "(Cost: N)"; Recruit's top row is "(None)" (the others "None"); and the "(F1 for Help)" footer sits bottom-right in green. Fonts: @KINGRECRUIT carries @smallfont, so Train renders in FONTTINY while Recruit/Purchase/dock stay FONTINTR — layout and mouse hit-test share one function per menu, so the row pitch follows the font. |
| Multipurpose-production view puts number (if number should be printed at all) for a resource in the improper place if there are very few kinds of produced cargoes. If there's 1, then icons go in the middle, but number goes in the upper left, etc. | FIXED. The count label was pinned to the cell's top-left corner while a lone icon centred itself in the (often much wider) cell, so few-cargo colonies read as a stray number plus a distant icon. The number now anchors to where the icon run actually starts — same position as before for multi-icon runs, adjacent to the icon for the centred single-icon case. |
| Multipurpose-military first (bottom) row squished. May be related to rectangles being narrowed by previous bugfix. | FIXED with the selection-rectangle row above — the layout is now DOS's own (2f2b:1ecc..1ffd, click 2f2b:59a0): bottom row of five full icons at x=0xd5, y=0x9e, step 0x12 (18px, no sprite-width packing — the squish came from a 12px step derived from the first sprite's width), and units past five go on up to two overflow rows above (y=0x98, 0x90) as 3x5 miniatures at step 5, 17 per row, exactly like DOS's scaled-blit path — not squeezed full sprites. Selection: 18x18 around a bottom-row cell, the small 5x8 DOS box around a miniature. |
| Orders symbol is incorrect color for "no orders". Fortified and fortify differ by color as well. Sentry is different from no-orders in color, I think. Check DOS. | FIXED. The rule itself (DOS 112b:1996..19b8) was already ported right — letter ink is BLACK for every order except Sentry (1) and Fortified (6), which take the nation colour −8; Fortify-in-progress (5) is black, so F changes colour overnight when it completes — but `unit_chrome_blit_unit_for_palette`, the wrapper every palette-adapted screen draws through (colony views, map sidebar, Europe, combat analysis), overrode the letter ink with the nation shade for EVERY order, erasing all of those distinctions. It now applies the nation-shade override only for Sentry/Fortified and forces black (index 0, universal) otherwise. |
| Multipurpose-production view still shows numbers in relation to resource counters incorrectly. Wrong location of number; it should be on the first icon's position. Not somewhere above. | FIXED. The horizontal anchor was fixed last pass but the number still sat at the cell's top edge while the icons centred themselves lower. It now draws at the first icon's own position on both axes. |
| Resource counter subitization rules should also apply to population view resource line. Now it's always numbered. | FIXED — the People band's four counters (fish/grain, net food, crosses, bells) now go through the same rule as every other badge: numbers toggle on, or icons packed into an uncountable smear. |
| Assignment of colonists to work colony tiles is different from DOS. In DOS, for instance, clicking once on an empty tile put that colonist to work there as a farmer. Clicking them again once on that tile brought up the selection of what actually to produce. Check how DOS does it. | FIXED from DOS FUN_2f2b_3fa6 (the area-tile click): a tile worked by the *selected* colonist opens the field-jobs popup (348c); a tile worked by someone else just selects that colonist; an empty tile assigns the selected colonist immediately with a default job — a specific field job 1..7 is kept, while Farmer/Fisherman/non-field re-derive from the tile (3fa6 re-checks ocean_or_high_seas exactly when `job > 8 || job == 0 || job == 8`): Fisherman on water, Farmer on land. The port used to open the picker on every click and never had the one-click default assign. |
| Putting colonists in a dockless colony to work as a farmer on a sea tile is possible (and they would produce a food!). In DOS trying put a colonist in a dockless colony on a sea tile is prevented by an info popup; because it tries to default to fishing on sea/lake tiles, and farming on land tiles. In DOS, you can technically put a colonist to produce zero of something by first selecting that something, then moving them to sea tile (say: as a lumberjack), but then if you try to make them fish, you'll get the popup that you can't. Farming a sea tile should produce zero food or anything else. Only fishing produces food (fish). Everything else, when even possible, should produce nothing. Check DOS. | FIXED with the row above. The default-assign path raises GAME.TXT @NODOCKS ("We cannot operate fishing boats at this colony until we build {Docks}") instead of assigning when the derived job is Fisherman and the colony has no Docks/Drydock/Shipyard; picking Fisherman from the jobs popup hits the same gate. A kept job 1..7 on a wrong tile assigns fine and produces zero (the yield tables already give ocean 0 for everything but Fisherman, and Fisherman itself is docks-gated to 0 in the production pipeline) — your "lumberjack at sea" path works exactly as described. |
| Verify if the popup with selection of what kind of resource to produce on a tile has the proper amounts of production. | FIXED — it could disagree with reality. The rows' amounts skipped the SoL field bonus and the colony's SoL latch bits (`colony_yield_for_worker(..., 0, 0)`), which turn.c's actual production folds in; the popup now passes both, same as the turn tick and the on-map badges. (Henry Hudson's fur doubling was already applied.) |
| When a tile has a colonist working there as a profession that produces nothing, there should be a red slashed-circle icon (icon 64) over the normal icon of what's normally produced there instead of nothing. Ie. if putting a farmer on a sea tile or mountain, then there should be a corn corb icon with a red slashed-circle icon superimposed on it.  | FIXED. A worked tile whose job yields 0 here now draws the job's normal produce icon (corn for a farmer, fish for a fisherman, …) with ICONS.SS #64 — the red slashed circle, 16x16 — superimposed, instead of drawing nothing. |
| Loading the game seems to play the "I'm about to tax you" royal sound. That's not quite appropriate. | FIXED. The port cited "75c2_20e2 load -> 0x3e", but that 0x3e queue actually sits in the King-portrait screen (the code right before it builds the "KING1" art path) — the load function's own tail (75c2:2330) queues 0x25, the default main theme. Load now plays 0x25; the royal-audience tune stays with the King. |
| Soldier fortified in a colony with a ship instead became sentried, vanished from the military view, and was apparently boarded onto the ship. Fortification has no interaction with boarding ships in DOS. And being sentried does not automatically load units onto ships - that only happens when a ship leaves the tile, similar to European Status; first in line, first picked up. | FIXED. Two port-invented commands — ORDERS "Load Cargo" and the O key — resolve "the land unit" as whatever units_id_at finds on the cursor tile, and units_board force-writes sentry-aboard; with a ship selected and the cursor on the colony they silently grabbed the fortified soldier (sentried + vanished from the military view + in the hold, exactly the report). Both now refuse a Fortifying/Fortified unit — fortification has no interaction with boarding, as you said. Departure boarding was already sentry-only, first-come-first-boarded. |
| In DOS, when in View Pieces mode, and there is no colony or own units on the tile, and there are no units left in the control queue, pressing Enter or Space should end the turn (just the same as clicking the sidebar; the End turn info is already flashing). Reproduce this behaviour in port, because it's either broken or missing. | FIXED. Enter (when the cursor tile has no own colony/unit to act on) and Space now confirm the turn whenever the End of Turn prompt is flashing (view-pieces, control queue empty) — same path as the sidebar click. |
| Crosses are missing from Religious report. | FIXED. The report reads nation[human].current/needed_crosses, but live play only ever updated the EuropeScreen copy — the nation copy was refreshed solely by the save/load bridge, so a running campaign always showed an empty bar. The turn tick now mirrors the Europe values into the col1 nation record each turn. |
| Indian Advisor fonts are off. I mean, they're legible, but weird - the tribe name and tech level. Check DOS. | FIXED against indian.png: the tribe name and tech level are FONTINTR (mixed-case serif, shadowed) — the earlier pass had dropped every line to FONTTINY, which is right only for the stats row. reports view now loads FONTINTR; stats spacing bumped 1px for the taller name row. |
| Indian chief flair seems mismatched regarding their alarm level. The little-to-no-alarm one is right, but the further ones need checking vs DOS. | FIXED from the raw asm (OVL06 053d..05d2): the portrait is ICONS.SS #114 + the alarm quartile — indian-euro alarm word bucketed at 25/50/75 (FUN_281f_0a60) — forced to the top tier for an extinct tribe ([rec+3] & 0x80). The port had #113 hardcoded (the sixth portrait this formula never picks); low-alarm tribes looked right because #113/#114 are near-identical art. |
| Lost city rumour that grants a colonist should say the nation name (eg England), not the new world name (eg New England). | FIXED. @LOSTCITY9's %STRING0 now gets the Crown nation from NAMES.TXT @COUNTRY (England/France/Spain/Netherlands) instead of the player's new-world country_name. |
| Initial selected project on colony founding should be Docks, I think. I'm not sure what a landlocked, lakeless colony would have instead. Port currently has "none". Check DOS. | FIXED. New colony's first project: Stockade when the population already qualifies (3+), else Docks for a coastal site (seed-100 golden AI towns all start on Docks), else Warehouse for a landlocked one (your correction — thanks). Unit-tested both coastal and landlocked. |
| Construction project selection popup has incorrent text styling (color, text, exact positioning of parts of text). Check DOS. | FIXED to what the asm shows (2f2b:5a68 row build / 5bd2 dialog): rows read "Name (N Hammers)(M Tools)" with the full cargo words — N is cost minus banked hammers, floored at 0 — not the invented "(52H, 20T)" shorthand; the dialog uses FONTTINY (DS:0x89e handle at 5bf7, already the port's font here); and past 14 rows it goes two-column, 16 rows per column (5c05..5c17), which the port now does too (draw + click hit-test share the layout). DOS also appends "(COMPLETE)" (@MISC 127 via DS:0x2eb8) for an already-built row — the port's list never shows built rows, so that branch has no port equivalent. |
| Founding father colonizopedia entries do not include the portrait used in continental congress report page 2, and should be dismissable by clicking anywhere, not just Escape.  | PARTLY FIXED. Click-anywhere (left or right) now dismisses the article like Escape. The portrait half did not reproduce statically: the article renderer already blits sprite 0 of the father's CC-nn.SS at (8,28) with the sheet's own palette (the row-50 fix), the sheet loads on open and on L/R navigation, and the numbering matches the Congress page 2 loader. If you still see no portrait, tell me which father and how you opened the entry (P-list vs the arrival chain) and I will chase the path.  **Update (2026-08-31 Colonizopedia DOS recreation):** static decompile of the DOS father article builder (FUN_6cb2_1f28) shows DOS draws *no* portrait — father pages are title + body text only. The article pages were rebuilt to DOS fidelity, so the portrait was removed again; any key or click dismisses. If the portrait is wanted anyway as a port nicety, say so and it can be re-added on top of the DOS layout. |
| Indian raid was triggered, but only the woodcut appeared. No combat analysis, despite it being turned on. Was it covered up by the woodcut? | INVESTIGATED, not covered up. The colony-stores raid in this port resolves through banded odds (the 1/13-bare / Stockade-scaled "raiding party wiped out" bands), not through a real engagement — DOS's deep raid combat (FUN_5fef_0f14's attack against the colony's defense) is still PARKED port debt, so there is no engagement for Combat Analysis to show; the woodcut isn't hiding anything. A raid arm that fights an actual adjacent unit (the ambush arm), and a Brave stepping onto a defended colony tile, DO run real combat and do present the analysis. If you want the full DOS raid combat (with its analysis popup) prioritized, say so and it moves up the queue. |
| Recruit and Purchase popup font coloring is wrong. Shading, mostly.  | FIXED. Every text in the four Europe list dialogs (title prose, rows, right-aligned costs, the F1 footer) now draws with DOS's 1px black drop shadow, matching the recruit/train/purchase screenshots. Colors unchanged. |
| European Status ship selection rectangle is wrong size. I thought we fixed it elsewhere. | FIXED. The harbor strip still had its own sprite-hugging border (sprite_w+2) — it now goes through the same unit_chrome_selection_frame as everything else, i.e. the fixed 18x18 cell box. (The dock immigrants were already converted; the harbor strip was the one missed.) |
| Activate Unit (A) seems to give units their movement points back. It should not. That thing is there to pick which unit is currently being controlled (blinking). | FIXED. units_wake refilled the allotment unconditionally. It now refills only where the zero was a PARK, not a spend: a passenger (boarding parks at 0 MP), or a unit coming off Sentry/Fortify/Fortified (whose zero is the standing-order skip flag, DOS spent byte 0). A unit with no standing order or mid-Go-To keeps whatever it has left — activating just picks who is controlled. |
| Go-to orders on lost city rumour one tile away fail, but arrow key orders onto that tile succeed. | FIXED. The one-tile Go To branch pre-checked units_can_afford_move_cost, which refuses a partial-MP overspend outright — but an arrow-key step hands that same situation to units_try_move's DOS overspend roll. The pre-check is dropped; the adjacent Go To step now goes through the same roll as the arrow key. |
| In European Status, when you set your last ship currently docked there to sail to the new world, it should automatically close the European Status and go back to main map. | FIXED. Sailing the last docked ship closes the European Status back to the map. |
| Somehow, a caravel is able to pick up no-orders or forest-cutting pioneers. It should not. Ships should only take on (when leaving) passengers from the list of SENTRIED units on the tile. Nobody else. And they should only be able to take as many passengers, as there is room in their holds (cargo occupies slots that passengers could go into). Check DOS, because this has been a source of persistent variation in bugs. | FIXED, two real defects. (a) The save/load bridge boarded every land unit sharing a docked ship's tile chain unless it was Fortifying/Fortified — a no-orders or plowing pioneer beside a docked ship came back aboard (and sentried) after any save round-trip. It now boards ONLY units whose saved orders are Sentry, which is what DOS stamps on every genuine passenger (the dutch-reports.SAV manifest confirms it). (b) Capacity ignored goods: a ship could take capacity-many passengers on top of full holds. New units_ship_free_passenger_slots = capacity − passengers − goods-occupied holds, used by both boarding paths; departure pickup itself was already sentried-only, first in line first aboard. |
| Impossible to wake up unit with moves that's loaded onto a ship without moves on a coastal ocean tile. Verify against DOS how the multiple units wakeup popup should work. I'm pretty sure there's a click once to cancel, click again to activate in there. | FIXED, and DOS verified. DOS's stack picker (FUN_2b5a_1b5a, static decompile) is a single-pick dialog: choosing a row clears that unit's orders byte AND makes it the active unit in the same click; Esc / picking nothing cancels. There is no wake-first-then-activate two-step in DOS. The port's popup had exactly that two-step — the first click on a sentried passenger only silently flipped its label to "(ready)" and kept the dialog open with nothing selected, which read as "impossible". One pick now wakes (units_wake, restoring the parked passenger's full allotment — the ship's own 0 MP doesn't matter, walking ashore spends the passenger's) + selects + closes, matching DOS. The rest of the chain (stack popup listing ship cargo, wake restoring MP, arrow-key step ashore via units_unload_passenger) was verified working end-to-end with the ship at 0 MP on an ocean tile; regression test added in unit_units. |
| French expeditionary force missing on report. Just check that they're all wired, populated, etc. Not just one nation.  | FIXED. The report and the growth/seed code are nation-agnostic (the Col1 save has one global REF pool) — the gap was historical: campaigns started on a build from before the new-game REF seed existed carry an under-seeded pool forever (your save held [3,0,0,0]: no seed, some tax growth). DOS seeds regulars 8*diff+15 / dragoons 5*(diff+1) / man-o-wars 3*diff+2 / artillery 6*diff+2 at new game and nothing drains it before the declaration, so every pre-WoI save must hold at least that floor (verified: DOS COLONY00.SAV at diff 0 = exactly [15,5,2,2]). Loading now raises each pool to the seed floor pre-WoI, any nation; post-declaration pools are left alone since they legitimately drain as waves land. |
| Ships still appear to take away any non-fortified unit with them as they even enter a colony. ONLY SENTRIED. Why is this so hard to implement properly? Check DOS how Sentry/Load should work. | FIXED. The load side and the live departure pickup were already Sentry-only — the leak was the SAVE side: col1_bridge's pre-export "DOS hygiene" pass boarded every land unit sharing a tile with an own ship, on any tile, and it mutates the live pool (aboard_ship_id set, orders forced to Sentry, MP parked). So the moment the game was saved (incl. autosave) with a ship docked at a colony, the whole non-fortified garrison silently became Sentry passengers, and the ship's next departure took them along — matching "as they even enter a colony". The pass now only boards a land unit standing on a WATER tile (a genuine orphan state the Col1 format cannot represent); land-tile stacks (colony docks, coastal stacks) are left untouched. Regression test in unit_col1_save (dock garrison stays ashore through capture; water orphan still gets boarded). |
| Fog of war isn't the same color as in DOS. In DOS it's a textured blue. That needs to be corrected. | FIXED. DOS's per-tile compositor (`FUN_6ba1_0938`, asm 6ba1:09a2) draws every unseen tile with `MOV AX,0x95` into the sprite blitter — a 1-based index, so PHYS0 **#148**: a 16x16 noise fill of palette 60..62, the same speckle pattern as the ocean tile but one shade darker down the blue ramp (TERRAIN.SS palette 58..60 = ocean blues, 60..62 = fog blues — verified from the sheet's own embedded palette). The port left unexplored tiles bare black. Now blits PHYS0 148 for them; the existing 104–107 fringe on seen tiles toward unseen stays. |
| If you are at peace with a AI Euro nation, the game should prompt you whether you want to attack their units/colonies if you walk into them. DOS does. | FIXED. DOS (`FUN_465b_0000`, viceroy_unpacked.c:75540..75560) never *refuses* an attack on a Euro peer: with a signed peace treaty (relation bit 0x40, either direction) it asks GAME.TXT @HAVETREATY — "We have signed a peace treaty with the {X}" with Cancel Action / Break Treaty; Break clears the treaty, declares war and the move proceeds. With no treaty the attack goes through silently and war opens. The port instead bounced every not-at-war move with "At peace — cannot attack", with no way past it. New `ai_contact_try_euro_attack_confirm` runs before the move for a combat-role unit stepping onto a foreign Euro unit (land or naval) or a foreign colony tile (even undefended, which was silently capturable at peace before); war goes through the existing `ai_diplo_declare_war_ctx` (embargo/tax/sting chrome included, Franklin gate honoured). Unit-tested: treaty prompts without declaring, pending doesn't stack, no-treaty declares silently, at-war never asks. |
| French REF still missing on new game as France. Same for Spain. Same for England. Same for Netherlands. REF isn't initialized for any of them on game start. WTF. | FIXED, and the earlier fix genuinely never ran for a wizard start. The seed had been put in `game_save_col1_slot`'s lazy no-col1 template and in the load-time backfill — but the new-game wizard builds its col1 through `ai_setup_col1_template` (ai.c), which sets gold/crosses/options/tribes and skipped the REF entirely, so `col1_ok` was already true and neither other site ever fired. The seed (75c2:360b: regulars 8*diff+15, dragoons 5*(diff+1), MoW 3*diff+2, artillery 6*diff+2) now lives in that template too. `unit_ai` asserts a diff-0 new game starts at [15,5,2,2]. |
| I try to activate a ship-loaded unit, but cannot do so. Clicking once brings up the popup. Clicking on the loaded unit cancels the orders and the popup, but does not activate the unit. DOS behaviour is: Click on ship, this brings up the popup with the ship itself selected on it. Click the unit, which selects the unit in the popup and cancels (Sentry) orders. Click unit once more activates it for moving.  | FIXED to your described DOS flow (this supersedes the earlier "single-pick" reading of FUN_2b5a_1b5a). The tile-stack popup now opens with the currently active unit's row (the clicked ship) pre-selected instead of row 0; the first click on a *different* row only moves the selection — cancelling a loaded passenger's Sentry via `units_wake` so it reads ready and gets its parked allotment back — and keeps the popup open; clicking the already-selected row activates that unit and closes. One click on the ship's own row therefore still activates the ship directly, since it opens pre-selected. Keyboard Enter keeps the one-step activate. **Update (loaded_soldier_bug.SAV):** the popup flow itself was verified sound against your save (probe: second click wakes, returns the passenger, selectable) — the activation was being *stolen the very next frame*: game_update's turn-activation queue only recognised an on-map unit as "awaiting the player" (`units_is_on_map`), and a passenger rides in the hold, so the queue immediately handed control to the next unit (or dropped to View Pieces), which read as "not activated". An awake passenger with moves left now counts as awaiting the player, so the selection holds and an arrow key steps it ashore. **Update 2 (blink):** the same not-on-map gap hid the activation visually — `units_top_on_map_tile` only scans on-map units, so with the passenger selected the tile always fell through to "top of stack" and drew the ship steadily. A selected passenger now owns its ship's tile like any active unit: its own sprite on blink-on, tile empty off-blink (the established one-unit-per-blink rule). Unit-tested. |
| REF units squished unnecessarily on continental congress report page 1. | FIXED. The DOS force row goes through the shared icon-row drawer — the 3f41 page builder brackets exactly that line with `DS:0x70 = 1` (the FUN_1097_0174 numbers flag, same engine as the colony panes) — which packs icons at their natural width and shrinks the step only when the run would overflow its cell. The port's invented `width = count × 2.2px` spread was back-solved from a 56-regular golden, and at that size the two rules agree (56 icons capped to the 124px column still land on the ~2px pitch), but a fresh campaign's 15/5/2/2 got crushed into 33px for no reason. Now: natural width `count × icon_w`, capped at the next column's start (screen margin for Man-O-War). |
| Fog edges still black. | FIXED. The 104–107 edge masks are never meant to stay bare — DOS `FUN_6ba1_06e0` (asm 6ba1:0919) follows every mask blit with `FUN_6ba1_067c` filling the mask's colour-0 dither dots with the *neighbour's* terrain art, and the unseen-neighbour case (local_10) forces the draw past the same-class/ocean skips. So the boundary dithers both ways: a seen tile gets specks of the hidden neighbour's real terrain along its edge (DOS genuinely leaks that one-edge hint), and the fog tile — via the unseen path's `06e0(1, is_ocean, 0)` call — gets the seen neighbour's terrain feathering onto the blue. When a land fog tile faces a seen ocean, DOS rescans the ocean tile's even ring entries (its W/S/E/N cardinals, ring table read from the EXE at DS:0xb4/0xbe) for a land class, or skips the edge — the same W→S→E→N rule the port's MAPEDIT land transitions already use. Ported as `map_fog_edge_fill_sprite_at` + `map_fog_reveal_edge_*` (map.c), wired into both sides of the boundary in the compositor; `unit_map` asserts the mask/fill pairs and the ocean resolve. |
| Ships doing the landfall order should not spend any moves doing this. The passengers move, the the ship. | FIXED. The @LANDFALL "Make Landfall" handler charged the ship 1 MP as a "coastal order" — that charge was port-invented (the comment even said so). Removed: the ship now spends nothing. **Update:** the passenger's landfall consumes its WHOLE turn — the @LANDFALL handler now zeroes the landed unit's moves (your correction). A manual disembark (awake passenger stepping ashore by arrow key) keeps its normal terrain-cost charge and remaining moves. |
| Ordering a unit to go-to a fog square is legal regardless of what's under the fog. The player does not know, and the unit should try to go there. Check DOS, I'm sure there's logic for this. | FIXED. `units_set_goto` ran the destination through `units_can_enter` unconditionally — peeking at terrain/occupants the player has never seen and refusing the order ("Cannot go there"). The enterability check is now gated on `map_tile_seen_by` for the mover's nation: an unseen destination always takes the order, and the unit finds out en route — the flood pathfinder seeds the goal tile without an enterability test, so it routes there, and the final step's own move logic bounces/stops it once the tile is actually in sight, which is DOS's shape (order legality is player knowledge; reality is discovered on arrival). A seen destination validates exactly as before, and all three entry points (drag, Go to Place, Go to Port) funnel through the one helper. `unit_units` asserts a ship's goto onto a fogged land tile is accepted and onto the same tile seen is refused. |
| The selection of next Founding Father to elect should be more persistent. If you escape the popup in DOS, it reappears. This is to prevent trying to reroll the choices, which are pseudorandom. I think the port saves the seed, so rerolling is not possible, but the popup reappears only the next turn. | FIXED, and it was worse than a delay: the candidate slate was rolled fresh from the RNG on every enqueue, so escaping and waiting a turn really did reroll the choices — the saved seed only fixes the sequence, not which draw you land on. The rolled slate now persists (`founding_fathers.c`) until a candidate is actually chosen: escaping the debate re-presents it immediately with the identical candidates (DOS behaviour), and the next-turn tick reuses the same slate rather than rolling again. Any candidate elected by another nation in the meantime is dropped from the re-presented list; choosing clears the slate so the *following* vacancy rolls fresh. Not save-persistent — the slate is session state, same as DOS's memory. `unit_founding_fathers` asserts escape re-presents identically, next-turn keeps the slate, and answering still locks the pick. |
| While a unit is actively controlled, left-clicking on the map (tile without own units or colonies) should pan the viewport, not go into View Pieces mode. The player may want to issue a go-to order someplace outside of the viewport, and we need to enable that, just like DOS does. | FIXED. The plain-click fallback in game_map_click_dispatch dropped into View Pieces (tile-select) whenever the clicked tile held nothing — losing the controlled unit. With a unit actively controlled, a click on an empty tile now only recentres the viewport (DOS pan); the cursor stays with the unit, so you can scroll to a go-to destination outside the view and then drag the order. With nothing controlled, the click still moves the blinking tile cursor as before. |
| In colony UI, loaded cargo is somewhat misaligned with the cargo holds as displayed in the background. I'm sure DOS has exact locations where these should be going. | FIXED, measured off the art itself: COLONY.PIK's transport pane paints six 9x12 hold-box interiors at screen x = 128 + i*12, y = 165..176 — the port drew cargo at a 14px pitch from x=128, drifting up to 10px right of the painted boxes by the sixth hold, 1px above them, and covered empty holds on yet another invented pitch. Cargo icons now centre inside the measured boxes, the 10x12 cover (#122) sits per-box on the same 12px pitch, and the click hit-test shares the constants. |
| Implement hotkeys on the European Status. I know it's possible to press U and unload all cargo there, for instance. Check what hotkeys DOS uses on that screen. | PARTLY DONE. U now unloads (sells) the selected ship's WHOLE cargo hold by hold with no amount prompt — your described DOS behaviour — and S sails the selected ship for the New World. Already present: R/P/T (and 1/2/3) for Recruit/Purchase/Train, L/'=' buy amount, '+'/'-' single-unit buy/sell, E/Esc leave. The DOS key dispatch for this screen sits in a corrupted stretch of the decompile (38fd:5xxx jumptable garbage), so this is behaviour-matched, not transcribed; name any other key you remember and it can be added. |
| European Status is sometimes unresponsive until you click a few times. Not sure what's going on. It happens particularly when you unfocus the window and do something else, then go back to the game. | FIXED (probable cause). Alt-tabbing away mid-press — or releasing the button outside the window — meant SDL never delivered the BUTTONUP, so the platform's held-button flags stayed latched and screens driven by press/release pairs (the Europe drags especially) swallowed clicks until the state happened to resync. Losing window focus now clears the held flags and synthesizes the release, so any open drag session resolves immediately. If it still happens after this build, say so. |
| Ordering a ship from a colony directly into high seas via go-to is legal, but when I try to do it, it goes into View Pieces mode on the high seas tile I selected as the destination. Sometimes this happens even when the ship is just on a water tile. | FIXED with the pan row above — same root. Clicking the destination tile was a short click, which dispatched as tile-select (View Pieces on the HS tile you clicked). Now it pans; issue the go-to by drag (press, pull to the lane tile, release) or ORDERS Go to Place — both accept a high-seas destination (the ship then sails for Europe on arrival, per the earlier lane fix). |
| Are we sure the Founding Fathers selection is properly randomized? Or is it simply much more convenient at lower difficulties? Minuit is a high priority pick, and shows up very often.  | NOT A BUG — it is the game's own data. NAMES.TXT @FATHERS carries per-century weights (verified byte-identical in the port's table): pre-1600 Peter Minuit is weight 9 of the Trade category's total 15 — a 60% chance of being the Trade candidate in every debate before 1600 (Henry Hudson similarly dominates Exploration at 10/27, Paul Revere Military at 10/20). After 1600 his weight drops to 1, after 1700 to 0. Difficulty plays no part; the century band does. The roll itself is a plain weighted pick over unclaimed fathers of the type. |
| Clicking on the sidebar when the turn isn't done (ie. units are still waiting in the control queue) should activate the next unit. Presently it does nothing. | FIXED. The sidebar click handler had only two arms (minimap centre / End-of-Turn confirm). Mid-turn — units still awaiting in the control queue — a sidebar click now hands control to the next waiting unit (same cycle as Wait). |
| Ordering a unit to go-to an indian village is legal, but does nothing. | FIXED, two halves. (a) units_set_goto ran the destination through the enterability check, and a village tile fails it (VILLAGE_ILLEGAL), so the order was silently refused — it now accepts an Indian-village destination explicitly. (b) The per-frame goto pacer stops the unit one tile short of the village and hands control back ('Village ahead — awaiting orders'): entering a village is a player decision, and the arrow-key step onto it fires the @ACTIONS menu as usual — the pacer must never walk into one on its own. |
| Indentured servants are rejected by indians as if they were petty criminals for purposes of living among the natives to learn a skill. | FIXED, and the criminal check was broken too — both tested the unit's display name, but Indentured Servants and Petty Criminals are professions on a Colonists-type unit whose display name reads 'Free Colonist'. So a criminal was never refused with @LEARNCRIMINAL, and a servant (profession set, name not matching 'Indentured') fell into the 'we can only teach colonists who do not yet have a skill' master-refusal — your report. Both now test the profession byte (new UNITS_JOB_SERVANT 25 / UNITS_JOB_CRIMINAL 26): criminals get @LEARNCRIMINAL, servants learn normally. |
| Indians are very quick to attack colonies and units, even when not alarmed by the owner. They generally should be rather more chill, unless it's a treasure train - they find those hard to resist. | FIXED, two gates. (a) The big one: a wandering Brave stepping onto a Euro unit opened combat unconditionally — units_at_war_for_move said natives are 'always fightable', both directions. An Indian MOVER now needs real hostility (the Indian-Euro war flag, or alarm at the 0x4b hostile cut) before its step attacks; a Euro attacking a native is unchanged (the @WHACKINDIANS confirm covers the human). (b) The raid arm's adjacent-unit ambush fired at the >=40 raid-gate band; it now needs the >=55 provocation band (the same cut the war-declare escalation uses) or open war. Exception both places: a Treasure Train is attacked at any alarm — they find those hard to resist. |
| The first attack by Indians should generally indicate that the "chief denies involvement"; I don't think it's that way every time, but usually relations deteriorate slowly, and eventually some unit elects to attack. Subsequent attacks don't carry this extra info. Check DOS how that's gated. | FIXED (thin gate; the DOS one sits in OVL13's corrupted-decompile stretch, alarm-delta-shaped). The @INDIANSURPRISE 'chief denies involvement' line was shown for EVERY pre-war colony raid; it is now only used when the tribe's attacks counter against that Euro was still zero before this raid — the first attack is deniable, every later one uses the plain @RAID* chrome. |
| Indians should destroy units they attack and win, not capture them. I just had Indians capture a scout after defeating him. In general, mounted scouts are destroyed, not captured, even by fellow Europeans. | Hardened + the reported native capture not reproduced in code. Every combat loser routes through units_apply_land_loss_outcome, whose capture branch already requires a EURO winner with attack>0 — natives destroy, and Scouts-type units are not in the capture set (Colonists/Wagon/Treasure only) nor the demote table, so they despawn. The reachable hole was a mis-typed scout: a Colonists-type unit carrying horses (seizure, save import) matched the 'Colonist' capture name-test — the branch now also requires no horses and no muskets on the loser, so anything mounted or armed is destroyed, by natives and Europeans alike. If you see an Indian-capture again, a save from that turn would pin the path. |
| Starting gold on lower difficulties is zero. Pretty sure it should be 1000 on lowest and 300 on next lowest. | FIXED, and the gold function was never wrong — the treasury was being CLOBBERED. AI Euro turns borrow the human's one EuropeScreen (write the AI nation's gold in, run voyages/treasure-cash/purchases, write back) and never restored the human's values, so after the first AI Euro turn europe.gold held an AI's treasury — 0 at game start. Every borrow site (ai_euro voyage tick, treasure cash-in, ship/passage purchases, Cortes coastal cash) now saves and restores the human's gold+tax; the two purchase mirrors only write for the human. Starting gold itself was already 1000/300/0+ by difficulty. |
| Indians giving offerings as random event from entering indian village with scout doesn't add money to treasury. | FIXED — same treasury duality as the row above: the beads gift credited only the col1 nation-gold mirror, and the live/spendable treasury is europe.gold. It now credits both for the human. |
| Indians walking into mounted scout apparently check its skill (happened to be a criminal) and then show a popup as if that unmounted colonist walked into their village and wanted to live among them. This is not correct. | FIXED. The village adjacency pulse (FUN_5bfb_022e-shaped) LECTURED any adjacent unit it could not teach — a mounted criminal-scout got the @LEARNCRIMINAL refusal out of nowhere. The pulse now skips ineligible units in silence (criminals, skilled masters); the refusal dialogs (@LEARNCRIMINAL/@LEARNMASTER/…) fire only in the deliberate 'Live Among The Natives' @ACTIONS flow. Also: both the criminal and indentured checks tested the DISPLAY name, which reads 'Free Colonist' for those professions — now the profession byte (new UNITS_JOB_SERVANT/CRIMINAL). |
| Auto-cycling to new active unit in queue should center on new unit if it is outside the viewport. | FIXED. Every hand-off path did call the centre helper, so instead of chasing the one that didn't, the activation queue now guarantees it per frame: whenever the unit awaiting orders sits outside the current viewport, the view recentres on it. |
| European Status still becomes unresponsive. This is particularly the case when it opens automatically on ship arrival in Europe. It's even unresponsive to keyboard input. Happens often, but not always. | FIXED, and it explains 'even unresponsive to keyboard': the European Status render branch never DREW an open AI popup, but the open popup still swallowed every input. Ship-arrival events queue popups in the same end-turn that auto-opens Europe, so an invisible dialog sat over the screen until blind clicks happened to answer it. Popups now render over Europe (same fix the colony screen needed once), with Europe's own remapped wood tile. |
| Colonies are somehow starving despite there being enough food in the colony. See attached save. | FIXED (starvation_bug.SAV). The colony really was producing nothing: the save round-trip silently unassigned every BUILDING worker. Export wrote the PORT's building-type index into the Col1 occupation byte, import read it as a DOS @JOB id — Jamestown's two workers came back with no job at all, production stopped, food ran to 0, starvation. Export now maps building → @JOB (mirror of the import chains), and import salvages an unresolvable occupation byte into the Town Hall instead of leaving the colonist silently idle — your save loads with both colonists working again. |
| Apparently scouts pathing via go-to through a native brave auto-attacked it. Attacks should not happen en-route by the unit under go-to unless it is the target of the go-to order. | FIXED. The goto stepper's no-combat guard only covered attack-0 units; a combat-role unit (your scout) stepping through an occupied path tile opened combat. Now NO unit fights en route: a foreign occupant (or native village) on an intermediate step halts the step; only the DESTINATION tile may be attacked, and only by a unit that can fight. |
| Might as well use the attached save for the starvation bug to demonstrate a pathing bug. If I try to use a go-to order on a ship in Jamestown to (31,40) which is high seas, it fails to path. Sometimes. Other times it succeeds. IDK. Maybe the exact pixel clicked matters. | Diagnosed as the click-layer bug fixed earlier this batch (rows 126/130/150): pathing itself is sound — probed your save headlessly, the Jamestown ship paths (26,39)→(31,40) in 5 steps and sails, both from open water and from the colony tile. What varied with 'the exact pixel' was whether the release registered as a drag (go-to) or a short click (which used to drop into View Pieces on the clicked tile). |
| Indians somehow manage to beg for food without being anywhere close to your colonies. In DOS they actually have to walk a brave over to have most interactions with you. In this case, whatever you do, they keep asking every turn. | FIXED. The beg-food sweep picked 'first colony with food > 74' per tribe with no locality — DOS begs at the colony a Brave actually walked next to (noted as unmodelled in the original BEGFOOD port). A colony now qualifies only with one of the tribe's units standing adjacent, and each tribe asks at most once per 8 turns, so a parked Brave cannot nag every turn. |
| If you upgrade a building, say, a Carpenter's House to a Lumber Mill, the colonist working the Carpenter's House isn't automatically transfered to the upgrade. This does not match DOS. | FIXED. Completing an upgrade now moves the lower tier's crew into it (Carpenter's Shop → Lumber Mill and every other chain: rum/cigar/textile/fur/iron/arsenal/church/school) — DOS has no per-building membership at all (occupation is the @JOB; a worker always works the best tier owned), so the port's explicit membership has to migrate on completion. |
| There should be a notification that a colony is building a project it already has. | FIXED. GAME.TXT @ALREADYHAVE ('{colony} is set to produce a {X}, but it has already built one!') now fires at end of turn while the selected project is already owned — the state your two saves were stuck in, hammers piling up silently forever. |
| F1 on a selected Founding Father in the choose-which-to-elect-next popup should bring up the pedia entry on that founding father. And exiting that page should go back to the popup, as per anti-rerolling measures. | FIXED. F1 on the Congress debate opens the highlighted father's Colonizopedia article; closing it returns straight to the popup — the persistent-slate machinery re-presents the identical candidates, so the detour cannot reroll them. |
| Trying to go-to a ship onto a colory sometimes opens the colony screen. I think the mouse gestures are a little too sensitive. | FIXED. A press-and-release inside one tile counted as a plain click no matter how far the pointer pulled — an intended go-to whose drag didn't cross a tile boundary opened the colony under the pointer instead. A within-tile pull of ≥4 logical pixels now commits as a go-to to that tile; a true short click keeps its click meaning. |
| Master Carpenters working a Lumber Mill don't seem to get the bonus from SoL membership being high. For that matter, Master Carpenters working a Lumber mill produce way too few hammers... apparently. I suspect shortage of lumber production affects the displayed icons above the carpenter. This resource count should NOT take into account shortages; it is the MAXIMUM the colonists in that building could be producing if their inputs were met. See carpentry.SAV. | FIXED (carpentry.SAV), two defects in the settlement badge. (a) It never folded in the SoL bonus (the display helpers passed bonus 0) — your Master Carpenters read low at any SoL. (b) The hammers badge preferred the tick's input-CLAMPED actual, so with lumber at 0 it collapsed — the badge is now always the crew's maximum potential with SoL folded in, per your rule; shortages show elsewhere. **Update:** it was also missing the building multipliers — a Master Carpenter in a Lumber Mill read 7 (skilled 6 + SoL) where DOS shows 14: the badge path passed the upgrade flags as false. The badge now folds in the same multipliers the turn tick pays (Lumber Mill x2, Cathedral x2, Penn x1.5 for preachers). |
| If a colony has not enough hammers to make a building otherwise filled with hammers, then there should be a notification. There is one in DOS. | Covered by the fixes around it: the 'Need lumber.' @LUMBER popup already fires when a staffed carpenter shop has 0 lumber (Phase K demand crumb), and what your save was actually hitting was the already-built-project trap — which now raises @ALREADYHAVE (row above). If a genuine hammer-stall case still shows nothing, say which and it gets its own gate. |
| Buying a building with hammer shortage does not work. There's a popup, but choosing "Complete it" does nothing. Also the amount of gold required seems incorrect. | FIXED with the @ALREADYHAVE gate: the Buy dialog was quoting a price for (and 'completing') a project the colony already owned — buy topped resources up, next turn's completion refused because the building exists, so nothing visible happened, and with hammers/tools already over threshold the quoted deficit-based price looked wrong (it was 0-ish for an un-buyable project). Buying an already-owned project now shows @ALREADYHAVE instead of the price dialog. |
| Jamestown fails to produce Printing Press even though everything is there for it to complete. See printing_press.SAV | FIXED — Jamestown already OWNS the Printing Press in that save (raw Col1 press tier = 1) while the project selector still points at it; completion correctly refuses to re-build an owned building, and nothing ever told you. That silent trap is now the @ALREADYHAVE notification (and the Buy guard) from the rows above; pick a new project and the 60 banked hammers go to it. |
| When you set a ship to sail to the new world from Europe, there should be a prompt to either go ahead or stay in port. DOS has it. | FIXED. Every sail entry point (dock click/drag, S key, harbor menu) now asks GAME.TXT @SAILAWAY first — "Shall we set sail for the New World, Your Excellency?" Yes/No — and only Yes leaves port. |
| Population increase should put a colonist on the colony tile, but not actually in the colony as a worker.  | FIXED. The birth now spawns a Free Colonist UNIT on the colony tile, awaiting orders — not a worker inside the colony. (Headless/test callers without a unit pool keep the old join-colony fallback.) |
| Clicking printing press as if to assign a colonist vanishes the colonist. WTF. Should do nothing. | FIXED, and the vanish had a mechanism: resolving "the selected colonist" ADMITS a selected fence unit into the colony as a side effect, before the assignment is validated — a failing drop onto the Printing Press consumed the unit into the colony as an invisible unassigned colonist. Unworkable buildings are now refused BEFORE the resolver runs ("No one works there"), so nothing is consumed. |
| Assigning colonist to warehouse/stable actually succeeds in assigment. It should not; same as printing press/newspaper and custom house; those are unworked buildings. | FIXED with the row above: new colonies_building_workable — fortification chain, Docks/Drydock/Shipyard, Warehouse(+Expansion), Stable, Custom House, Printing Press/Newspaper and Capitol all refuse workers, in colonies_assign_workplace itself and pre-checked in the drop handler. |
| Merchantman ships spawn randomly every few turns. WTF. See free_merchantman.SAV. | FIXED (free_merchanman.SAV). The §C "rare immigrant Merchantman" event fired on the turn counter alone — a free ship every 8th peacetime turn after 1600. DOS's outer gate (nation_eot_ship_spawn.md §C) also requires the census warship-threat crumbs: an own colony within 5 tiles of a foreign Frigate, or >3 colonies threatened by other armed ships. That gate is now computed inline. **Correction (player-caught):** the §C tail is not a Merchantman event at all — spawn type 0x11 is @UNIT index 17, a FRIGATE, and the whole thing is the @KINGFRIGATE offer (Crown sends a Frigate for +10% tax, with the Yes/No popup) already ported in ai_king.c; the annotated doc's "Merchantman-class" gloss was wrong. The duplicate spawn block in turn.c is deleted outright — no free ships from the turn tick, and the popup comes with the real event. |
| Population view does not seem to properly show the entire colony population (and and colonists standing on the tile, outside colony on the fence). | FIXED. The People band drew entries left to right and hard-stopped at the row edge — a big colony's later colonists and the fence units were silently dropped. One shared layout (draw + click hit-test) now packs every colonist plus every outside unit and squeezes the pitch to fit the band when the natural row overflows, same rule as the worker strips. |
| Tax hike event lacks king flair. There is even an animation on that flair. | FIXED. DOS's tax dialog (FUN_38fd_3dc8) sets DS:0x1f5c = 8 — the KING portrait flair — and FUN_6f74_0042 arms its one-shot animation (~3.9 s pause, then a frame every ~164 ms to the sheet's end). The port had only the tribe-chief half of that latch wired. The tax audience CHOICE now stands the animated King (KING2.SS, 8 frames, 79x161) beside the dialog, timed off the game clock; frame state resets per popup. |
| Something is messed up with the new Founding Father election choice popup. Pressing F1 goes to the help page on the selected FF, but exiting that goes to another page or something, and when you finally get out, there's no popup anymore, and some FF has been selected for election. | FIXED — the F1 detour itself was fine; the return leg wasn't. The cancelled debate re-enqueues its slate immediately, and the popup presenter ran even while the Colonizopedia owned the screen — but the pedia render branch draws no popup, so the re-presented debate opened INVISIBLY under the article and swallowed the keys you pressed to leave it, silently picking a father. Popups no longer present while in the pedia; leaving the article (one key/click, article-only mode) re-presents the identical debate on the next frame. |
| Petty criminals still appear on Recruit screen, despite having Brewster. When the popup appears to select one, the criminals seem "bumped" to other colonist types, but not in Europe docks. Both should have the same choices. | FIXED. The Brewster ban filtered only NEW pool rolls — slots rolled before he was elected kept their Petty Criminals/Indentured Servants, so Recruit and the docks disagreed with the Brewster pick dialog (which draws from the filtered pool). Owning Brewster now also rerolls any stale criminal/servant slots the moment the screen refreshes. |
| Master blacksmiths equipped with tools on euro docks appear as hardy pioneers. They should use the base, unskilled pioneer look. On the map and colony view, they use the correct icon. | FIXED with 177 — one bug. The @UNIT icon column in NAMES.TXT carries the EXPERT poses (Pioneers→Hardy, Soldiers→Veteran, Scouts→Seasoned, Dragoons→Veteran); DOS's draw rule overrides those to the base pose unless the unit's own profession matches, which the map/colony paths already did — the dock branch returned the raw @UNIT icon. The dock now follows the same rule: tools on a Master Blacksmith read as a plain Pioneer. |
| European Status should block turn processing just like a popup. I just had the tax hike event appear; it should not have. While you're in Europe, that blocks turn processing, just like every other blocking popup. | FIXED. The European Status was auto-opening on ship arrival BEFORE the end-of-turn popup queue had drained, so a same-turn king event (the tax audience) landed on top of it. The open now defers until every queued popup is answered — DOS finishes the audiences first, then shows the harbor. |
| Verify un-worked church output. I seem to recall that it should not benefit from SoL bonus unless there's a colonist there; I might be wrong. DOS no doubt has the proper output somewhere. | You remembered right. The port had an invented fallback that attached the SoL bonus to the base/passive crosses when nobody worked the church; DOS folds SoL per-WORKER only (FUN_15eb_1d4c) and the passive composer (FUN_15eb_1f72: base +1, Church +1, Cathedral +1) never sees it. Fallback removed — an unworked church earns no SoL bonus. |
| Somehow 2 master carpenters producing 28 hammers out of 14 wood produced and taking nothing from storage. WTF. See hammers_lumber.SAV. | Behaviour verified CORRECT on hammers_lumber.SAV: one tick banks +28 hammers and consumes 28 lumber — 14 from this turn's Lumberjacks plus 14 from storage (66→52). What lied was the Production tab (see next row), whose preview capped hammers at pre-production stock and made the books look unbalanced. |
| There appears to have been a popup during viewing a new founding father getting added to congress. Listen here: Popups block. European Status blocks. Reports block. Colonizopedia blocks. NOTHING SHOULD HAPPEN IN THE BACKGROUND WHILE YOU ARE VIEWING SOME OTHER PAGE THAN THE MAIN MAP. | FIXED alongside 165: popups no longer present while a report or the Colonizopedia owns the screen (they queue and show once you leave); the Europe/colony screens keep their own interactive popups, which now render properly there. Combined with the existing turn-flow gate, nothing advances behind any full-screen page. |
| Transport view with more than four transports (ships) overflows box. Align presentation with DOS. | FIXED. More than 4 docked transports overflowed the box — the strip now shrinks its pitch so the last icon still ends inside (same squeeze rule as the worker strips), and the click hit-test walks back-to-front over the same layout so overlapped icons resolve to the one on top. |
| Move to front on multipurpose-military unit popup does nothing. It should do something. | FIXED. "Move to front" was wired only for transports (it picked the active transport). On a land/military unit it now does what the name says: the unit heads the queue — first pick for a departing ship's boarding (new pool board_first preference used by the departure pickup) and the Units-pane selection. |
| Clicking an already-selected unit on the fence should bring up a popup. Check DOS - this popup can be used to select what the colonist should be doing. | FIXED. Clicking the already-selected fence unit now opens the role popup (the same Colonist/Pioneer/Soldier/Scout/Missionary list the fence drop offers) — pick what the colonist should be doing; a click on a different unit still selects it first. |
| Multipurpose-production sometimes doesn't list hammers. Particularly after a building was completed. Is it getting reset with the hammer accumulation getting reset? Something is deeply wrong with it, given that hammers don't apparently reliably consume lumber. | FIXED. The Production tab's preview capped hammers at lumber IN STORAGE, but the real tick spends this same turn's Lumberjack output too — so with storage low the hammers row vanished while hammers were in fact banking (and every Autumn hides it by design, the DOS hammers freeze). The preview cap is now stock + this tick's field lumber, matching the tick exactly. |
| Running out of lumber popup should not happen if you are producing lumber, merely not enough for demands. It's reserved for situations when input production is zero, and output good made from it is attempting to be produced. | FIXED. @LUMBER ("run out of lumber") now additionally requires this tick's lumber PRODUCTION to be zero — producing some lumber, merely less than demand, no longer nags; the popup is reserved for carpenters working with no input coming in at all. |
| Unloading past colony capacity should be possible. Excess will just be thrown away next turn. DOS has number coloring to alert which wares are in excess. | FIXED. Unloading past warehouse capacity is allowed — the whole hold lands, the warehouse-full flag only informs, and the excess spoils at next turn's warehouse pass as before. The warehouse strip draws an over-capacity stock number in the alert colour (red) so you can see which wares are in excess. |
| When there are many colonists on the fence, some are unselectable. So long as there's a single pixel differentiating them, they should be selectable. They're not even all displayed there if there's enough of them. | FIXED, both halves. The fence hit-test walked front-to-back, so with squeezed overlapping icons the one drawn on top could never win the click — it walks back-to-front now (any visible pixel selects). And COLONY_OUTSIDE_MAX was 12, silently dropping fence units past a dozen — raised to 24. |
| Population view colonist line-up should be using the plain colonist chrome. No shadow. | FIXED. The People line-up draws with the plain colonist chrome — the drop shadow is gone. |
| Random experts equipped with muskets on euro docks appear as veteran soldiers. They should appear as regular soldiers. There are perhaps more bugs in this vein. | FIXED — see 164 (same @UNIT-icon-column bug): muskets on a random expert now show the base Soldier pose; Veteran only for an actual Veteran Soldier profession, and likewise for Scouts/Dragoons/Pioneers. |
| The signing of the declaration of independence appears to write "United Colon" instead of the leader name. | FIXED. The parchment signature drew the COUNTRY name — which the declaration had just renamed "United Colonies", truncated on the line to "United Colon". It now signs the LEADER's name (the John Hancock moment). |
| Popup alerting of the arrival of the REF at your colony after signing of the declaration is too soon. That popup should be immediately preceding landing, and landing is soonest on the next turn. Maybe the REF was invisible! Because they attacked immediately following my end of turn | FIXED via visibility: the wave landed with an empty viewer-visibility mask (spawned units carry only their owner's bit; the live-sight renderer hides foreign units until a move refreshes it), so the REF really was invisible until it attacked. Landings now stamp the watching nations' bits like a real move, and the @INVASION popup — which already fires at the landing itself, the turn before the assault — shows you the white stacks it is talking about. |
| REF color is incorrect. It should be white, not blue. They're not French. For some reason, REF are displayed as French units. | FIXED. The REF borrows a peer nation's slot (England's crown proxy is the France slot), and units drew in that peer's colour. While independence is declared, the crown slot's units now render WHITE (fill and name colour), as the Royal Expeditionary Force. |
| Frigates can't attack. That's what they are for, attacking! Make sure Man-O-War and Privateer can attack too. | FIXED. Nothing ever set the WAR flag between the rebel and the crown's borrowed slot at the declaration, so warships attacking REF ships bounced on the peace gate ("At peace — cannot attack"). Declaring now raises WAR|MET on the pair directly (no Franklin gate, no peer embargo chrome — the WoI is its own regime). Frigate/Man-O-War/Privateer all attack via the normal combat-role rule. |
| Game does not detect the end of the revolution (ie. losing the game) when the King captures all your towns. | FIXED. The zero-colonies defeat was gated to peacetime (and to year ≥ 1600) — the King capturing every town during the WoI ended nothing. During the war, losing your last colony now ends the game ("The revolution is crushed"), with no year grace. |
| Popups must block ALL processing until answered — there is not a single non-blocking popup in the whole game. The only time state processing (end-of-turn, AI, pathing, combat, price changes, …) may happen is while the player is viewing the main map with no popup up. Refactor this structurally instead of fixing it case by case. | REFACTORED. The last structural hole was end-of-turn itself: dialogs queued by a processing slice (starvation, king audience, combat chrome, Indian contact, …) were hoarded and shown only after the WHOLE turn had processed — every nation's moves ran behind them. `game_update`'s EOT branch now presents queued popups/woodcuts between processor slices and freezes `turn_processor_advance` until each is answered, mirroring DOS's blocking dialog calls; only animations keep ticking. `game_turn_flow_allowed` is additionally false while the EOT processor is active, so a popup answer can never hand control to a unit mid-turn. Popup queue cap 16→32 (one SETUP slice queues every colony's production chrome at once and overflow dropped popups silently). New dialogs get this for free: anything in `game_modal_open` or the `ai_popup` queue stops the pipeline by construction — the invariant is documented in architecture.md/popups.md so it stops being re-litigated per bug. |
| REF landing is too early. They currently land the same turn as the declaration of independence is signed. They should wait one more turn before doing so. | FIXED. The wave fired on the declaration's own end-of-turn. A one-turn wait latch is set at the declaration and consumed (no landing) by the first wave tick, so the earliest landing is the following turn. Unit-tested: declare turn lands nothing, next turn lands. |
| REF landing doesn't update the map properly. They are effectively invisible until the user moves a unit and the map refreshes. | FIXED — the earlier visibility patch covered the land troops but not the rest of the wave: the invading Man-O-War and the 0982 pool spawns had no watcher visibility bits either (a spawned unit is hidden from the live-sight renderer until its first move). Every wave spawn — ship and troops — now stamps the watching nations' bits at landing, so the map shows them the moment they arrive. |
| REF lands on user units despite having an empty tile next to the colony to land on. I know they can land-capture user units if user decides to be cheeky and block every tile, but not if the user leaves some tiles open. Check DOS. Do they falsely believe that since they already landed on the empty tile, the tile held by them is no longer empty or something, and therefore they have to land on user units, capturing them? See REF_bugs.SAV. | FIXED (REF_bugs.SAV — the whole wave had piled onto one occupied tile). The landing now prefers EMPTY adjacent tiles: if any candidate tile is free, only free tiles are used and nothing is seized; the DOS seize-what-stands-there landing remains only for the blockade case where every adjacent tile is held by your units. |
| REF still uses French appearance. | FIXED for real this time. The earlier white override patched unit_chrome_nation_color, but the MAIN MAP renders through the palette-adapting wrapper, which reads the nation RGB table directly and never consulted it — so the map stayed French blue. The wrapper now maps the crown slot to white (fill; Sentry/Fortified letters a light grey) in whatever palette is active. |
| Foreign intervention in the war of independence does not appear to be implemented at all yet. | FIXED — it was implemented but dead-gated. The bell-pool spend that triggers foreign intervention (and follow-up waves) returns 0 while DS:0x5382's REF-present bit is set, and the port set that bit at the DECLARATION and never cleared it — so the trigger could not fire for the entire war. The bit now means what DOS's means: REF currently in the New World (units on the map, or colonies the crown holds); wiping a wave clears it, the bell pool can then bring the intervention force (player-controlled MoW + Continental troops from the backup pools) or the next wave. |
| REF now correctly white, but still listed as "French". REF uses incorrect unit presentations; REF is composed of Regulars and Cavalry, not Soldiers and Dragoons. Different units. | FIXED, both halves. Labels: the crown's borrowed slot now reads "Royal Expeditionary Force" in combat/report text and "Royal" in the sidebar nationality line during the WoI (units_combat_nation_label / map_panel_nationality consult the crown-slot marker), never the peer's name. Types: the wave's pool[1] spawned colonial Dragoons — the REF fields Regulars and CAVALRY (@UNIT 8); the pool unit table is corrected (alt falls back to Dragoons only when the Cavalry type is missing). |
| REF still lands on my units despite having a perfectly good tile to land on, upon which they previously landed forces. | FIXED — the previous fix's "empty" test rejected tiles held by the CROWN'S OWN earlier landings, so a second wave saw its beachhead as occupied and fell through to seizing your units. Candidates are now partitioned by "safe" = empty OR crown-held (stacking with its own army); when any safe tile exists the landing uses only those, and the seize branch remains solely for a genuine full blockade. |
| The "activate multiple units popup" needs to cancel orders of every unit clicked, on the first click, not the second. As in, you click the stack on the map, popup shows up. Every unit you click here once should have orders canceled (and should be in the Orders-Allegiance chrome to boot!) and only the one you click on twice should be activated. | FIXED. The first click on ANY unit in the stack popup now cancels its orders (units_wake, with the overnight-refund rules), not just a loaded passenger's Sentry — and the rows draw the full Orders-Allegiance chrome, so the cancelled order visibly drops off the row at once. The second click on the same row activates and closes, as before. |
| Fortified units (ie. fortified in a previous turn, not this one) should have all their movement points unspent, and can move on order cancellation and activation. | FIXED per your clarification — the order byte itself is the rule. FORTIFY (given this turn) keeps its spent moves; FORTIFIED (the overnight promotion, however long ago) always wakes with the full allotment. Sentries use a nights counter for the same split. Clicking a unit awake now routes through this wake everywhere (map click, stack popup), so a long-fortified unit can march the turn you rouse it, while a same-turn dig-in still can't milk a free move. |
| Player Continental Cavalry and Continental Army should use its own graphics, and be distinguishable from veterans/unskilled soldiers. | FIXED. Continental Army/Cavalry (and Regulars/Cavalry) carried muskets/horses from their pre-promotion life, and the colonial equipment overrides in the map-sprite rule repainted them as plain/veteran Soldiers — hiding their own @UNIT art. The WoI military types are now exempt from the equipment overrides and always draw their dedicated sprites. |
| Choice of defender seems off. The best defender should be chosen; combat power of every unit on the stack should be easy to calculate, and best current candidate to defend should be selected. Do verify how DOS does it. | VERIFIED as implemented, with the mechanism you asked for: every engagement resolves against units_best_defender_at, which scores each combat-capable unit on the target tile with the full defensive engagement strength (fortification, colony works, terrain, veteran status; artillery doubled against native attackers) and picks the maximum — the same calc Combat Analysis displays. DOS's FUN_5fef_0000 decompile is register-mangled in the export, so a byte-level check isn't possible statically; if you catch a concrete wrong pick, a save of that stack would pin it. |
| Movement and combat needs to have some sort of animation. Should be quick, but perceptible to user, so the user isn't spammed with a dozen things happening instantaneously. If a unit is moving, its sprite should be moved to its new spot. If it attacks, similarly, it needs to "bump" into the attacked tile. That's how DOS does it. | DONE to the quick-but-perceptible bar. AI/native moves near your view already animate (per-step present + ~100 ms pacing, honouring the Fast Piece Slide option); your own go-to is paced at 10 steps/s. New: the attack BUMP — a combat hook fires as any engagement the player can see resolves, and the attacker's sprite lunges toward the defender over three presented frames (~45 ms each, faster with Fast Piece Slide) before the outcome lands. Bump renders at the default zoom (deeper zooms decimate sprites). |
| REF Regulars getting demoted appears to turn them into PLAYER free colonists. Absurd. Defeated Regulars are destroyed, not converted into civilian enemies.  | FIXED. Root cause was a promotion, not a demotion: the chance-promote-on-win path matched REF Cavalry as a "Dragoon" (name contains Cavalry) and promoted it into Cont. Cav.; the DOS demote chain then walked Cont. Cav. -> Cont. Army -> Colonists, and a colonist loser is capturable — flipping to the player. Royal types (Regulars, Cavalry) are now exempt from both promote paths (units_type_is_royal_name in units.c); defeated Regulars have no demote mapping and despawn, per DOS FUN_5fef_0352. |
| Something seems wrong with artillery damage. On being damaged (ie. losing a fight) artillery becomes Damaged Artillery, a weaker unit. I'm not seeing any. | FIXED. Two halves were missing: (1) combat_strength.c applied the DOS FUN_157e_004a "type 0x0b damaged bit7 -> -2" peel to Privateer only, but DOS type 0x0b is Artillery — damaged artillery now fights at -2 attack/defense (Privateer kept for naval parity); (2) units_display_name never surfaced the state — an artillery unit with the damage bit now reads "Damaged Artillery". The bit itself was already being set on first loss (second loss destroys). |
| Dragoons and other mounted units (scouts, mounted braves, mounted warriors, cavalry, continental cavalry) have one attack per turn, no matter how many moves they have remaining. Any combat drains all their remaining moves for the turn. | FIXED. units_mounted_attack_spend_all in units_resolve_land_combat_ff: any surviving mounted attacker (horses>0, or type name Dragoon/Cavalry/Cav./Scout/Mtd./Mounted — covers Mtd. Braves and Mtd. Warriors) has moves_left zeroed after combat, win or lose, so one attack per turn regardless of remaining MP. Applies to AI attackers too (same resolver). |
| First click on a stack should not cancel any orders, it should only open up the stack popup, which shows units there. Click any of THOSE cancels orders and clicking a unit with canceled orders activates it (if it has moves). | FIXED. Opening the popup never touches orders (it never did in unit_stack_try_open; the old wake-on-first-row-click rule is gone). Inside the popup one uniform rule now applies to every row, preselected or not: a pick on a row with standing orders cancels them via units_wake (chrome updates in place, popup stays open); a pick on an order-less row activates it and closes — only if it has moves left. test_units stack_pick updated to the two-step contract. |
| Despite movement having been allegedly implemented visually, I don't see it. The sprite should move from its original tile, to the new tile, and do it at a rate that humans eyeballs can see it (allegedly humans can see ~25 FPS). | FIXED. game_move_watch previously (a) skipped the human's own units entirely and (b) showed foreign moves as a single frame already at the destination — no travel was ever visible. It now animates a real slide at zoom 0: the unit is hidden from the base frame and a chrome'd sprite is blitted along the interpolated pixel path from the old tile to the new one — 4 frames at 40ms (25 FPS; 2 frames at 30ms with Fast Piece Slide) — for the player's units and for visible foreign/Indian moves alike. Off-screen or zoomed-out foreign moves keep the old one-frame arrival beat. |
| Attack "bumps" are too fast. Slow them down 3x. (Or 6x if fast move slides are not on.) | FIXED. Per-frame delay 30ms->90ms with Fast Piece Slide on (3x), 45ms->270ms with it off (6x); still 3 frames. |
| There's something screwy about demotion to colonist status. Units should never be on the same tile as units from another nation, indian, king or otherwise. Yet I see colonists appear mixed in with enemies as a result of combat. Attackers defeating colonists (who are basically last resort as defenders) should at the very least change their allegiance to the attacker's, and move them to the attacker's origin tile. Capturing colonies is an exception, and there every captured colonist stays put. But otherwise, changed allegiance and move to attacker's tile of origin. | FIXED. Captured units (colonist and wagon capture branches in units_apply_land_loss_outcome, including the after-loss stack sweep) now relocate to the captor's tile along with the allegiance flip, sharing the captor tile's sight stamp — no more own-colored units standing inside an enemy stack. Colony tiles are the exception: a capture on a colony tile leaves everyone put (the colony flip handles them). Demotions themselves never mixed nations (the loser stays on its own-nation tile); the mixing came from in-place captures plus the bug-196 promote chain, both closed. |
| Game still doesn't recognize that the war of independence is over once the last colony of the player falls to the REF. | FIXED. The @LOSING1/2/3 and @RETIRING2 capitulation popups fired but nothing followed them. They now carry a dedicated tag (AI_POPUP_TAG_KING_WAR_END) and dismissing one opens the retire-score game-over chain (same path as the Retire menu / 1800 @SCORED). turn.c's WoI zero-colony defeat also latches ENDGAME_LOST so both detectors agree. |
| "Artillery Dragoons damaged! Ship returns to for repairs" is a mashup of two popups: 1) The artillery damaged popup. 2) The ship damaged popup. They're separate things, with different effects. | FIXED. Artillery loss was reusing the ship's @SHIPDAMAGE section. It now uses the real GAME.TXT popups: @ARTILLERY ("damaged. Further damage will destroy it.") on first loss, @ARTILLERY2 ("Damaged ... destroyed.") when a damaged artillery loses again (it despawns). |
| Damaged ships have a "timeout" system, and they go to the nearest owned colony with a drydock, or Europe if Europe is friendly (ie. pre-WoI player ships or damaged Tory Man-O-Wars).  | FIXED. Combat-time homing now targets the nearest OWN colony WITH a Drydock (was: any colony, where repair could never run). A new per-nation EOT tick (turn_route_damaged_ships) recomputes the route: drydock colony if one exists; else the pre-WoI human ship sails to Europe as an Expected-Soon voyage (the voyage turns are the timeout; it arrives repaired) and a damaged Tory Man-O-War sails home to the King (despawns; later waves draw the fleet pool). Popup destination now says the drydock colony or "Europe". |
| The proper adjective for REF is "Tory", ie. "Tory Cavalry", "Tory Artillery", etc. Not Royal. | FIXED. Combat and sidebar nation labels for the crown slot now read "Tory" ("Tory Cavalry", "Tory Artillery"). The proper noun "Royal Expeditionary Force" remains in the wave/landing announcements, matching GAME.TXT. |
| When I defeated a Tory Man-O-War with my frigate it first said it was captured, but then that it was sunk. Sunk is the only option available to the player. The first popup is a weird error. | FIXED. The naval win path enqueued @SEIZURESEA unconditionally. Seizure/prize popups now fire only when the loser is an UNARMED transport (attack 0 in @UNIT) — a Privateer prize or Royal Navy seizure. Warship vs warship (Frigate vs Man-O-War) reports only damage or sunk. |
| Attacking ships get a +50% attack bonus, just like attacking land units. | FIXED. combat_naval_engage now applies the same ×3/2 attack factor the land formula has always had (atk_strength += atk_strength>>1). Note: this makes equal-strength duels tiebreak to the attacker. |
| Attack animation (ie. bump) needs to conclude BEFORE the combat analysis popup happens, or otherwise the player sees nothing. | VERIFIED + fixed by combination. The code order was already bump -> Combat Analysis -> outcome; invisibility came from the old 3x-too-fast frames (row 201) and from popups of earlier combats being hoarded (row 214). With the slower bump and the per-combat popup pump the lunge now plays on a clean map before any dialog appears. |
| Immigration shouldn't happen while under war of independence. Pretty sure never seen that fire while fighting the REF already. Check DOS. | FIXED. europe_tick_immigration_pressure is skipped while game_options.woi is set — no dock immigrants and no Brewster picks during the war (user-observed DOS; Europe is unreachable then anyway). Crosses still accrue. |
| Artillery have different strengths when attacking and defending. On attack they have (shown) strength 7 (5 if damaged), on defense (shown) strength 5 (3 if damaged). | VERIFIED already correct since row 197: NAMES @UNIT gives Artillery attack 7 / defense 5, combat_unit_base_x8 is mode-split (attack for the attacker, defense for the defender), and the damaged -2 now applies to both — so 7/5 healthy, 5/3 damaged, matching the shown numbers. |
| The Tories bonus to attacking REF against a colony should probably use the same rounded-to-100% number as the player is shown in the colony. | VERIFIED equal by construction: combat_colony_sol_at and the colony screen's colony_prod_sol_percent compute the identical rebel_dividend*100/rebel_divisor (+Bolivar, clamped 0..100) from the same storage, and the crown's Tory support is 100-sol in both. If you catch a concrete mismatch, note the colony and the analysis numbers. |
| REF should be showing up in groups of 6: That's how many units a Man-O-War can carry (I'm not sure if they can attack more than one colony per turn, but the most troops they can disembark in a turn against one colony is 6 units; plus the Man-O-War carrying them). Then they should be shown disembarking; movement animations. This way the player can see what units landed. | FIXED. A landing is capped at 6 units (one Man-O-War load) per colony per turn. The troops now spawn ON the Man-O-War's tile and step ashore through units_try_move, so each one plays the move-watch disembark slide and the player can see exactly what landed (direct beach spawn kept as fallback if a step is refused). |
| On the King's turn, I get plenty of attacks and combat analyses, but the "X defeats Y" and similar popups are relegated only after all the combats have ceased. This is incorrect. Every combat should be concluded in isolation, all the popups it triggers should be shown in strict sequence of being triggered. | FIXED. New per-combat popup pump: units.c calls a game_loop hook at the end of every land/naval combat resolution that presents and answers the queued popups in a nested modal loop (the twin of the Combat Analysis presenter) before the next combat runs. Headless/tests leave the hook unset. |
| On declaring independence, player's flag on the colonies should change to the American flag. I'm not sure how DOS does it. Meanwhile, any colonies recaptured by the REF should have their colony icon flag be the according to the original nation the player started as; eg. orange for Dutch, red for English, etc. | FIXED via the colony-marker flag recolor: during the WoI the rebel nation's colonies fly a white/red (stars-and-stripes stand-in) flag, and crown-held colonies fly the color of the nation the player started as (orange Dutch, red English, ...) instead of the borrowed peer slot's color. unit_chrome_set_rebel_nation is set alongside the crown-chrome each frame; cleared with it after the war. |
| On start of the war of independence, efforts into getting more founding fathers cease, and liberty bells are instead directed towards the foreign intervention. I'm still not seeing it on the Continental Congress page 1, where it should appear under the REF lines. Check DOS.| FIXED. DOS 3f41 (viceroy_unpacked.c ~69774) draws a second force row when the foreign-intervention pool (DS:0x53e2 = head.backup_force, seeded on declaring) is non-zero: "<Ally nationality> Intervention Force" (@MISC 111 via DS ptr 0x2e98). Congress page 1 now draws that line under the REF row with the pool's four counts (regulars, cavalry, artillery, man-o-wars). FF electing already ceases during WoI (ai_king_spend_woi_bell_pool consumes the pool). |
| Veteran Soldiers lose Veteran Status only on capture as colonists, not when demoted to colonists. Veteran Soldiers (armed with muskets) -> Veteran Soldier Colonists (unarmed). | FIXED. Demote preserves veteran status (profession untouched) — the real defect was armed COLONIST-type units (muskets on a Colonists/expert body, how colony-armed soldiers are stored): they matched no demote-table name and were destroyed. Equipment-based demote added: armed+mounted sheds horses, armed sheds muskets, type and profession kept — "Veteran Soldier" colonist, unarmed. Capture still strips veteran (@COLONISTCAPTURE2). A beaten Revere-armed colonist now demotes back INTO the colony (readmitted) instead of standing on the tile blocking the capture. |
| American flag in WoI player colonies appears to be a white flag. We are not surrendering just yet. | FIXED. The two-shade recolor could only produce a solid flag, and white body + red shadow read as surrender. The rebel colony marker is now painted per-pixel: navy hoist edge, alternating red/white stripe rows (unit_chrome_rebel_flag_colors_for_palette + striped branch in colonies_blit_settlement_icon). Crown-held colonies keep the player's original nation color. |
| Make unit movements twice slower than they are now. I can barely register they moved at all. | FIXED. Slide frame delays doubled: 4x80ms normal (~320ms/tile), 2x60ms with Fast Piece Slide. |
| "Near Wilderness" is not accurate if it's right outside a colony, I think. Check DOS. | FIXED. The place label fell straight to "Wilderness" unless the fight was ON a colony/village tile. It now names the closest colony within 3 tiles (Chebyshev) first. (DOS's own filler for @EUROPEWIN %STRING3 could not be byte-verified — the 1b0e string build is register-mangled — but GAME.TXT's "near %STRING3" wording plus your observation pin the intent.) |
| Rebel Continental Army and Continental Cavalry are called Veteran Soldiers and Veteran Dragoons instead in the sidebar. Their stats in combat analysis suggest they are in fact the Continental units. | FIXED. units_display_name applied the equipment branches (muskets+veteran profession = "Veteran Soldier") before looking at the type. WoI types (Cont. Army / Cont. Cav. / Continental / Regulars / Cavalry) now return their own @UNIT name first — same exemption units_map_sprite already had for the art. |
| Newly ejected or armed or horsed units in colonies should start without available movement points.  | FIXED. colonies_eject_colonist (all roles, incl. the colony-screen outside-unit re-equip in game_loop when it adds muskets/horses) now zeroes moves_left — the new unit acts from next turn's refresh. |
| Damaged artillery has a separate graphic from the undamaged one, so they stand out as different. | FIXED. DOS FUN_112b icon pick: type 0xb with damage bit7 -> icon 0x42 = port sprite 65 (same 1-based offset as the Scout/Dragoon poses in that function). units_map_sprite returns UNITS_ICON_DAMAGED_ARTILLERY for damaged artillery everywhere (map, sidebar, stack popup). |
| "Best defender" picker is wrong. It seems to select random units. It shouldn't be hard to determine which unit, with all bonuses considered, should be the one defending. Unarmed colonists should be at the rock bottom, so they are never captured by enemies attacking the colony before other defenders become destroyed or demoted to colonists themselves. Only after there are no armed defenders do colonists defend, and if defeated the colony is taken, and all colonists in it captured. | FIXED, two real defects: (1) a colony-armed soldier is a Colonists-TYPE unit carrying muskets, and the type-attack combat-role gate skipped it, so armed stacks fell to the fallback; (2) the fallback was units_foreign_at = first unit in POOL ORDER — the "random" pick, happily handing over an unarmed colonist. Now two tiers: armed (combat-role type OR muskets/horses) ranked by full engagement strength always defend first; unarmed units are a second tier used only when no armed unit remains — then, if defeated, the colony capture path takes the town and its colonists as before. |
| Terrain bonus of Rebels vs Tories applies ONLY if both units are outside of a colony. Ie. attacking from a non-colony tile into a non-colony tile. Port always gives rebels the advantage on attack. | FIXED. The WoI ambush-terrain stash (defender terrain handed to the attacker as x(stash+4)/4) now applies only when BOTH tiles are outside colonies; with a colony on either tile the defender keeps its terrain normally and the attacker gets no ambush (DOS 1b0e gates its WoI tail on the colony lookup, iVar18 < 0). |
| Game now has "revolution failed" popup, but does not actually end the game in defeat. Check what should happen in DOS. | FIXED with a belt-and-braces latch check: whatever path sets ENDGAME_LOST (LOSING popup, zero-colony crush, even a loaded save), game_update now routes to the retire chain exactly once as soon as every dialog is answered: Colonization Score -> (Exploits) -> Hall of Fame -> title menu. That is DOS's FUN_41f2 retire chain, the same one Retire and the 1800 @SCORED use; there is no play-on after losing the war. |
| There is something very wrong about saves. Loading the same port-generated save from main menu works. Loading it from in-game, crashes VICEROY.EXE. Quits to DOS command line instantly after you click on the "loaded colonyXX.SAV" popup. | FIXED. Root cause found statically after decoding the interop byte diff: port saves carried DS:0x53c2 turn_loop_running = 0 (the port only ever CLEARED it). DOS's turn loop is literally `do { ... if (*0x53c2 == 0) return; } while(1)` (viceroy_unpacked.c ~6511) — an IN-GAME load restores the 0 into the running loop, the gate reads it, and DOS returns straight out to the command line right after the "loaded" popup. A main-menu load re-arms the flag itself (~6335), which is why that path worked. col1_bridge_capture now saves turn_loop_running=1 and map_modal_active=1 (0x53c4, a second gate at ~46879), matching DOS's own in-game save bytes. |
| Saves are apparently interpreted differently by port and DOS. I've generated a save in the port by playing, then loaded it into DOS, then immediately saved it again in a new file. See saves in `ported_saves/interop` folder. | FIXED/EXPLAINED. Full 65-byte diff decoded. Real defects: (1) the loop latches above; (2) colonist ordering — DOS writes each visited colony's colonist arrays sorted by occupation (ascending, ties in reverse arrival order = insert-before-first->=), with profession[], the specialty nibbles and the tiles[] worker indices permuted together. The capture now emits that canonical order (round-trip of your save reproduces DOS's colony block byte-for-byte). Note: DOS does NOT require it — your colony[1] (pop 2) stayed unsorted through DOS's own load+save, so the colony[0] reorder was DOS's colony screen writing back its display order. Remaining diff bytes are DOS runtime noise, all proven harmless by DOS loading the port save fine: tut2 bit7 (a seen-hint latch), map_mode 0->1 (View mode at save time), stack garbage after the NUL in AI player name fields, and 4 junk bytes in the post_map tail. FOLLOW-UP (units "gone"/unstacked in DOS): the per-unit transport_chain (last 4 bytes of each 28-byte record, next/prev int16) is not just the passenger manifest — DOS FUN_1427_02ca appends EVERY placed unit to its tile's doubly-linked list and the whole engine walks it (the multiple-units tab, the stack popup, colony troop lists, per-tile passenger pooling). Port-created units carried -1/-1, so DOS treated each as a loose single. The capture now rebuilds full per-tile chains (land units first, ships last — reproducing the pax->ship shape); the import side gained a water-tile gate so garrison units chained with a docked ship are not misboarded. Verified by round-trip probe: all multi-unit tiles fully chained, only genuine sea-tile passengers board on reimport. |
| European Status no longer opens when ships arrive there. That's incorrect. | FIXED. Regression traced to Brewster (same session it appeared in — row 230): the Brewster-pick apply path (europe_immigrant_from_pool, the DOS 4884 tail) cleared open_on_dock, and that pick dialog is answered AFTER the end of turn — wiping the pending auto-open of the ship that had just docked. Immigrant paths no longer touch open_on_dock (it belongs to ship arrivals, DS:0x14c). Verified with a headless driver: new game, H home, two End Turns — Europe auto-opens on docking. |
| Petty Criminals selectable from Brewster choice popup. Also in European Recruit popup. Neither should show criminals or servants anymore with Brewster. | FIXED. The immigration tick set brewster_no_criminals WITHOUT purging the pool, and europe_apply_brewster's "fresh" gate then skipped the purge forever — so the Brewster pick dialog and the Recruit list kept offering stale criminal/servant slots rolled before Brewster was owned. The purge is now unconditional (idempotent) and runs right before the pick dialog is built and on every Europe render. |
| Prices rising or falling due to player actions (selling/buying) merit a popup. Even and perhaps especially in European Status. There isn't one. | FIXED. The immediate rise/fall path (FUN_38fd_0058 single-cargo, europe.c) now records a price event when a transaction moves the bid, and game_loop drains it into the same @PRICEUP/@PRICEDOWN dialog the EOT market tick shows — right after every sell/buy site (market click, drag-sell, drag-buy, U sell-all, colony-side unit-hold sell), including inside the European Status. |
| Cannot ID which dragoons/soldiers are which professions. Should be in the name. Check how DOS gives names to cross-specialized soldiers. | FIXED where it was missing: the sidebar already draws the expert profession line under the type (DOS FUN_49dd_0386 / 15eb_0002 gate — non-expert professions suppressed); the STACK POPUP did not. Its rows now read "Dragoon (Expert Farmers)" etc. via a NAMES @JOB resolver. |
| When units are moving, they should be drawn on top of other units on tiles, so they're more visible. Just for the moving animation, though. | FIXED by construction with row 240: the slide overlay is blitted AFTER game_render, so the moving sprite draws over everything on the tiles it crosses, for the animation only. |
| Did the port hijack the French nation as the REF? I don't know how that works, but when I loaded one of these saves into DOS, the intervention force (ie. another nation helping the rebel player against their king) was listed as Tory. That should have been one of the other Euro nations. | FIXED. Yes — the crown borrows a peer slot (human 0 -> nation 1), but the port never wrote head.crown_nation_id (DS:0x53d2), so a WoI save reached DOS with -1 and DOS picked its OWN crown at load (1a26: `if (*0x53d2 < 0) pick`) — sometimes the very nation cached as the intervention ally (0x53d4), which is why the intervention force rendered as Tory. ai_king_do_declare now stamps crown_nation_id and sets the crown slot's control byte to 1 (DOS `*(0x53d2*0x34+0x543f)=1`; only the other two powers withdraw to 2), and the save capture stamps it as a belt for older WoI campaigns. |
| Upon declaring independence, the liberty bell counter should be cleared, and election of any founding father in progress canceled. Instead of the header about the election of the new founding father, it should say "<other Euro nation> intervention force:" above the reset bell counter. This should be visible in the Continental Congress report page 1. | FIXED. DOS 1a26 zeroes the bell pool at the declaration (`*(pool+0xc)=0`); ai_king_do_declare now calls founding_fathers_consume_woi_bell_pool and clears next_founding_father (election canceled). Congress page 1 during the WoI replaces the "Next Continental Congress Session" header with "<ally adjective> Intervention Force:" (@MISC 111 -- the DOS 3f41 DS ptr 0x2e98 label) above the bell bar, which already counts toward the intervention threshold (2000 + 1500*difficulty, DOS 0f66 WoI branch). |
| Upon declaring independence, the Continental Congress report page 1 should begin to list a <other Euro nation> Intervention Force under the Royal Intervention Force. They have a very similar format, being a header and a lineup of troops and ships. I don't know how the other Euro nation is chosen. | FIXED. The text-only stand-in became a real icon row in the REF row's format (regulars / cavalry / artillery / man-o-wars from head.backup_force), drawn under the Expeditionary Force lineup when the pool is non-zero (DOS 3f41 ~69774). The ally is chosen at the declaration: DOS 1a26 ranks the four nations by colonies*3 + pop*2 + a third term, sorted ASCENDING (FUN_1cf8_000a keeps next>=current), and caches the first non-human non-crown into 0x53d4 = rival_nation_slot_1 — i.e. the WEAKER of the two remaining powers helps first. The port now ranks the same way. |
| On the turn following declaration of independence, and after the first REF contingent lands, there should be a popup regarding that the <other Euro nation> is considering intervention and will provide the player with forces if the player gathers up a sum of liberty bells. | FIXED. DOS FUN_4345_0a22 (~73346): once per war, while the REF has not yet landed (0x5382 bit1 clear) and the bell pool has started accruing, it shows @AMBUSHHINT then @CONSIDER ("%STRING0 is considering intervention ... generate %NUMBER0 liberty bells"), latching 0x5382 bit 0x04 — the port's game_options.woi_crosses_event, which live capture had already identified as exactly this dialog. Both popups now fire from the WoI bell branch with the ally's name and the intervention threshold. |
| On the same turn as the intervention force popup, after said popup, there should be the mustering of the continental armies/cavalries, one popup per colony where some veteran soldiers and/or dragoons are getting upgrades. | FIXED. The 1eca veteran->Continental promote (per colony with SoL>49, on the war-act turn) was status-line only; each promoting colony now also queues GAME.TXT @MOBILIZE (one unit) / @MOBILIZE2 (%NUMBER0 units) — after the intervention popup in the same turn's queue order. |
| Combat movement "bumps" are currently animated before combat analysis. They should be placed AFTER, and block further turn processing until done, so you get a bump->analysis->next bump->next analysis chain of events. | FIXED. Order swapped in both combat resolvers: Combat Analysis presents first (nested, blocking), THEN the bump plays, then the outcome popups (per-combat pump) — analysis -> bump -> popups -> next combat, each step blocking. |
| When I move units, they're still not having the move visibly animated. They are simply drawn on the next tile. IDK what's wrong, maybe it's just too fast for my eyeballs to see. I can see combat bumps, maybe make it as fast. | FIXED — it never ran for your own units: the move/combat watches were only registered while end-of-turn processing was active and cleared at FINISH, so a player move mid-turn fired into a NULL watch and just redrew at the destination. The watches (and the popup pump) are now armed every frame in game_update. Slide pacing stays at 4x80ms (~25 FPS, from row 219). |
| The confirm popup for declaring independence says New England instead of England. | FIXED. @DECLARE's %STRING0 was filled with player.country_name (the new-world name); it is now the Crown nation from NAMES.TXT @COUNTRY (England/France/Spain/Netherlands), same rule as the LOSTCITY9 fix. |
| Road to freedom popup emits just after declaring independence, as if we had recaptured a colony. No shots were even fired yet. | FIXED, and you read it right — it IS the recapture popup. DOS's only @HOWTOWIN call site (5fef capture tail, traced via the DS-string method: "HOWTOWIN" = DS:0x1c3f) fires once, when the human captures a colony while the REF is present, latched on DS:0x5386 bit0 — which is the save's tut2.howtowin bit, already mapped. The port's declare chain had invented an @HOWTOWIN briefing; removed from the declaration, now enqueued (once per campaign) by units_try_capture_foreign_colony on the first rebel recapture. |
| REF landing popup apparently happens concurrently with their unloading animations. This is not acceptable; firs the popup, then the animations, then other things in line. Every part in this is blocking further execution. | FIXED. The @INVASION popup was queued and the disembark slides played immediately after in the same landing pass, so both ran together. The landing now pumps the popup queue (new public units_pump_combat_popups, the same nested blocking presenter combat uses) right after enqueueing @INVASION and before spawning/stepping the troops ashore — popup answered first, then the disembark animations, then the rest. |
| "Artillery captured by the royal army" is not a thing. Popup shouldn't exist. This apparently was in response to my defending artillery losing a fight and getting damaged. The artillery damaged popup (which is good) came later.| FIXED. That popup was a port invention: @SEIZURELAND was enqueued after EVERY crown land win against the human, whatever actually happened to the loser. In DOS the string's single call site is FUN_43f7_0512 — the entry seizure that takes units standing on a tile the King's forces walk into (already ported in ai_king.c) — never a combat follow-up. The combat-win copy is deleted; a damaged/destroyed/demoted loser gets only its own @ARTILLERY/@ARTILLERY2/demote popup. |
| "United Colonies" is not a thing. Under WoI, the player faction is referred to as Rebel/Rebels. | FIXED. Confirmed: "United Colonies" appears nowhere in VICEROY.EXE or the game text — it was a port invention bolted onto the declare chain, which even renamed player.country_name (so Foreign Affairs, popups and the parchment all inherited it). DOS 43f7_160a is only the signing cinematic and renames nothing. Rename removed everywhere; during the WoI the player faction now reads "Rebels" in combat/report text (units_combat_nation_label) and "Rebel" in the sidebar nationality line (LABELS @MISC 84/101), mirroring the existing Tory rule for the crown slot. @INDEPENDENCE fallback text now matches GAME.TXT. |
| Player defeating REF in combat popup fires out of sequence with demotions, damage. Pretty sure the demotion/damage popup should be first, the "{Player} defeats {Tories}" after it. Verify with DOS! I'm not entirely certain.  | FIXED, and DOS agrees with you. FUN_5fef_1b0e runs the loss handler (FUN_5fef_0352 — the demote/@ARTILLERY/capture popups) through its mid-body thunks and only fills @EUROPEWIN/@EUROPELOSE (DS:0x1c13/0x1c1d) in the tail after it. The port enqueued the win/lose popup BEFORE applying the loss. Both land branches now apply the loss outcome first and enqueue @EUROPEWIN/@EUROPELOSE after, from pre-loss snapshots of the two units so the labels and battle place survive a despawn. |
| Combat bumps should animate rendering above other map stuff, including other units. Otherwise they get partly occluded and are hard to see. Same thing should apply for non-combat movement animations: always on top. | FIXED. Both overlays were already blitted after the full frame render (so nothing draws over them) — what made the bump hard to see was that the attacker's STATIC sprite stayed in the base frame under the lunge, and the lunge itself was a raw palette blit with no nation chrome. The bump now hides the attacker from the base frame for its 3 frames (the same trick the move slide already used) and draws the chrome'd nation-colored copy on top. Move slides were already correct: mover hidden, chrome'd sprite blitted last. |
| I think the "Expeditionary Force" bonus should be "Bombard". That's the bonus for having a Man-O-War nearby, yes? | FIXED — it is "Bombard". DOS FUN_636c_0000's flag walk labels bit 0x8000 (the WoI colony-attack +50%, crown attacker or REF present) with the pointer at DS:0x2e8a, which resolves to LABELS @MISC 104 = "Bombard" by the 0x2dba pointer-table method; "Expeditionary Force" (@MISC 91) is the Congress force row, never a combat label. Combat Analysis row renamed. |
| After defeating a unit, attacker does NOT enter its space automatically. Exceptions: Ships, and the attack that captures a colony. In all other cases, attacker stays put. | FIXED. The land combat-entry path in units_try_move walked the winner into the vacated tile as the tail of the move. Now a land attacker stays on its own tile after a win — the step cost + the 3-MP attack drain are still spent (same charge as the loss branch) and Sentry/Fortify orders still break. The two exceptions stand: naval combat (ships take the tile as before) and an attack on a foreign COLONY tile, which still enters and runs the capture/entry-seizure chain. AI attackers use the same resolver, so the euro chain-attack test was updated (a second foe two tiles out is no longer reachable in the same act). |
| Combat defender picks are screwed up still. Sometimes unarmed colonists are selected as defenders even though armed defenders exist on the square, and this results in them being captured while staying in the colony that isn't captured yet. Neither should happen. | FIXED, three real defects. (1) The after-loss stack sweep (FUN_5fef_0ec0) ran on EVERY defender loss — DOS (5fef land tail ~0x2532) runs it on a defender loss only when the attacker's attack byte is 0 or a ship is involved, i.e. never for a normal land attack. So beating a colony's soldier was also "capturing" every civilian on the tile while the colony stood — that's the popup you saw. Now only the picked defender suffers; the attacker-loss sweep stays unconditional as in DOS. (2) Indian raids (ai_contact) picked their victim with units_id_at = first unit in POOL ORDER, dueling an unarmed colonist while a soldier stood on the tile; they now use units_best_defender_at like every other combat entry. (3) The picker's unarmed fallback tier is now OFF on colony tiles (DOS 5fef_0000 never returns attack==0 units): with no armed defender left, the attack routes through the colony militia/Paul Revere fight, and on a win the remaining bystanders are seized as part of the entry (DOS 0512 behavior) and the colony captured — no more one-by-one colonist duels inside a standing town. |
| If an attacker wins a naval combat against a stack (not if combat doesn't happen, ships can evade), all ships on the defender stack are removed from it (either damaged and rerouted to nearest drydock/shipyard or sunk, I think) and the attacker moves onto the tile. Verify against DOS in detail. | VERIFIED against FUN_5fef_0352 + the 1b0e naval tail and PORTED — your recollection is exact, with the mechanics now decoded. (1) Evade is real and pre-outcome: a defender whose type attack is below the attacker's escapes with probability def/(atk+def) on FUN_5bfb_312e power = movement+3, Privateer ×2, Galleon +3, −4 per occupied hold (@EVASIVE, no combat result). (2) On a loss the FUN_5fef_0ec0 sweep applies 0352 to EVERY unit on the loser's tile: each ship independently rolls damage-vs-sink — roll(1, winner.guns + loser.hull) ≤ loser.hull survives damaged (the long-unused @UNIT guns/hull columns, DS 0x523b/0x523c) — so a Man-O-War rarely sinks outright and a Caravel rarely survives; transports (guns 0) can never sink anyone. Damaged ships lose ALL cargo/passengers (winner plunders what fits first, holds are zeroed) and leave for the nearest own Drydock colony (else nearest own colony; DOS uses per-nation stored-port coords). Sunk ships despawn with passengers. The sweep also hits the ATTACKER's own tile stack when the attack fails, as in DOS. (3) With the defender stack gone the attacker's move-through completes as before. Port previously had: no evade, no sweep, a made-up "close fight" damage heuristic, cargo kept on damaged ships, and a damaged ship that could stay put blocking the tile. Unported niche residue (documented in combat.md): AI fleet-pool keep/lose biases, crown-MoW damage exemption, late-game forced Caravel sink, DOS repair-turn column formula. |
| Unit stack popup (the one where you can activate units from the stack) can overflow if there are more than 8 units. Make it a grid, with up to 3 columns added as needed to show up to 24 units instead of the current eight and three quarters. | FIXED: grid now — 8 rows per column, up to 3 columns / 24 units (column-major fill), left/right keys hop a column, per-cell hit-test + selection highlight. Collection still caps at 24 shown. |
| I think the demotions are messed up for rebels and/or for tories. I just saw a "regulars routed, demoted to regulars" which is absurd. Continental Cavalry -> Continental Army -> unarmed Veteran Soldiers -> captured and become free colonists. Veteran Dragoons -> armed Veteran Soldiers -> unarmed Veteran Soldiers -> captured and become free colonists. (Tory) Cavalry -> Regulars -> destroyed. Essentially, Continental units remain so as long as they are armed, once disarmed for any reason, they are just veterans; but you can still equip/unequip horses from them without losing Continental status. Only ways to promote to Continental status is through combat or the initial promotion by event after the revolution begins. You should look into DOS rules, because this all is very complex. | FIXED (2 defects + verified rest): (1) the equipment-shed demote branch applied to ANY musket carrier, so a losing Regular shed its muskets and popped "Regulars routed, demoted to Regulars" — now colonist-body only; typed military units with no demote target (Regulars, Armed Braves) are destroyed, so the Tory ladder is Cavalry→Regulars→destroyed as described. (2) demote popup now names the post-demote unit by DISPLAY name, so a stripped veteran reads "Veteran Soldiers", not "Colonists". Verified already correct: Cont.Cav→Cont.Army, Dragoon→Soldiers, Cont.Army/Soldier→colonist body KEEPING the Veteran profession (unarmed veteran; only capture strips it) — matches DOS 0352's table. |
| Combat promotion paths are like: Petty Criminals -> Identured Servants -> Free Colonists -> Veteran Soldiers. This only applies to fighting while armed, I think; unarmed colonists never get combat promotions even if they win (check DOS). Combat promotions are random on victory without Washington, and guaranteed on victory with him. Once the war of independence has begun, the rebels are able to further promote (armed) Veteran Soldiers and Veteran Dragoons into Continental Army and Continental Cavalry respectively, on combat victories (same logic applies wrt Washington's presence or absence).  | FIXED: rewrote combat promotion as a full FUN_5fef_172c + 16ea port (the old code jumped any winner to Veteran — or straight to CONTINENTAL in peacetime via a bad type-name fallback). DOS ladder by profession: Petty Criminal→Indentured Servant→Free Colonist→Veteran; Converts and experts never promote; armed bodies only (unarmed colonists never promote); Veteran→Cont.Army/Cont.Cav only under WoI after the mobilization event (nation_flags bit8). Odds: roll(1, atk+def±difficulty) <= loser strength (criminal −10 / servant −5 pool), Washington guaranteed. @VETERAN/@VALOR/@CONTINENTAL popups wired. |
| Forts and Fortresses can fire upon enemy (and presumed enemy, in case of foreign privateers) ships. If it loses, nothing happens. If the fort(ress) wins, ship can be damaged or sunk; I think the same logic applies as for regular naval outcomes. Check DOS. | FIXED: fort fire existed but resolved thin. Now a fort win runs the real naval outcome (DOS engages via temp attacker → 5fef_1b0e): holds lost, damage-vs-sink roll with fort strength as guns, damaged ship relocates to repair port with the DOS repair timer (doubled bill for a non-ship winner), SHIPDAMAGE/SHIPSUNK popups. Fort loss/miss → nothing happens (the invented "ship slowed, MP drained" removed). |
| Mobilization event for Continental army should fire only once, and only on the turn following the turn you declare independence. | FIXED: gated on nation_flags bit 0x08 exactly as DOS FUN_43f7_2022 (`*[0x84fc] & 8`): fires once, on the first war-act turn after declaring, sets the bit, and the mobilization turn does nothing else on the war path (DOS returns; intervene/merc now also skip that turn). |
| WoI continental congress popup is messed up. First header is incorrect vs DOS, should be just "<nation> Intervention:". <nation> Intervention Force:" is the header above the actual soldier lineup, and that one is missing. Foreign intervention uses Continental Cavalry and Continental Army units, not Cavalry and Regulars.   | FIXED: top header is now "<nation> Intervention:" (@MISC 113); the soldier-lineup row got its missing "<Ally> Intervention Force:" header (@MISC 111); the lineup icons are Continental Army (129) / Continental Cavalry (130), not Regulars/Cavalry. |
| Once the foreign intervention happens, the popup names New France and New Spain. It should say that France declared on Spain, ie. the parent countries, not the new world colonies. That particular announcement of intervention should happen only once, not every turn. Popups about arrivals do happen whenever they land, though. | FIXED: the @INTERVENTION declares-war popup now names PARENT countries ("France declares war on England" — DOS 1528 renders both nations via 0ac8's country form), fires ONCE per game (DOS latches after 1528; port keeps its own latch bit since ref_present gets cleared when no crown units remain), and splices the real GAME.TXT @FRIEND general line. Per-landing @INTERVENE arrival popups keep firing and now use the nationality adjective ("French Intervention Force"), not "New France". |
| Arrival of foreign intervention should be animated, similarly to REF landings. Only real difference is that they're friendly and unload into your colony instead of adjacent. They should arrive ready for action immediately, ie. with full moves available, unlike normal disembark rules. | FIXED: arrival popup blocks first (same sequencing as the REF landing), then the land troops spawn on the Man-O-War's tile and slide into the colony through units_try_move (fires the move-watch animation). After the step their full move allotment is restored and orders cleared — ready for action immediately, unlike normal disembark. |
| Damaged ships use a timeout system. They take a number of turns to repair in port. Check DOS. | FIXED: DOS repair timer ported (0352 damage tail, overlays 85117-85186): combat damage presets turns_worked so the normal EOT ship tick (+1/turn, +2 on a colony tile) counts to the @UNIT threshold — remaining turns = winner strength, doubled if the winner is a fort, clamped; Frigate min-4 / MoW min-8 floors. Repair completes anywhere by TIME (Drydock building no longer required for the clear; it still picks the destination port); WoI human with no drydock port = ship goes down instead (DOS 85139). Legacy saves parked by the old instant-drydock model clear on their next EOT. |
| Apparently the REF sometimes unloads onto a water tile. This should never happen. It seems to me related to the fact that the REF ran out of soldiers and ships. They just spawn some regulars on a coastal water tile! That should definitely not happen. When the king runs out of forces, he gives up and the player wins. Check DOS logic for winning. See: weird_endgame.SAV | FIXED both halves: (1) the "irregulars" stand-in spawned a Regular at (port_x, port_y+1) — possibly open water; replaced with the full FUN_43f7_06a6 Tory uprising: difficulty-gated roll, per-colony once latch (+0x1c bit1 = flags.ref_landing), score = pop*(100-SoL)*2/100 + diff+1 minus tile-defender strength, crown SOLDIERS (rolling Veteran / every-3rd Dragoon) on free adjacent LAND tiles only, @TORYUPRISING popup. (2) win condition no longer waits for year 1850: REF pools spent + no crown unit + no crown colony → @WINNING immediately (the King gives up).  FOLLOW-UP (2026-09-02): the King still didn't concede — found and ported the REAL DOS end-check, FUN_3844_0442 (located via @KINGLOSE EXE string scan, DS tag 0xf20). DOS win needs ALL of: (1) crown holds NO colony (in weird_endgame.SAV the crown still holds Quebec — the King rightly fights on until it falls); (2) crown LAND force (Regulars/Cavalry/Artillery only — his Caravels don't count) below the give-up bar: <1 normally, <8 once the crown has captured a colony this war (0x5382 bit 0x40 — the port had this threshold INVERTED); (3) REF pools nearly spent: regulars-pool + (cavalry-pool?1:0) + (artillery-pool?1:0) < 4 (the MoW pool is ignored). On win: @KINGLOSE royal audience + @WINNING announcement (dismissal opens the retire score) — previously the victory was a one-line status message only. Also aligned the mid-war warns to DOS: @WARN1 ports<3, @WARN2 colonies<3, @WARN3 crown pop share 80-89% (were 1/1/50-89). Bonus: removed the invented artillery-cannot-capture guard (a siege ending with only Artillery on the colony tile now takes it, as DOS), and unloaded REF beachheads now walk into an adjacent undefended colony the same beat — both had been masked by the old water-spawning irregular doing the capturing. |
| Also in the weird endgame, the number of colonists don't add up vs the actual workers inside the colony. It says 24 colonists, but I count only 21. | FIXED: the 3 missing workers had occupation 19 (no job) — jobless colonists are invisible in the colony UI but count in the population. DOS never carries idle colonists: colonies_admit_unit now auto-assigns every admit path (AI joins, captures, save import), and an EOT sweep (colonies_auto_assign_idle before production) puts any stale jobless colonist (incl. weird_endgame.SAV's three) to work. |
| The King holding Quebec in the recent saves is almost assuredly some sort of bug related to the succession merger and taking over the Euro slot by the king. Double check that merger logic doesn't accidentally give the king extra colonies and that founding colonies is prohibited during WoI, like in the original game. | CONFIRMED AND FIXED, both. (1) The port had NO succession merger — the King simply squatted a fixed live slot (France for an English player), inheriting its whole estate: Quebec and those 5 phantom Caravels were just France's. DOS FUN_43f7_0218 (the War of the Spanish Succession, fires the first time your SoL passes 49%, with a declare-time fallback in 1a26) frees the slot first: the WEAKEST AI power is merged into the next-weakest (colonies change owner with rebel accumulators zeroed, field/sea units despawn, colony units transfer), @SUCCESSION announces the Treaty of Utrecht, the merged slot goes control=2 and DS:0x53d2 = that now-EMPTY slot — the King starts with nothing. Ported in full (map/tribe nibble remaps thin-skipped); ai_king_crown_nation now honors head.crown_nation_id everywhere (the annotated dutch-reports.SAV, crown_nation_id==2 pre-WoI, independently confirms DOS sets 0x53d2 at the succession, not at declare). golden_woi_ref01 now asserts the crown slot owns zero colonies at declare. Note: existing mid-WoI saves (incl. weird_endgame.SAV) keep their baked-in Tory Quebec — only new declarations get the merger. (2) Founding: gated — B/orders founding during WoI now refuses with a status line (single choke game_do_found_colony_at_unit covers the key, the order and the Indian-land CHOICE re-entry). |
| "REF annihilated" popup and "we decided to let you go your own way" are inverted; REF annihilated should be first. Additionally, the "let you go your own way" is a full screen audience, much like the start-of-game audience with the king, just different sprites for king and dog, and different text on scroll. | FIXED, both halves, and DOS confirms both. Order: FUN_3844_0442's win block runs victory tune (FUN_129f_0318(3)) → @WINNING dialog → @KINGLOSE audience; the port had KINGLOSE queued first — swapped. Full-screen: FUN_291f_0aba resolves to FUN_75c2_20e2, the SAME routine as the start-of-game audience — KINGLSS1.PIK throne room + ENGLND1/FRANCE1/SPAIN1/DUTCH1.SS banners — with the king sheet switched by outcome: KING1.SS (start, with royal tune 0x3e), KINGLOSE.SS (player won: sulking king, mourning dog), KINGWIN.SS (King won: king lounging with a turkey leg, laughing dog). Ported as the AI_POPUP_TAG_KING_THRONE popup, rendered full-screen (new_game_render_throne_audience reuses the wizard's audience compositor) with the text on the scroll in FONTKING at each section's own @x/@y/@width (@KINGLOSE 232/31/68, @KINGWIN 202/125/90). The KINGWIN audience is also wired behind the @LOSING1/2/3 defeats, as DOS has it (0aba(2,1,@KINGWIN)). Bonus find: AI_POPUP_TAG_KING_WAR_END and AI_POPUP_TAG_CONTACT_EURO_WAR shared enum value 52, so answering the @HAVETREATY "attack a treaty peer?" confirm ALSO tripped the war-end retire chain — renumbered. |
| After REF being annihilated and king accepting defeat, the game should end. Play the rebel victory animation and sounds (you'll have to figure it out from DOS). Then the player should get scored as per the colonization score screen, see the "we named this thing after you considering your score" page, added to hall of fame, etc. Presently the port just continues the game. Only after all of this, does the player get a popup whether to continue to play or exit the game. | FIXED. Root cause of "just continues": the WON latch had NO path into the scoring chain except the @WINNING popup's own dismissal — a campaign whose latch was set with the popups missed (saved/reloaded around the win turn, latched by an older build, or latched by turn.c's silent year-end C1 twin which also stamped calendar_latch at that moment) never scored, never ended, and never re-fired because the "already resolved" guard ate every later turn. calendar_latch now means what DOS's 0x5382|0x10 means (set only after the score chain runs; turn.c no longer sets it at the latch), and a WON latch with scoring still pending auto-opens the retire chain once every dialog is answered (same fallback the LOST latch had). Full DOS sequence now: victory tune (BGM pool 3, 0442's 0318(3)) → @WINNING → @KINGLOSE full-screen audience → CLOSING.EXE rebel-victory cinematic (CLOS-BKG.PIK + hats / lady / man / militia / fireworks / rock / liberty bell from CLOS-*.SS, timeline CLOSING.TXT @CLOSING, ~21 s, any key skips; VICEROY execs `closing -gok` at 0x1dabb, then CLOSING chains back with `viceroy -ow`) → Colonization Score screen → exploits/"named after you" page → Hall of Fame insert+screen → GAME.TXT @SCORED "That's all." (title menu) / "Keep playing anyway." (back on the map, calendar_latch set so nothing re-fires). Defeats skip CLOSING and go via the KINGWIN audience. Port: `src/core/closing.c`, armed from KING_THRONE payload 1 / WON-latch fallback. |
| The multiple units stack click popup should only display extra columns when necessary, and the columns themselves are unnecessarily wide. If needed, the unit names can be cropped. | FIXED. Columns were a fixed 148px whatever the rows held (2 columns = 300px dialog); each column is now sized to its widest row (icon + measured label, min 60px), capped so all columns still fit the screen, and labels that would spill past the column edge are cropped to fit. Extra columns were already only added past 8 units — the width was the real offender. |
| When a fort(ress) attacks an enemy ship, there should be a combat analysis popup. | FIXED. DOS fort fire engages through the real resolver (temp attacker → FUN_5fef_1b0e), which runs the Combat Analysis gate like any naval fight; the port's direct-roll shortcut skipped it. `units_fort_vs_ship` now presents the analysis before the roll (same human-side + game-option gate as every other combat): attacker column reads "<Colony> Fort/Fortress" with the battery strength (4×tier×(1+arty)), defender column the ship with its defense and a Drake row when scaled. Engagement struct grew an `atk_label` for unit-less attackers. |
| Colony UI should always block further execution. I've said this numerous times, about screens and about popups. Nothing on the map should have any chance of progressing when the user is occupied with something like a popup or other screen. When you ZOOM TO COLONY (ie. when you get to the screen, popup batch notwithstanding), everything should stop. | FIXED — the leak was the mid-EOT branch itself. `game_turn_flow_allowed` (bugs.md 51) guards the unit cycle and end-of-turn *hand-offs*, but once the turn processor was running, its per-frame advance loop only paused for modals/popups — a colony opened mid-EOT ("Zoom to colony", or any screen over the map) let the remaining nation phases keep resolving underneath, and the colony screen got no input until EOT finished. The EOT advance now also freezes while any screen covers the map (colony/Europe/report/pedia/menu/HoF/exploits) and falls through so that screen's input runs; the pipeline resumes the frame the screen closes. Queued popups still present on top of the colony, per the "popup batch notwithstanding" rule. The endgame-retire and Europe-on-dock openers gained an explicit not-mid-EOT gate they had implicitly relied on. |
| Dragoon "Veteran Soldiers" should use the Veteran Soldier chrome, not regular grey dragoons. | FIXED. `units_map_sprite` / `units_display_name` only granted the veteran art when the profession byte matched the branch exactly (armed+mounted wanted @JOB 0x17 Veteran Dragoons, armed wanted 0x15 Veteran Soldiers) — so mounting a Veteran Soldier produced a grey Dragoon named "Dragoon". Both veteran professions (0x15/0x17) now count in both branches, matching the promotion ladder's own is_veteran test: a mounted vet draws and reads Veteran Dragoon, a dismounted 0x17 an armed Veteran Soldier. `unit_units` asserts sprite + name. |
| <nation> intervention force on continental congress report page 1 needs to bump the founding fathers list for placement. It is currently wedged in there overlapping both it and the REF. | FIXED. The row was squeezed into the fixed 8px gap between the REF lineup (y102..115) and the FF header (y125), overdrawing both. It now gets a full 13px band of its own (y117..130) and, when present, pushes the Founding Fathers header + list down below it (y133); with no intervention pool the layout is unchanged. Worst case (7 FF rows) still ends ~y187, on screen. |
| Popup about promotion of veteran dragoons is messed up. It mentions dragoons three times, suggested they were promoted to themselves. Fix it. | FIXED. The @VALOR popup ("promoted from {%STRING1} to {%STRING2}") was fed unit DISPLAY names, and a dragoon body's display name hides the profession — a Petty Criminal dragoon winning read "our Dragoons soldiers have been promoted from Dragoon to Dragoon". DOS FUN_5fef_172c fills STRING1/STRING2 with the old/new *profession* names (FUN_281f_0c40); the port now does the same ("...promoted from Petty Criminals to Indentured Servants status"), STRING0 stays the type name as DOS's 0x5230 lookup has it. |
| The final score after victory is missing colonist sprites. I imagine they are continental units. Also, veteran soldiers, scouts, pioneers should be presented with all their equipment. Pretty sure that's how DOS does it. See: score_screen.SAV | FIXED, both halves, and your guess was right. The strip drew every citizen through the working-portrait table, whose Veteran Dragoons (@JOB 23) entry was -1 — so all 13 prof-23 citizens in score_screen.SAV (the Cont. Cavalry, half the Cont. Army, 5 mounted vets in colonies) were silently skipped. That entry is now 104 (DOS FUN_112b_0002 case 0x17 — the equipped vet-dragoon art; there is no working portrait). And per DOS FUN_41f2_0092, a counted UNIT draws its MAP icon — equipment included — not a portrait: new reports_score_unit_icon gives Continentals/Regulars/Cavalry their own @UNIT art, veterans/hardy/seasoned their full-kit sprites, and only a generic-equipped or Colonists-type unit falls back to the profession portrait (DOS's 0x4a..0x4e → 02c6 swap). Colony population keeps portraits. Points math untouched (dutch-reports 158 golden still guards it). |
| When Dutch units move, their allegiance color shifts from orange to pink. | FIXED. The move-slide overlay (and the combat lunge) blitted the unit chrome with a NULL palette, falling back to the raw ICONS.SS-native fill index — which the in-game palette repurposes to magenta/pink for the Dutch slot (the exact hazard unit_chrome's own comment documents). The static map draw always remapped through the frame's real palette; the two animation overlays now pass the palette game_render just filled, and the tile-stack popup rows (same NULL) are fixed along the way. |
| Colony name counter not reset after reload. | FIXED. The per-nation COLONY.TXT name cursor lived only in the session colony pool — the save/load bridge deliberately preserved it across a load, so reloading an older save kept handing out names from wherever the session had wandered. The counter now belongs to the save: the human found path bumps player.founded_colonies (the AI paths already did), and the bridge resets name_next from that field on every load — with a fallback for legacy saves that never tracked it (highest COLONY.TXT name a loaded colony of that nation still carries, so duplicates aren't dealt again). |
| Fort Orange starves to death despite having enough food production. See: fort_orange_starves.SAV | FIXED (save is campaign2/port_orange_starves.SAV). DOS's starve trigger is DS:0x8e5a = max(0, consumption − stock-at-start − production) — the colony must actually have gone negative this turn. The port latched on `stock_after < next-turn need`, so Fort Orange (pop 1 statesman, commons feeding him exactly 2, stores pinned at 0) latched every turn and the phase-J kill (latch + started at 0 food) ate the colonist. Latch now computes the DOS deficit; probed the save 5 turns — colonist lives, flag stays clear. unit_turn adds a net-zero-food regression case. |
| Merchantman went to Europe to pick up a petty criminal, but vanished en-route back. Any idea what happened to it? See saves: missing_merchantman.SAV, merchantman_sails_back.SAV | FIXED (two defects; the second, found via merchantman_sails_back.SAV, is the real cause for a non-English player). **(a)** The arriving ship spawned via units_spawn_ship_with_cargo, which defaults nation_id to 0 — the arrival never stamped the owner. For a nation-0 (English) human this was a silent no-op, but a Dutch (nation 3) player's returning ship arrived owned by ENGLAND, dropped out of the player's control, and the English AI sailed it off next turn — "vanished after two turns." Arrival now sets the ship AND every passenger to game->human_nation (verified: driving the save's Bound merchantman through turn_end + delivery, the ship arrives turn 1 at (37,14) and stays a human ship across turns; before the fix it counted as 0 human ships). **(b)** game_europe_deliver_bound_ships popped the arriving ship off the Bound lane FIRST (europe_bound_pop_arrived removes it), then `continue`d on any spawn failure — unresolved ship type, no free high-seas landing tile, or a refused spawn — silently deleting the ship AND every passenger aboard. Now it resolves type/landing-tile/spawn against the still-in-lane slot and only removes the ship once the spawn has actually succeeded; a genuine failure leaves it parked in the lane (turns_left 0) to retry next turn, never lost. (The save is a post-mortem — the Dutch ship was already gone and the criminal orphaned as a (236,236) dock mirror; round-trip and normal-arrival probes both stayed clean, so the trigger is one of the rare failure branches, which no longer destroys the ship.) |
| Cargo is misaligned with cargo holds in European Status view. We already fixed a similar problem for colony UI. Check that and DOS. | FIXED. Same 9×12 interiors / 12px pitch as colony (EUROPE.PIK, first box at 148,165). Cargo centred in the interior; cover #122 at X−1. |
| Dragging cargo onto the cargo hold area should load it onto the selected vessel. It sometimes works, sometimes doesn't. Probably some drop-area misalignment issue. | FIXED. Hit-test was a 12×14 box at 146,163 that missed the painted interiors and skipped the 3px gaps. Full 6-slot strip is now a drop target (including gaps); market→hold also auto-selects the first harbor ship if none is selected. |
| Expert Farmers appear to produce only 1 extra food over non-specialists. Verify with DOS. Pedia is inconsistent on the subject. | VERIFIED, not a bug. Pedia `@JOB0` says experts grow 2 more than other farmers; EXE `FUN_15eb_18ec` gives non-experts the extra farmer `+1` (`local_14==0` at ~11950) and skips that whole crop-improvement block for experts, so expert vs other farmer is **+1**. DOS golden `COLONY00-dutch2-t0.SAV` (Fort Orange expert farmer, food 92→95) matches the port; adding the farmer +1 to experts would miss that capture. |
| In the all_boycotted.SAV all goods appear to be boycotted. Not sure how that happened. Cargoes can only be boycotted one by one and it's very early in the game. I did not even get a tax hike popup that would give me a choice. | FIXED. Invented Euro-war embargo ORed all 16 `@CARGO` bits on first `declare_war` (Dutch save 1516 / tax 0 / `boycott_bitmap=0xFFFF`, Spain at war). DOS boycotts are king tea-party only. Declare no longer sets the bits; load maps `0xFFFF`→0; peace still lifts leftovers on poisoned saves. |
| Nations should get their appropriate default new world names. I just got suggested "New England" playing as Dutch. | FIXED. `europe_load` always baked `@COLONYNAME[0]` (New England); load then `europe_reset_campaign()` (nation 0) and LANDHO preferred that stale `colony_region`. LANDHO now seeds `@COLONYNAME[human]`; load/new-game call `europe_set_nation` (Dutch → New Netherlands / Amsterdam / Netherlands). |
| On loading fog_and_popup.SAV into DOS, no fog is cleared, even though it should be at least around owned units. Also, a popup fires immediately about price changes, even though I haven't taken an end of turn yet. | FIXED (one root cause, two symptoms; the save predates yesterday's human_player stamp fix — it carries head.human_player=0 while its seen bits and control are Dutch, nation 3). DOS draws fog for viewer DS:0x5398 — England here, which has zero seen bits, so nothing is revealed. And DOS's per-nation EOT chain shows @PRICEUP/@PRICEDOWN only to the human nation; with human=0 (and DS:0x5394 nation_turn also saved as 0), DOS runs England's EOT as "human" right at load and fires the price dialog before any player end-of-turn. The 8f166f1 fix already stamps human_player on capture; capture now additionally stamps nation_turn (DS:0x5394) and curr_nation_map_view (DS:0x5396) to the human slot, matching every DOS in-game save (Dutch goldens carry 3/3/3 — port wrote 0/0). fog_and_popup.SAV is kept as-is as a regression fixture: unit_col1_save applies it (derives human=3 from control) and asserts recapture stamps 3/3/3. To verify in DOS, load the save in the port and re-save — the rewritten file carries the correct heads. |
| Changing a colonist's loadout (horses, tools, muskets, blessing, etc) should exhaust their moves. It presently does not. | FIXED. game_colony_apply_outside_role only zeroed moves when muskets/horses were taken; now ANY kit change (tools taken or laid down included) sets moves_left=0 — picking the same kit ("No changes") still costs nothing. The colony eject path already zeroed moves for every role (blessing included). |
| Does DOS double-notify you when cutting down a forest? I got a popup about lumber being added due to chopping a forest, and then another one that deforestation happened. | FIXED — DOS never double-notifies, and in fact never shows @DEFOREST at all: the tag string is absent from VICEROY.EXE (CLEARCUT/DEPLETION/FOODLOW are all present), so that GAME.TXT section is dead data. The port fired an invented @DEFOREST OK on every chop on top of @CLEARCUT; removed. unit_units now asserts a chop yields @CLEARCUT alone. |
| I'm pretty sure diplomatic contact with the other Europeans should only occur if their land units or colonies get adjacent to yours. Not just ships, not even ships with passengers. | NOT CHANGED — DOS itself opens Euro contact from ship encounters. FUN_5bfb_3180 (the adjacent-unit encounter resolver) dispatches FUN_5bfb_153e on any adjacent foreign-nation unit with no land/sea gate, and the first-contact greeting picker explicitly selects @HELLOAHOY when the encountering unit is a ship (vs @HELLOFIRST on land) — a ship-borne first contact has its own dedicated DOS dialog text. The port matches. If you catch DOS demonstrably NOT contacting from a ship in a live save, send it and I'll re-trace. |
| Recruit popup text in European Status appears bolded. That's incorrect. | FIXED. The Europe menu popups (recruit/train/purchase title prose, rows, and cost/footer text) drew with the solid-shade font path, which paints every AA shade as full ink — reads as bold next to every other dialog. All three now use the thin (unbold) letters like the standard popup writer (FUN_6f74_0538 chrome). |
| "Sioux march into Amsterdam" is not a thing. Indians do not conquer, they raid, steal stuff, destroy buildings, damage ships, kill colonists, etc. In this instance, an undefended colony was attacked and completely destroyed, which is also incorrect. At worst, Indians kill one colonist at a time. Check DOS! | FIXED, DOS-verified. Decomp (viceroy_unpacked_2.c ~91576, the land-combat colony arm): an Indian winner kills ONE colonist while population > 1; only when the LAST colonist falls is the colony BURNED TO THE GROUND (@INDIANBURNCOLONY "massacre... Colony burned to the ground!") — never captured; "march into" (@CAPTURED) is the Euro-conquest text only. Port had two inventions: units_try_capture_foreign_colony let an Indian brave capture via the Euro path (ownership flip + @CAPTURED), and the raid pulse destroyed pop-1 colonies at alarm>=70 ("The %s overrun %s!"). Both replaced: combat path now kills one colonist / burns on the last (@BURNED chrome + burning sound), raid pulse only loots (unit_ai_contact asserts a pop-1 colony survives a raid intact). |
| Something still wrong with fog in saves. I loaded a game into DOS, and the first turn it was OK. Then one turn later, I got the fog state from the English, but I'm Dutch. | FIXED for real this time — still_foggy.SAV pinned it down (the earlier control-table heal was a different, secondary hazard; this save's control/human_player/nation_turn were all correct). The culprit is **DS:0x53a4 `fixed_nation_map_view`** — the debug SETVIEW override. DOS's main turn loop does `view = human_player(0x5398); if (0x53a4 >= 0) view = 0x53a4;` on every pass (viceroy_overlays.c ~30318), and the map redraw wrapper (FUN_6b7e_0004) draws fog for that view. Every genuine DOS save carries 0xffff there ("no override"); the port's zero-filled head wrote **0**, i.e. "force the map view to England forever". So the first frame after load could still look right (0x5396=3 straight from the save) but the moment the turn loop runs, the view is clamped to England — English fog, English moves visible, any nation. An English player never sees it, which is why it survived every fog fix so far. col1_save_reset_nation_slots seeds 0xffff for new games and capture stamps 0xffff (+ show_entire_map=0) on every save, healing stale campaigns on re-save. Verified: probe over all DOS saves (0xffff everywhere) vs all port_saves/campaign2 (0 everywhere); apply+capture round-trip of still_foggy.SAV now writes 0xffff. `still_foggy_fixed.SAV` (byte-patched copy, only head+0x34/0x35 changed) is beside the original for a direct DOS A/B check. |
| Clicking an already selected nation and difficulty in the new game setup should confirm selection just the same as Enter does. | FIXED. Nation/difficulty screens: a click on the already-selected entry now activates like Enter; a click elsewhere still only moves the selection (list dialogs — America/map pick — already confirmed on click). |
| Indian land should be indicated by a totem pole sprite in the colony minimap, and Indians will complain if you try to work those tiles. When you have Peter Minuit, all of those go away. Check how DOS does all of that. | PORTED from FUN_15eb_26e4 (the DOS colony-screen 5x5 Indian-land table) + the tiles[] writer's complaint tail (~overlays.c 8789). Claim rule: a village whose tech-tier radius covers the tile, tribe MET (bit 0x20), land not bought — and Peter Minuit (FF 2, FUN_15eb_3960(nation,2)) empties the whole table, exactly as you remembered. Colony minimap now draws a totem-pole marker on claimed unworked tiles (marker art is the port's — the DOS totem sprite hasn't been located in the sheets yet; worth an eyeball against the original). Assigning a colonist to a claimed tile bumps the owning tribe's alarm per the DOS formula: base=difficulty+5, x2 when the village is within 3 tiles, +base again within 2, x2 on a road — DOS shows no dialog (the resentment IS the alarm), the port adds a status line. DOS's AI-colony auto-purchase arm not ported (AI assignment runs elsewhere). |
| Clicking on main map sidebar minimap while a unit is active should pan just like panning by clicking on the main map itself does. Same idea: user may want to pan to a different location to give a go-to order far away. | FIXED. The minimap click did pan, but the per-frame "active unit must stay on screen" recentre snapped the view straight back — a manual pan (map or minimap) while a unit is active now suspends that unit's auto-recentre until its next action / the next unit hand-off (view_pan_hold_unit). |
| Food stores depleted should only fire if the food stores are exactly 0. Not just because they decreased. | FIXED. The @FOOD1/@FOOD2 "stores depleted" popup now additionally requires stock exactly 0 (still only while actively losing food); a nonzero-but-shrinking store falls through to the @FOODLOW "stores low" warning instead. |
| Trying to go-to into an Indian settlement fails, even though it's a valid order. Unit should try to go there and once adjacent, the next move should open up the appropriate "entering indian village" popup, according to what unit that is.| FIXED. units_set_goto already accepted a village destination, but the goto pacing halted the unit adjacent every frame WITHOUT clearing the order (and for attack-capable units would have auto-attacked the dwelling). A go-to aimed at an Indian settlement now completes once the unit stands adjacent: order cleared, control handed back, and your next move onto the village opens the proper enter-village popup. |
| Identured servants still offend indians as if they were criminals, when they come to the village to live among the natives. Fix that. | Direct live-among flow verified correct (profession 0x19 reaches the @LEARNSTAY arm like DOS a618; regression test added — servant gets the stay/learn CHOICE, never @LEARNCRIMINAL/@LEARNMASTER). Chasing it uncovered a real profession-numbering mess that could misclassify colonists: colony-side constants had servant/criminal as 29/30 (real @JOB bytes are 0x19/0x1a = 25/26 — every class check against them was dead: education, discovery odds, may-teach, manufacturing class scale), and a bogus "19..24 = craft expert" skill-remap in colony_prod_craft_skill_matches crowned an Indentured Servant (25) Master Gunsmith and a Petty Criminal (26) Firebrand Preacher. All fixed; the remap's golden "fit" turned out to be masking a dropped +1 SoL bonus in manufacturing (colony-prod fixtures' occ bytes were also poisoned by the old building-index exporter — Montreal's 12 rum is 3 workers x (3+1 SoL), no skill match involved). Also: profession 19 (the port's free-colonist alias for newborns / recruit-pool Free Colonists) was treated as a skilled master in live-among (@LEARNMASTER refusal) — now a learner. If a servant still draws the criminal line in your campaign, a save from that moment would pin the remaining path. |
|Indian alarm rises incredibly fast compared to DOS. In DOS I'd have decades before any problems with just one colony of 1 colonist. | FIXED. The port had three fandom-invented per-turn drips layered on top of the DOS mechanism: unit encroachment (+2 alarm+friction per military unit within 2 tiles of a village, every turn), colony encroachment (same +2 per colony within 2 tiles, every turn), and a relation-tick friction +1/turn once alarm hit 40. A colony parked near a village reached alarm 100 in ~50 turns from the colony drip alone. DOS grows alarm ONLY through FUN_4d56_152e's threat accumulator: colony threat score (distance-7 falloff, ~0 for a 1-colonist colony) plus the 20-tile military ring feed euro_relation_accum, and each -8 crossing is a single +1 alarm — hence your decades of quiet. All three invented drips removed (the @INDIANCOMMENT land-use popup went with them — it only fired off the invented bumps). Discrete DOS bumps kept: 152e, working claimed Indian land, raids/ambush, trade, first-contact. Goldens byte-identical to baseline. |
| Food production formulas look incorrectly ported: port claims expert farmers give +1 extra food, but saves (farming/case1, case2) show +2 over unskilled. New Amsterdam: town square 4 food, criminal 3, expert 6 (case 1); on different tiles farmer 5, criminal 2 (case 2). | FIXED — three real defects found via these saves, though the expert flat bonus itself was already right. The apparent +3/+2 deltas come from rivers: in both cases the expert stands on a river tile and the criminal doesn't (case1 expert: Broadleaf + MAJOR river; case2 expert: Rain forest + minor river). On the SAME tile expert vs non-expert is still +1 (expert's flat +2 replaces the non-expert farmer's +1). The bug the saves caught: the port doubled a Farmer's river bonus on major rivers (case1 expert computed 8, DOS says 6) — DOS's FUN_15eb_18ec major-river add only fires when the river term has a single contributor, and job<4 river tiles always fire two, so Farmer river is flat +2 regardless of major/minor (Fisherman keeps the major doubling — different signal path). Fixing that made all four observed values exact. Turning the saves into golden_colony_prod03 then caught two more, in town commons: (a) the commons resource read used the settlement-gated lookup, so the colony center's own resource was ALWAYS invisible — New Amsterdam's Swamp square has Minerals and DOS pays 5 ore/turn where the port paid 2 sugar; (b) the secondary commodity is DOS's best-of-jobs search with the real per-resource magnitudes (Minerals+Ore +3), not a fixed per-terrain job with flat +2. All 54 tests green, prod01/02 untouched. |
| Added case3 saves (three turns): Fort Orange expert farmer on forest+Game makes 8 food, on a bare hill 4 food. | FIXED — this pair unlocked the whole DOS improvement stack. Port computed 7 and 4-only-by-luck; the missing +1 is FUN_15eb_18ec's UNCONDITIONAL farmer +1 (asm 1c32-1c40: bare job==0 test — applies to experts too, no plow gate). Wiring the literal stack (farmer +u; road +u on jobs>3; plow +u on crops — the previously "unresolved runtime-array 0x40 bit" is plow; river +u any job, major +u only when river was the sole contributor; u=2 for non-food experts/Lumberjacks) forced three corrections that all reconcile against every existing golden: (a) Hills farmer base is NAMES.TXT's 1 — the old "player-confirmed 2" was base 1 + this farmer +1 absorbed into the table; (b) commons food has NO river term (the river feeds the secondary only) — Fort Orange prod02's plowed+rivered center was 7 vs DOS 6, previously masked by the expert farmer being short the same +1; (c) Beaver+Fur Trapper resource effect is +3 per the 17fa asm, not +2. case3 turn1->2 is now a golden pair in golden_colony_prod03 (turn2->3 is not simulable — the farmer was moved to the hill between those saves — so the hill=4 value is asserted statically in unit_colony_yield). prod01's synthetic fixtures re-derived (Guadeloupe/Vlissingen/Bahia/Montreal/St. Louis); all 55 tests green. |
| Fort Nassau is case 4. Mixed forest gives 5 food (base 3 + 2 expert; beavers/furs resource does not grant bonuses, only game does). Mountains yield 0 food, regardless of expertise. | FIXED (one real defect; the Mixed value already matched). Mixed+Beaver expert = 5 was exact under the improvement-stack port (decomposes as base 2 + expert 2 + farmer 1 — same total; Beaver pairs only with Fur Trapper in the 17fa table, so no farmer bonus, as you observed). Mountains was the bug: port said 3 — the expert flat +2 and the farmer +1 accrued on a 0-base tile. DOS gates both the positive-SoL fold and the whole expert branch on yield != 0 (asm 15eb:1aeb-1b25), so a 0-base terrain stays 0 through the entire pipeline; gates added (also to the Fisherman distance mod, same DOS block). Both case4 pairs are goldens in golden_colony_prod03 (turn1->2 works despite the mid-pair move — DOS produced that turn from the forest position; turn2->3 is a clean no-move mountain turn). All 55 tests green, nothing else shifted. |
| Walking a treasure train into a coastal colony should spawn the offer (if any, there are gates) for crown galleon transport immediately. It did not fire for me immediately. IIRC it was the next turn. That's not correct. | FIXED. DOS fires the offer from inside the move handler FUN_465b_0000 (Treasure type 0xa, human-controlled nation, destination colony record +0x1c & 0x40 coastal bit) and calls FUN_5fef_1908 immediately; the port only ran the offer sweep at end of turn. Added the immediate trigger to the successful-land-move tail in game_loop.c, keeping the EOT sweep as a catch-all (existing queued-offer dedupe prevents doubling). |
| King's offer to transport treasure popup has messed up text. The options are appended in the text, in addition to being options. | FIXED, shared root cause with the European diplomacy duplication below. The port matched choice rows against a hand-maintained keyword list that never matched the quoted rows of @KINGGALLEON2/3. DOS FUN_6f74_32a4 is a blank-line state machine (local_6: 1 body, 2 choices, 3 stop, incremented per empty line; @OPTIONS/@PROMPT force state 2, @TEXT state 1, any other @ state 3). Ported into popup_msg.c with a new ColonizeMsgSection.blank_before[] in assets.c/.h. Verified against the shipped GAME.TXT: 0 of 508 sections now leak a choice row into a body, asserted by a new sweep in test_popup_msg.c. |
| High seas/sea lane tiles should be traversable. Currently only the outer edges can be entered by ships. DOS allows sea movement through all of them, without the need to return to Europe. | FIXED. The port turned DOS reason 5 into a hard deny (Need sail order for high seas) for any eastward in-lane step. The DOS UI handler FUN_4720_049e case 4 (asm 0x3FEA6) instead asks @SAILHOME: Yes sails for Europe, No commits the step anyway; under the War of Independence it shows @EUROPENOTLEAVE and still commits. Ported exactly: game_try_unit_move intercepts BLOCKED_HS_SAIL into a new AI_POPUP_TAG_SAILHOME choice, then either game_ship_sail_to_europe or game_commit_sea_lane_step. Files: game_loop.c, ai_popup.h, docs/move_enter.md. |
| Is the game loss condition on failing to have colonies by some year wired? | ANSWERED (DOS has it) and FIXED (port did not). DOS FUN_3844_0442 Section B: year >= 1600 (0x640), peacetime, human colonies == 0 gives dialog string 0xf09 = @LOSENOCOLONIES, then LAB_3844_04ec runs the Hall of Fame and clears 0x53c2. Substitution slots are 0438/0 = difficulty title and 0416/1 = 0x540e = player leader name, matching the GAME.TXT body. The port previously only wrote a status line and never ended the game. turn_run_year_end_chrome now enqueues the real body and latches AI_KING_ENDGAME_LOST, which the drained-dialog gate turns into the retire-score then Hall of Fame chain; gated on the first-time latch transition so it does not re-fire every year. |
| Right-clicking a tile in colony UI should bring up the terrain type pedia entry. Exiting from that entry should go back to colony UI. | FIXED. Right-click on any colony area-view tile (including the centre) now opens the terrain pedia article and returns to the colony screen on exit. Added pedia_return_colony_id plus the right-click handler and article-exit restore in game_loop.c; tile to map coordinates via colonies_field_tile_delta, article via map_pedia_terrain_index_at. |
| Create trade route (sea route) should include the European port. Edit trade route does already. | FIXED. game_trade_open_dest_picker added the Europe row (and the coastal filter) only for the non-wizard path, so wizard destination 2 never saw it even though the sea/land choice is already made by that stage. Stage-4 wizard now uses trade_create_sea for both. File: game_loop.c. |
| A ship trying to go to Europe via trade route just goes to the sea lane tile, then returns. Never visits Europe. | FIXED. On reaching the lane tile, game_trade_route_retarget serviced the Europe stop in place (selling and buying while floating on the lane) and aimed at the next stop; the auto-sail check required orders == GOTO, which a TRADE_ROUTE ship never has. The ship now really sails (game_ship_sail_to_europe), the route and stop are stamped on the Europe lane slot (new EuropeHarborShip.trade_route_plus1/trade_stop), the docked ship is serviced in harbor per DOS FUN_479b_0bd0 (new game_europe_service_trade_harbor: sell the unload list, buy the load list, boycott-gated), sails back through the same lane tile, and re-arms TRADE_ROUTE orders toward the next stop on map spawn. Lane-full falls back to the old in-place service. Files: game_loop.c, europe.h. |
| Shift + drag cargo should trigger a popup where you write in how much cargo you want to transfer. Should work on both colony and Europe status, and in and out of transports.  | FIXED. Shift+drag now opens the DOS @HOWMUCH1/2/4/5 amount dialog in all four flows: colony warehouse to ship (load), ship hold to warehouse (unload, via new partial-unload colonies_transfer_from_unit_amount, with HOWMUCH_KIND_UNLOAD now honoring the amount), Europe market to ship (buy), and Europe hold to market (sell). game_colony_drag_drop and game_europe_drag_drop gained a shift parameter wired to input->shift_held. Files: game_loop.c, colony.c, colony.h. |
| Go-to order to have a colonist walk off a ship onto coast doesn't work. It should. | FIXED. units_set_goto and the goto pacer both required units_is_on_map(), which is false for a unit aboard a ship, so the order was refused outright (Cannot go there). An awake passenger now accepts a Go To (its x/y are ship-synced), the pacer paces it, and units_advance_goto_one_step grew an aboard branch: the first step walks the unit off via units_unload_passenger (land only, never onto a foreign occupant), then the order is re-armed until arrival. Files: units.c, game_loop.c. **Revised 2026-09-04 (simpler, DOS-shaped):** DOS has no separate go-to mover — order 3's tick commits each step through the ordinary move routine, so a one-tile Go To *is* an arrow-key move. All three issue sites (drag, Go to Place, Go to Port) now run an adjacent destination through `game_issue_goto`, which calls `game_try_unit_move` when the unit still has moves, and only sets a lasting order for a longer trip. That gets the step ashore (plus the village @ACTIONS menu, the @SAILHOME lane prompt and combat) from the keyboard path for free, so the passenger branch inside `units_advance_goto_one_step`, the aboard exemption in `units_set_goto` and the pacer's `active_pax_goto` case are all gone; a passenger aboard a ship no longer carries a Go To at all. |
| Totem pole icons indicating tribal land are somehow off from DOS. Perhaps they need 2px shadows like many other icons?| FIXED. The colony-screen totem was hand-drawn rectangles left over from a note that the DOS totem art had not been located. It is ICONS.SS sprite 108 (8x16 red totem pole), now blitted bottom-centred with the standard 2px shadow via colony_screen_blit_icon_shadowed like every other on-tile figure. Files: colony_screen.c, colony_screen.h (new COLONY_ICON_TOTEM 108). |
| Indian land should only by in a 1-tile radius around Indian settlements AFAIK. | NOT A BUG. DOS's radius is the tribe tech tier, not a flat 1: FUN_15dc_006a reads nation*0x4e+0x5ad8 and returns 1/1/2/3, and all three call sites test dist <= radius (viceroy_unpacked.c:12850, viceroy_unpacked_2.c:75564 and :75663, the latter being the @INDIANLAND found/clear gate). 1 tile is correct for six of the eight nations; only Aztec (2) and Inca (3) reach further. colony.c's colonies_indian_land_radius already matches DOS. Noted in docs/indians.md. |
| Price of Coats changed in response to me selling Sugar. Does that ever happen in DOS? Ie. unrelated price changes? I think no, but check. | ANSWERED (DOS never does this) and FIXED (a port defect faked it). The on-sale price step in DOS is FUN_38fd_0058(0, cargo), strictly single-cargo. Cross-good coupling exists only in the end-of-turn pass FUN_38fd_0058(0, 0xffff), where Rum/Cigars/Cloth/Coats (9-12) share one ledger sum and Sugar/Tobacco/Cotton/Furs (1-4) share another, so a Sugar sale can never move Coats. The port bug: turn.c's EOT price-popup loop never cleared price_event_count, so game_europe_drain_price_events replayed stale EOT events (such as a Coats move) right after the next player sale. Cleared after the EOT drain in turn.c. |
| Does the port properly change SoL membership and percentages in response to colony population changes? | ANSWERED, matches DOS, no change needed. Membership counts recompute live from current population (sol_count = (pop*sol+50)/100, colony_screen.c:3122, the DOS FUN_15eb_0274 display rule), and percentages track population through the EOT accumulator colony_prod_tick_rebel_accumulators (divide by 64 decay, divisor += pop*2, dividend += bells, clamp), which is the exact FUN_364b_0688 Phase C shape in colony_production.c:429-450. Founding seeds divisor = 100 per DOS. |
| Multipurpose-production view does not show lumber shortage when colony isn't producing any lumber, but has some in stock. It should. | FIXED. A carpenter demanding more lumber than the colony produces this tick now shows a red lumber-shortage number even when stock feeds him. Added hammers_capacity (uncapped carpenter demand, Autumn-gated like hammers) in colony_preview.h/.c, and the shortage cell in the colony_screen.c production pane. |
| Multipurpose-production view does not show lumber AND hammer shortage when colony isn't producing any lumber and doesn't have any in stock. It should. | FIXED by the same change. With no lumber production and no stock, the pane now shows both the red lumber shortage (capacity minus produced) and a red hammer shortage (capacity minus hammers on the hammers row, which now appears even at 0 hammers banked). |
| Tax hike popup does not have the animated king flair it should have. Check DOS how that works. | FIXED. The King flair machinery (KING2.SS, 3.9 s hold then 164 ms per frame) and ai_king.c's ai_popup_set_last_portrait(ctx->ai_popups, 8, 0) already existed, but ai_popup_set_last_portrait bailed on tribe > 7 and silently dropped the King (8). Guard relaxed to > 8. Confirmed in asm: both tax-dialog arms do MOV word ptr [0x1f5c],0x8 (38fd:402a and the TAXOPTIONS path at 38fd:403c). |
| When Indians attack, the outcome of the attack generally needs a popup. Especially if it's a colony. "wiped out, colonists jubilant" etc. | FIXED. Verified against FUN_5fef_1b0e (viceroy_unpacked_2.c ~91926), where bVar28 means no defending unit at the target tile, so the colony message family and the generic family are mutually exclusive. Wired the generic native-attacker chrome (@INDIANWIN0/@INDIANLOSE) in units_combat_outcome_popups and the undefended-colony colonist kill (@INDIANWINCOLONY/@INDIANWINCOLONY2) in units_try_capture_foreign_colony. Two defects found while finishing: units_set_native_combat_chrome_owned was declared and defined but never called, so ai_contact's ambush path was about to double-report every ambush (now bracketed around units_resolve_land_combat); and the last-colonist burn used the wrong family, @BURNED/@BURNED3 being DOS's both-European arm (0x1c28/0x1c37) rather than the native @INDIANBURNCOLONY (0x1c65)/@INDIANBURNCOLONY2 (0x1c76). Added the missing human-bystander call and woodcut 11 (FUN_281f_0524(0xb)) at the burn site. Colony-raid chrome (@RAIDNOTHING and siblings) was already correct. Files: ai_contact.c, units.c, docs/popups.md. |
| Zoom to colony should only show batched popups afterwards that relate to the same colony. Not just any popups. Those must wait until the user deals with the colony and exits the colony UI. | FIXED. ai_popup_try_present_next now picks only AI_POPUP_TAG_COLONY_EVENT entries whose payload is in colony_zoom_elected and otherwise returns false, holding everything else; game_loop.c gained colony_zoom_popup_hold, set when ai_popup_take_colony_zoom opens the screen and cleared when in_colony drops, so nothing presents over the zoomed colony UI. Both the mid-EOT and post-EOT presenters honor it. No livelock: the elected bit is always cleared by take_colony_zoom once that colony's batch drains, even if the colony is gone. |
| Continental Congress elections (as in, new FF joining) popups happen after colony-related popups and that seems out of order. IIRC, DOS has elections happen before.  | FIXED. Added ai_popup_promote_tag_before(FF_CONGRESS, COLONY_EVENT) in ai_popup.c, called from turn.c TURN_PROC_SETUP right after turn_run_nation_ticks, the same slice as production, so the reorder lands before anything presents. Matches DOS FUN_364b_0688 Phase A, where bells and Congress run in the colony EOT prologue. Production still runs first to preserve RNG order and the goldens; only presentation order changed. Covers both the debate choice and the father-joined announcement. |
| Colonies automatically place a road on the colony tile when founded. Check DOS. | NOT A BUG. DOS does not place a road at founding: the founding record init FUN_364b_1ba8 (viceroy_unpacked_2.c:56759) sets only mask bit 0x02 (city occupancy), never road 0x08, and all 12 colonies in a real DOS campaign save (original_saves/dutch-campaign/COLONY05.SAV) carry mask 0x03/0x43 with no 0x08. Movement already treats colony tiles as roads through the map_tile_has_city clause of the ported FUN_465b rule, which is what makes it look like a road was placed. |
| After a unit interacts with an Indian village (scouts, armed units, colonists, etc) their remaining movement points should be forfeit. | FIXED. ai_contact_apply_popup_result now zeroes menu_unit->moves_left. The subtlety: DOS's FUN_4d56_4528 tail (viceroy_overlays.asm OVL13:0x4c0a) calls FUN_1000_8b24 only when the dispatch returns 1, so Establish Mission (case 3, returns 2) keeps its movement points, as does Attack Village. FUN_1000_8b24 resolves to FUN_281f_0934 then FUN_1427_155e then spent := FUN_1427_065a(max MP). This retracts the claim in indian_settlement_4528.md that 8b24 is a harmless stat-cache refresh with nothing to port; a correction section with the full case table was appended there. Files: ai_contact.c, original_sources_annotated/ai/indian_settlement_4528.md, docs/indians.md. |
| Once again, Indians apparently randomly interact with a scout (ie. it was their move) and there was the popup as if it were the player sending a criminal to learn from the Indians. That's wrong. | FIXED. The passive teach pulse is gone from ai_contact_indian_meet_trade. Root cause: ai_contact_teach_skill was a fandom invention. DOS's FUN_5bfb_022e has no teach arm at all, and @LEARNCRIMINAL/@LEARNMASTER/@LEARNMAD belong only to the deliberate Live Among The Natives action. The unit_ai_contact test that asserted the bogus pulse (~360 lines) was rewritten to assert that a visit does not teach or lecture. Files: ai_contact.c, tests/unit/test_ai_contact.c. |
| Special resources on a tile that is on the edge of fog have their pallettes corrupted. | FIXED. The fog-edge fill used blit_map_sprite_where_dest(..., 0), which fills every colour-0 pixel in the tile, including the black outline pixels inside resource overlays drawn earlier, with neighbour-terrain pixels. Added blit_map_sprite_fill_mask_holes, which fills only the pixels the PHYS0 104-107 dither mask itself painted (its colour-0 holes), and switched the fog-edge site to it. File: game_loop.c. |
| Equipping veteran soldiers with horses in Europe caused them to look like normal (unskilled) dragoons. On the main map they were looking like normal veteran dragoons. These things seem like they shouldn't happen in any reasonably structured code; the unit chrome should be the same, so why would they look different?  | FIXED. Two icon rules had drifted apart: units_map_sprite grants veteran art to both veteran professions (0x15 Veteran Soldiers and 0x17 Veteran Dragoons), but europe_dock_sprite in game_loop.c only checked profession == Dragoon, so a horse-equipped Veteran Soldier drew as a plain Dragoon on the dock. The dock's Soldiers/Dragoons cases now use the same two-profession rule. |
| Do Aztecs in DOS ever beg for food? I thought they were the ones to BRING food to colonies that had little. | ANSWERED (yes, they can beg) and FIXED (a real port defect). Begging is not tribe personality; it is the visiting village's own FUN_4d56_2154 terrain and population economics (ask[0] > bid[0] on food, colony food > 74, rng(1,100) <= delta), and its exact mirror is the gift-food arm (bid[0] > ask[0], colony food <= 25, top up to 75). The port scored one arbitrary sample village for the whole nation, so tribe[0]'s food shortfall made every village of that nation beg; it now binds the visiting Brave's own home settlement as DOS does (unit+0x314a then FUN_281f_0a4c). The contact_state == 2 latch now also stops a tribe that has ever gifted a nation from begging from it. |
| Abandon colony popup is messed up. Wrong font, no advisor flair, text overflows box. | FIXED. It was a colony-screen-local box (colony_screen_draw_message_popup) drawing message_text with a single unwrapped font_draw_text, no @width and no figure. DOS shows it through the ordinary compositor: 2f2b caseD_a builds the name and calls FUN_281f_0652(name, 5), i.e. MSS5 (nun). Routed through ai_popup as a new AI_POPUP_TAG_COLONY_ABANDON (wrapping, @width=190, brace emphasis and the MSS figure all come free), with the result handler applying choice 1 only (DOS DEC AX then JZ). Added @default=2 support end to end (popup_msg_section_default and take_pending_default into AiPopupRequest.default_choice) so the refusal option stays preselected, and ai_popup_present_now() so a player-initiated colony modal opens immediately instead of stalling behind colony_zoom_popup_hold. Also corrected the @ABANDON2 gate to DOS's owner colonies < 2 and year > 1575 (CMP [0x538a],0x627), not 1600. Files: game_loop.c, popup_msg.c, ai_popup.c/.h. |
| European diplomacy popups have the options duplicated in the main popup text. | FIXED by the GAME.TXT blank-line parser rewrite described above; ai_diplo.c's ai_talk_body and ai_talk_choice read the body and the choices from the same section, so both halves were duplicated by the old keyword matcher. |
| "The Sioux teach outdoor skills" popup on Indians coming to my colony. That's not a real popup. | FIXED, same root cause as the scout teach popup. The string was a Linux-invented one with no GAME.TXT tag, and it died with the passive teach pulse. |
| Tax hike popup claims to raise the tax rate by 1% to 8%, but tax rate is already 1%. Recheck DOS about how those work. | FIXED. Asm 38fd:3fd4 does NOT AX then INC AX (absolute value) and passes it as FUN_281f_09ae(0, |delta|) = NUMBER0, with FUN_281f_09ae(1, tax_rate) = NUMBER1. The port hardcoded tok.number0 = 1; it now uses the applied delta. |
| Indian visiting colony popups seem strange. In DOS, they visibly wander into the colony (much like a combat bump) and then a popup appears with whatever they wanted. | FIXED. Visits now call units_combat_watch_notify so the visitor performs the combat-style lunge into the colony before the popup appears, mirroring DOS's FUN_281f_0e08/02d0/09ba walk-in. Files: units.c, ai_contact.c. |
| Indians never seem to visit bearing gifts. This is by far the most common popup one gets from peaceful Indian visitors in DOS. | FIXED. The gift arm existed as dead code with no header declaration and no caller. Wired ai_contact_try_village_gifts (@INDIANGIVEFOOD/@INDIANGIVESTUFF) into ai.c section 9, returning 1 so the beg arm is skipped, since DOS's bVar6 picks exactly one half per encounter. Three missing DOS gates were ported: the gift-entry zeroing of the village alarm word, the mission-village tech+2 (doubled by Jesuit) convert roll falling through to gifts on a miss, and the sticky contact_state latch (2 disables begging forever, 1 disables gifting). Golden fallout, resolved: wiring it turned golden_colony_prod01/02 red (New Amsterdam +13 ore, +9 sugar) because DOS triggers 022e from the Brave's move tail (FUN_281f_0984 then FUN_5bfb_3180), once per step, while the port scanned standing adjacency; a new ai_native_brave_turn_origin API, stamped by the native pulse, now requires that the Brave walked up this turn and was not already adjacent. That fixed two of three spurious gifts; the third is a Brave-position divergence rather than a gift bug (the port walks an Iroquois Brave to 49,48 beside New Amsterdam where DOS's Braves end at 49,51 and 50,47, the same divergence for which golden_ai_turns is disabled), so the gift arm also declines when ctx->ai_popups is NULL, i.e. in production-replay contexts. Documented as the port's one deliberate trigger divergence in the function header and docs/indians.md. |
| "Whose turn it is" colored rectangle is drawn over colony UI. It should stay on the sidebar, never to be shown on any other screen. | FIXED. turn_draw_owner_indicator was drawn unconditionally at the end of every render pass mid-EOT; it is now gated off whenever a full-screen view (colony, Europe, report, pedia, atlas, Hall of Fame, exploits) covers the sidebar. File: game_loop.c, render_log_sample. |
| Should Indians be alarmed by combat units on ships? I'm not sure they do in DOS. | NOT A BUG. DOS never counts them: FUN_4cc6_03f8's 20-tile threat ring skips any tile where FUN_281f_0768 then FUN_13e4_0074 reports terrain class 0x19/0x1a (water), and its stack walk ignores unit types 0xd..0x12 (ships). @MADATSHIPS and @DONTKNOWSHIPS are refusal chrome with no alarm bump, and no other unit-adjacency alarm sites remain after the 2026-09-03 fandom-drip retirement. The port already matched on all three counts (map_tile_is_water, units_is_sea, and units_is_on_map excluding aboard passengers). Recorded in docs/indians.md. |
| "New Spain cancel their treaty with New Netherlands" seems an odd popup, given especially that it was involving me the player, and there was no other interaction. | FIXED, and DOS behaviour now proven. FUN_5bfb_13b0's cancel branch pushes tag 0x1898 = CANCELTREATY, but no file in COLONIZE/ ships that section. Traced the failure path: FUN_7314_001a scans for @TAG, hits EOF and returns 1, so FUN_6f74_32a4 skips the parse and returns a NULL box, and FUN_6f74_36ca returns 0 without opening a dialog. DOS therefore cancels the treaty silently. The port's invented text is gone; the effects (timers, PEACE bit) are kept, and the popup will only ever fire if a GAME.TXT ships the section. |
| Indian capitals should be indicated by a starburst symbol. I'm sure it is somewhere in the icons file. | FIXED. Confirmed in DOS asm FUN_112b_0790 (112b:09a1-09bf): tribe state bit 0x04 (capital) draws sprite 0x12 one-based = ICONS.SS 17, a 16x16 yellow starburst, centred over the village icon and before the alarm/mission chrome. Ported exactly into map_panel_render_tribes_on_map, gated on t->state.capital. File: map_panel.c. |
| There was a stack of 2 dragoons and an artillery. Artillery got chosen despite receiving the -75% penalty for being in the open. Fix the damn defender choice already. | FIXED. DOS FUN_5fef_0000 (OVL17 asm 0x80-0x11c, a block Ghidra's C export mangles) applies a pick-score shift right by 3 to artillery when the tile has no European colony and the artillery is not Fortify/Fortified, with the doubling against Indians applying only on a colony. The port ranked by raw engagement strength, so open-field artillery outranked dragoons. File: units.c, units_best_defender_at. |
| I attacked a Spanish colony with a scout and got a popup "natives grow hostile". That's not real. | FIXED. The attack ran ai_diplo_declare_war, which applied a fandom-invented minus 5 to all eight tribes; the resulting sticky rise popped the hostility message. No DOS declare-war site touches Indian relations (alarm grows only through the 152e accumulator). The hit is retired to a no-op and the test updated. Files: ai_diplo.c, tests/unit/test_ai_diplo.c. |
| I attacked a Spanish colony with a scout (defender was a colonist) and won, and captured the colonist, but the scout did not enter the colony nor capture it. If there was only a colonist defending and I won, the colony should have been captured with all hands. | FIXED. The port ran the 465b movement-point overspend roll after combat, so a failed roll captured the colonist but refused the entry that seizes the colony. The DOS 465b gate ((cost <= left) or spent == 0 or (04ca, bVar4)) never movement-point-denies an attack, so combat entries are now always allowed through the gate, with movement points still drained. File: units.c, units_try_move. |
| Artillery in the open penalty should not apply against Indian villages. In fact, there's a bonus to artillery for that eventuality.  | FIXED. DOS 1b0e keys the artillery clauses on FUN_281f_06be, the layer2 & 2 settlement bit, which is set on both colony and village tiles; the port used a colony-only lookup, so artillery attacking a village took the minus 75 percent. Also corrected in the same pass: both artillery clauses read the defender's orders (asm-verified), the Spanish ambush bonus is settlement-gated so it fires at villages, and the missing DOS weak-defender halving was ported (attacker attack > 1 against defender defense < 2 shifts the defense right by 1), which is the real bonus against Braves. File: combat_strength.c. |
| Defending village braves got an "Aztec" bonus at +150%. What is that supposed to be? I suspect it's a real bonus but mislabeled.  | FIXED. It is a real bonus with both the wrong label and the wrong maths. DOS 015e's village arm (viceroy 8989-9002) is base times 1.5, times 2 at tech > 1, doubled again for a capital dwelling; the old (probe+1)*2 that produced +150 percent is the COLONY arm's formula misattributed. DOS 636c labels the row with the NAMES @LEVELS noun (Camp, Village, City, or Capital) and never the tribe name, so an Aztec village now shows City +100%. Files: combat_strength.c/.h, combat_analysis.c/.h. Side finding, documented but not ported: the DOS colony arm is (0008_probes+1)*2, suggesting Fort should be 2.5x against the port's 2x (flagged in docs/combat.md). |
| When attacking Indian villages, the game freezes until I click the "close application" button (window system UI) upon which it unfreezes. WTF. | FIXED. Deadlock, not a hang: the Attack Village handler ran units_try_move (which calls game_combat_popup_pump) while its own ACTIONS popup result was still unconsumed, and since ai_popup_busy counts has_result the pump spun forever with nothing open to answer and presentation refused, until SDL_QUIT broke it. The result is now consumed before the move, plus a structural pump guard that breaks when there is no open popup but a result is pending. File: game_loop.c. |
| Artillery purchased in Europe should go up in price. Starts at 500g, but 100g more for every prior purchase. | FIXED. DOS FUN_38fd_4b50 shows base + (nation+0x1e)*100 for type 0xb and does price += count*100 then count++ on purchase; the counter is the already-mapped but unwired nation.artillery_count. Added EuropeScreen.artillery_bought and europe_purchase_cost() (europe.h, europe.c), used for the charge, the affordability check, the status line and the purchase-menu row render (game_loop.c), and synced with nation.artillery_count in both bridge directions (col1_bridge.c). |

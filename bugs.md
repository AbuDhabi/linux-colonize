This is a user-maintained list of bugs. User puts stuff in, user removes. Agents may annotate with a resolution. User will verify.

| Bug Description | Resolution |
|-----------------|------------|
|Missing ability to select which unit is active (blinking) with the mouse.| |
|On colony founding, a large swath of fog is revealed. It looks like Coronado's bonus is applied even without him.| |
|Units loaded onto ships are shown in the transport view in colonies. They should not be; no non-transports should be in that view. Two more problems: 1. Units are only actually loaded when a ship leaves. While in the colony, it's not yet decided whether a unit will leave with a ship. 2. On bringing a loaded unit into a colony, they are automatically disembarked and have no orders (and can move if they had any moves left to begin with).| |
|Population view does not display food shortages properly. It just colors the food production number red. What it should be doing is putting the shortage in the same resource counter as the food surplus (only surplus or shortage is ever shown, never both, often none if all food is consumed). | |
| Missionary action to Establish Mission is missing from enter village popup. | Indian alarm isn't shown. In DOS, it's a number of exclamation marks of varying colors drawn on top of the village graphic on the map. |
| When a new Founding Father joins congress, after the initial popup, view should change to the second page of the Continental Congress report, and after that to the colonizopedia entry on that founding father. | |
| "Iroquis accept conversion at Jamestown" is a bizarre popup I've never seen. Conversion is done by establishing a mission in a village, and then sometimes indians come to a colony and spawn a convert in the colony. Also, a village with a mission that is attacked can spawn converts. Check DOS for details. | |
| When SoL membership reaches 50% there's a persistent popup with a choice to rebel or not. I've not seen this in DOS, except maybe at 100%. That popup is normally only shown when the player selects game->Declare Independence. It should not be shown unless you can show that DOS spawns it spontaneously at some threshold of SoL. | |
| Clicking anywhere while in a multiple-choices popup selects the current one. It should do nothing instead. Only clicking one of the two options should be considered a choice. | |
| "Tea Party" never actually happens the "Tea" part is always substituted by some actual cargo Colony models. | |
| Clicking the map, even a colony, ends the turn if the End of Turn marker in the sidebar is active. Only clicking the sidebar should then be considered an end of turn press. Clicking a colony should almost always be considered as an instruction to open up the colony UI. | |
| "Throwing away excess cargo" should never happen just from filling up from production. If a colony produces more goods than its stores allow, that's normally ignored. Every 100 units of a cargo is reason for a "colony has a new load of <cargo> available" popup instead (gated behind the appropriate notification setting). | |
| | |
| | |
| | |
| | |
| | |
| | |
| | |



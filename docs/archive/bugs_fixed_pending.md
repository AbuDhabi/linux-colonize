# Fixed / refuted bugs awaiting user verification

Rows moved here from [bugs.md](../../bugs.md) once an agent set FIXED or REFUTED. IDs are permanent; cite as `bugs.md #NNN`. User verifies, sets CLOSED, and moves the row to [bugs_closed.md](bugs_closed.md) (resolution text may be trimmed there; this file keeps the full text).

| # | Status | Bug Description | Resolution |
|---|--------|------------------|------------|
| 952 | FIXED | A ship adjacent to an already-met native village could not open the village menu: moving into the village tile did nothing, no popup (campaign4 COLONY01.SAV, Iroquois village at 43,52). | 2026-09-26 The village tile holds a Brave (save unit type 20 at 43,52), and `units_enter_probe`'s foreign-occupant scan in `units_capture.c` ran BEFORE the HAS_CITY village arm, so a sea mover vs a land occupant fell into the silent `BLOCKED_DOMAIN` refusal (`sea != foe_sea`) and never reached `COLONIZE_ENTER_VILLAGE_SHIP` / `ai_contact_try_ship_village_unit`. DOS `FUN_465b_0000` dispatches the village tile to `FUN_4d56_4528` at raw 75484-75492, before any occupancy test. Fix: skip the foe scan for a sea mover onto a HAS_CITY tile with no colony record (`village_ship`), mirroring the existing `own_colony_dock` exemption. Land units were unaffected (pre-move `game_move_native_prompts` intercepts them). |
| 951 | FIXED | @NODOCKS popup (picking Fisherman on a water plot in a colony without Docks) was deferred until the colony screen closed instead of opening immediately. | 2026-09-26 The mouse/plot-click path in `game_loop_colony.c` (~795) enqueued the popup but never presented it, so `colony_zoom_popup_hold` kept it queued until the screen closed; the keyboard jobs-list path already called `game_colony_present_now`. Added the same `game_colony_present_now(game, AI_POPUP_TAG_INFO)` call after the enqueue. |

# Mechanics smell audit — 2026-09-08

Eight-subsystem sweep for bug-smells (counter-intuitive logic, doc/DOS contradictions, invented mechanics, asymmetries). Findings pending user review; items marked FIXED are done, rest open. Confidence: H/M/L. Items marked DOC are documentation-only.

**FIXED 2026-09-08 (batch 1):** cross-cutting inversion, combat #1, #2, #7, Europe #51, FF #82. Details per item below. ctest 60/60 + goldens green after.

**FIXED 2026-09-08 (batch 2):** #16, #17, #52, #53, #54. Details per item below. ctest 60/60 after.

**FIXED 2026-09-08 (batch 3):** #28, #50, #55, #65, #95. Details per item below. ctest 60/60 after.

**FIXED 2026-09-09 (batch 4):** #3, #29, #31, #83, #96 (plus 1840-gate correction to #28's fix, see #29). Details per item below. ctest 60/60 after; FF head-only test fixtures (Pocahontas/Hudson/Jefferson/Brewster/Bolivar) converted to bitmask grants — they encoded the #83 bug.

**FIXED 2026-09-09 (batch 5):** #4, #5, #6, #18, #68. Details per item below. ctest 60/60 after; SoL-bonus fixtures in test_colonies.c/test_turn.c flipped to latch-before-check (they encoded the #18 live-term bug).

**FIXED 2026-09-09 (batch 6):** #19, #30, #46, #84, #85; #98 REFUTED by golden (comment corrected). Details per item below. ctest 60/60 after.

**FIXED 2026-09-09 (batch 7):** #33, #42, #45, #57, #58, #66, #73, #74, #75; #40/#41/#44 REFUTED (user DOS observations confirmed); new items #103-#106 filed from the verification. Details per item below. ctest 60/60 after.

**FIXED 2026-09-09 (batch 9):** #8, #10, #20, #21, #32, #43, #56, #72, #76, #97. Details per item below. ctest 60/60 (goldens green) after; test_ai_contact Spain-gate + prelude-escalate fixtures rewritten (they encoded #76/#72 inventions).

**FIXED 2026-09-09 (batch 10):** #9, #11, #12, #25, #34, #35, #47, #48, #69, #70, #77 (user-confirmed DOS behavior, mechanism located and rewired), #86, #91, #92, #93, #94, #102; #22 REFUTED (cascade is DOS-real), #62 REFUTED (DOS arm sales untaxed; spurious price-threshold pass fixed instead), #99 REFUTED (DOS writes 0x08 itself; flag renamed AI_DIPLO_AMICABLE). Details per item below. ctest 60/60 (goldens green) after.

**FIXED 2026-09-09 (batch 8):** #39, #59, #60, #61 (royal_money half REFUTED), #67 (premise corrected: DOS cripples the port ship, kills no unit), #87, #88, #90, #105, #106 (closes #103 as side effect). Details per item below. ctest 60/60 (goldens byte-green) after.

## Cross-cutting: native moves_left semantics inversion (H) — FIXED: `units_mp_charge`/`units_mp_exhaust` spent-aware writers in units.c; all listed sites converted to `units_remaining_mp`/helpers (try_move gate+charge+shore, village-raid branch, win/loss drains, mounted spend-all, afford gate, goto gates, ai_contact raid+escort gates). ~40 test fixtures in test_ai_contact.c + 2 in test_units.c flipped to spent semantics (they were written against the inverted gates).

For native nations `moves_left` holds the DOS **spent** byte (turn.c:179 refreshes natives to 0 = unspent; `units_remaining_mp` units.c:6292 is the safe accessor). Multiple sites read/write it as "remaining":

- units.c:6589 — `units_try_move` rejects `moves_left <= 0`: fresh Brave can never move via this path, exhausted one can.
- units.c:6963 — charges `remaining - cost`, i.e. *refunds* a native's spent count.
- units.c:4738 — `units_mounted_attack_spend_all` writes `moves_left = 0` for Mtd. Braves/Warriors = full refresh instead of drain. Same inversion in post-combat drains units.c:6833, 6883.
- units.c:6353, 8654, 8702 — affordability/goto gates read raw `moves_left`, inverted for natives.
- ai_contact.c:8066, 8086 — raid loop skips Braves `moves_left <= 0`, escorts `> 0`: both inverted (unmoved excluded, exhausted admitted).
- ai_contact.c:8712, 8207 — alarm≥70 approach march + post-ambush advance go through `units_try_move`, dead at turn start under spent semantics.

## Combat

1. combat_strength.c:817-819 — two DOS colony-tile resolve peels unported (raw 100557-100564): (a) last-colony shield — native attacker strength zeroed vs a European's only colony; (b) human defender +`(4−difficulty)*4` when attacked colony holds ≥ half nation's colony population. Largest live combat gap. H — FIXED: both clauses at combat_apply_1b0e_resolve_handicaps tail; counts from `stuff.colony_counts`/`colony_pop_totals` (DS:0x9298/0x940c mirrors, DOS per-turn staleness kept).
2. combat_strength.c:184 — veteran +50% gated on JOB_SOLDIER only; **Veteran Dragoons fight at plain Dragoon strength**. Rest of port treats 0x15 and 0x17 as veteran. H — FIXED: accepts UNITS_JOB_SOLDIER or UNITS_JOB_DRAGOON.
3. combat_strength.c:58-59 — name match includes "Cont. Army" but not "Cont. Cav." (asymmetric); DOS gates on type 1/4 only, so whole Continental arm uncited. M-H — FIXED: matcher is now "Soldier"/"Dragoon" only (= DOS @UNIT 1/4 per viceroy 8942-8944); Regulars/Cavalry/Cont. Army/Cont. Cav. already carry promoted attack/defense rows, so no +50% on top. Kept name-based (pool index ≠ DOS @UNIT id in synthetic fixtures), same idiom as `ai_euro_5d04_dos_type_of`.
4. combat_strength.c:170 — damaged −2 peel applied to Privateers too; DOS applies to Artillery (type 0x0b) only. docs/combat.md:82 documents the wrong form. M-H — FIXED: Artillery-name-only (FUN_157e_004a 8936-8938 tests exactly type 0x0b); combat.md corrected.
5. combat_strength.c:415, 388 — Fortify(5) accepted for the +2 defense and terrain-denial; DOS `FUN_157e_015e` requires Fortified(6) only. Digging-in unit gets bonus a turn early. Sibling 1b0e clauses correctly accept 5 or 6. M-H — FIXED: both sites Fortified-only (viceroy 9035 terrain-deny + 9045 +2 both test orders byte == 6); 1b0e artillery-shield sibling untouched. combat.md updated.
6. combat_strength.c:723-726 — Scout-vs-Artillery force-defender-wins invented; DOS's only auto-loss is Brave vs human-controlled Artillery (ported separately 2026-09-08 at units.c:4774). Also units.c:4885 fakes roll without consuming RNG. Stale stand-in to retire. M-H — FIXED: block + `force_defender_wins` field + units.c no-RNG branch deleted; real Brave-vs-arty latch (roll drawn first) stands alone. combat.md updated.
7. combat_strength.h:38-39 — `COMBAT_FLAG_FATIGUE_66` and `COMBAT_FLAG_VILLAGE_CAPITAL` both 0x0008; attacking a capital village prints phantom "Fatigue −66%" row (combat_analysis.c:184). H — FIXED: VILLAGE_CAPITAL relocated to flags2 0x0010 (runtime-only, nothing serializes flags2).
8. combat_strength.c:41-52 vs units.c:3114-3126 — occupied holds computed two ways (goods-only vs goods+passengers); troop-laden ship gets evasion penalty but no strength penalty. M — FIXED (inverted premise): combat side was DOS-literal; `units_holds_used` (evasion) dropped the `cargo_count` passenger term. DOS +0x3150 = goods holds only (writers 15eb_30b8/317c viceroy 13301/13339; boarding parks passengers off-map); strength peel viceroy 8957-8959, evasion peel 5bfb_312e viceroy 98448 read the same byte. combat.md corrected.
9. combat_strength.c:287-294 — colony fortification multiplier requires owner match + nation ≤3; DOS colony probe has no owner test. Internally inconsistent with `combat_unit_on_colony` (255-263). M-L — FIXED: owner-match + unit-nation guards were invented; DOS 157e_015e (viceroy 9004-9012) takes the colony arm on euro_settlement_owner alone (137f_0358, owner < 4). Probe now "European-owned settlement on tile" only; fort tier math untouched. combat.md updated.
10. combat_strength.c:363-390 — "village on either tile → defender gets terrain" invented; DOS's clause is colony-probe on attacker's tile. Port also adds a `combat_woi_active` guard DOS lacks. M — FIXED: village test deleted; DOS 157e_015e 9023-9037 probes euro_settlement_owner (137f_0358, owner<4) on defender tile then attacker tile, neither hit = ambush stash; village probe (137f_0392) runs only on the defender tile at 8988. WoI guard removed (redundant: 9015 gate already implies `0x5382&1` on this arm). combat.md stash section rewritten.
11. combat_analysis.c:451-453 vs combat_strength.c:879 — naval ×3/2 attack factor applied but Attack Bonus row suppressed for naval; displayed mods no longer sum to strengths. docs/combat.md omits ×3/2. M — FIXED: ×3/2 is DOS-real and unconditional (5fef_1b0e viceroy 100457-100458), and DOS 636c draws the Attack Bonus row for ships too (157e_004a sets DS:0x8d00 bit0 on every mode-1 eval, viceroy 8926-8928; printed at 101874-101891). `!is_naval` suppression dropped at both combat_analysis call sites; test inverted (it encoded the suppression); combat.md naval pipeline documents the factor.
12. units.c:5525 vs units.c:3206 — two copies of damage-vs-sink no-RNG fallback break ties opposite ways. L — FIXED: shared `units_ship_damage_vs_sink` (DOS rule 5fef_0352 viceroy 99518-99530: winner guns != 0, roll(1, guns+hull) <= hull = damaged); no-RNG tie resolves damaged (analogue of the file's `attack_str >= defense` convention). Three fixtures sat on the guns==hull coin flip demanding opposite outcomes — moved clear of the tie; Drake assertions now "damaged".
13. units.c:5520 — dead gate (`attack_str <= 0` already returned at 5464). L
14. combat_analysis.c:168 — Cargo row prints `holds*100>>3` (12.5%/hold) but real penalty is −holds after ×8; only equal at base 1. L
15. docs/combat.md:577 — coastal-fort Resolve row stale vs rebuilt guns/hull roll (bugs 255). DOC

## Colony economy

16. colony_yield.c:23 — Prairie Farmer base **3**, NAMES.TXT and terrain_yields.md say **2**; lone outlier in otherwise cell-exact tables; pedia (reads NAMES) disagrees with production in-game. H — FIXED: base 2; user-confirmed DOS bare-prairie unskilled farmer = 3 (2 + unconditional farmer +1) and bare hills = 2 (1 + 1). golden_colony_prod01's Guadeloupe/New Holland/Bahia prairie-farmer tiles were fixture re-picks calibrated to base 3, not independent evidence — re-picked to Prairie+Plowed (same real-capture totals and horse surplus parity).
17. turn.c:629 — EOT Fisherman gate short-circuits `has_docks` with "coastal ⇒ true" before checking Docks building; docs say Docks required; the 4 sibling sites (colony_preview.c:91, colony_screen.c:1589/3853, game_loop.c:9025) have no shortcut, so UI shows 0 fish while tick banks it. H — FIXED: coastal shortcut removed, building-scan only, matching siblings (FUN_15eb_18ec ~11925-11939).
18. colony_production.c:299-305 — SoL production bonus adds `max(latch, live%)`; docs (both) say latch-bits-only; live term is an invented "stand-in" that defeats the one-step latch ramp; town-commons sibling correctly latch-only. M-H — FIXED: latch-bits-only (18ec 11881-11886: +0x1c bit4/bit2, port SOL_50 0x04 / SOL_100 0x02 match); fixtures flipped to latch-before-check.
19. colony_production.c:657-662 — unworked Town Hall still emits raw `sol_bonus` bells; the sibling crosses path deleted exactly this as invented (584-589). Unworked Town Hall at 100% SoL yields 3 bells not 1. M-H — FIXED: fallback block deleted; base bell + Press/Newspaper/Jefferson multipliers kept (user-confirmed those apply to the passive), SoL is per-worker only, matching the crosses sibling.
20. colony_yield.c:356-357, 427 — Silver Miner special-resource deferred to `post_resource`, added after expert multiplier without expert doubling; doc rule doubles additive bonus for matching expert. Surviving uncited curve-fit from commit 3a9f688. M — FIXED: `post_resource` bucket deleted; silver takes the generic pre-multiplier add like every other job. DOS 15eb_18ec tail (viceroy ~11900-11914) doubles the additive resource bonus for any matching expert (`local_18`), job-agnostic; 17fa gives Minerals+silver +1, Silver Deposit +2. Pre-multiplier placement exact for silver (distributive, no DOUBLE resource). Expert Silver Miner on Deposit mountain: 4 becomes 6. Fisherman/Farmer deferred-×2 forms already correct.
21. game_loop.c:8554-8561 — ORDERS "Join Colony" auto-assign has no school filter (can seat specialist as teacher); bugs 414 fix landed only on the colony.c:1576 copy. M — FIXED: same `colonies_school_building_tier(...) > 0` skip replicated in `game_auto_assign_new_colonist`'s fallback building loop (textually parallel to colonies_auto_assign_idle; no shared helper existed).
22. colony_craft.c:24-28 — recipe array order lets ore→tools→muskets cascade in one tick; NAMES @BUILDING order (natural DOS iteration) would make muskets consume last turn's tools. Uncited ordering. M (rests on catalog-order inference) — REFUTED: DOS 15eb_1f72 tail (viceroy 12682-12689) emits ore→tools (row 3) before tools→muskets (row 8); 0b96's cargo-0xe arm subtracts the ore shortfall from tools supply, so fresh tools ARE gunsmith-spendable same tick. No building iteration exists — production solved up front, demand derived. Recipe array reordered to DOS emission order (behavioral no-op) with citation block; DS:0x8de6/0x8de8 identified as prod[15]/prod[16] slots of the DS:0x8dc8 scratch array (closes the colony_eot_production.md open question).
23. colony_yield.c:518-545 — stale comment claims flat +2 commons food; code below implements the 4-way class split docs agree with. L
24. colony_production.c:715/723 + turn.c:1356/1372 — sol-free `lumber_total` contract (header + doc) vs live sol-adjusted debit; code likely right (bugs 169), doc/header stale; dead path invites wrong "fix". L
25. colony.c:2605-2607 — food warehouse cap hardcoded 199, uncited, ignores warehouse level; drives permanent "warehouse full" chrome in a growing colony. L — FIXED: DOS 15eb_0a50 (viceroy 10043-10053) has ONE capacity `100*(1+warehouse_level)` for all 16 goods; food's exemption is per-rule index-0 skips (spoilage 364b_0688 viceroy 57806-57872, @WAREHOUSEFULL 479b_0f60 77396-77404, alert ink 2f2b_28d6 49118-49123). 199 branch deleted; food exempted from full-chrome/alerts/unload flag; fixture updated. New-colonist banking was never actually blocked — chrome and AI headroom only.
26. colony_yield.c:471 — improvement-stack road term uses road-bit only; DOS mask 0x0a = road-or-settlement per map.c:1674; three consumers of same mask disagree. Practically unreachable. L
27. colony_production.c:630 vs :238 (+ turn.c:334 vs :577) — opposite precedence for `population` vs `colonist_count` fallback. Latent. L

## Turn flow

28. turn.c:2854 — 1790/1840 anniversary chrome missing DOS gates `autumn==0` and `!at_war`: @WARN1 fires **twice in 1790** (Spring+Autumn) and during WoI. Sibling @SOONRETIRING0 (ai_king.c:4950) has the gates. H — FIXED: both gates added to the Section E condition (`!(game_autumn && *game_autumn)` + `!game_options.woi`).
29. turn.c:2892 + :2949 — 1800 game-over year missing `!at_war`, then sets `calendar_latch` unconditionally; a WoI win on/after 1800 latches WON but never runs win sequence (game_loop.c:11880 gate). Also Section E runs before woi computed. M — FIXED: game-over condition is now `((year == 1800 && !woi) || year == 1850)` per FUN_3844_0442 (viceroy 58618-58649: the WoI latch `0x5382&1` gates only the early pair 1790/1800 — independence extends the game to the 1850 horizon). Same reading corrected the #28 fix, which had over-applied the war gate to 1840 too. `woi_latched` read directly from `head.game_options.woi`, so the "runs before woi computed" ordering worry is moot.
30. turn.c:3670-3676 + game_loop.c:9702-9715 — decade autosave writes slots 8 **and** 9 (DOS: exactly one) and omits `turn > 2` guard. M-H — FIXED: FUN_130d_0172 shape — decade Spring (`year%10==0 && autumn==0 && turn>2`) sets only the slot-8 flag, else slot-9; the port's extra `year != year_before` term dropped. game_loop applier untouched (flags now mutually exclusive).
31. turn.c:3484-3520 — INDIAN phase runs all 8 slots; DOS skips extinct tribes (bit 7 of tribe stance, transcribed at ai.c:4786 but never tested). Extinct tribe keeps rolling, drifts RNG stream. M-H — FIXED: `ai_indian_nation_turn` gated on `!col1->indian[n-4].extinct` per FUN_4d56_1b3a phase 2 (viceroy 81709-81715: `DS:0x5ad9 + 0x4e*slot & 0x80` skips only the 1816 call; phase 1/3 and the cursor walk run unconditionally, so midpass clear/claim, active-nation, occupancy and MP refresh stay outside the gate).
32. game_loop.c:8371 — auto-end-turn (EOT option OFF) still uses `turn_human_units_exhausted`, the helper the file documents as wrong; all other sites migrated to `game_units_pending_orders`. M — FIXED: site now `!game_units_pending_orders(game)` (sense preserved); forward decl added. `turn_human_units_exhausted` caller-less (public helper left in place).
33. game_loop.c:10146-10148 — turn autosave written before Europe ship arrivals applied; reloading slot 9 delays every arriving ship one turn. M — FIXED: `game_apply_turn_autosave` moved after `game_europe_service_trade_harbor`/`game_europe_deliver_bound_ships` in `game_finish_end_turn`. DOS order: per-nation EOT tick FUN_3844_00f2 (viceroy 6393-6396) tail-calls FUN_48d3_06ba (lane tick 77965-77980 + landfall 78036) before FUN_130d_0172 autosave runs (viceroy 6402-6417).
34. game_loop.c:8358 — post-move handoff calls `turn_select_next_unit` without the standing-order skip loop siblings use; frame-flicker of wrong selection. L-M — FIXED: idiom factored into `game_select_next_unit_awaiting_orders`, used at the handoff + 3 sibling copies (net −30 lines); helper validates final selection so guard exhaustion can't leave a standing-order unit selected.
35. turn.c:3534 + 1963-1971 — negative `human_nation` (headless) makes SETUP skip nothing and FINISH filter nothing: colonies produce **twice** per turn in headless sims. L-M — FIXED: `>= 0`-as-armed replaced by explicit `s_prod_only_set`/`s_prod_skip_set` flags; headless produces once, in SETUP (the AI-owned phase, matching per-nation 3844_00f2 slices). Scratch driver verified 12→6 rum; goldens all use a real human nation, none moved.
36. turn.c:3306 — SETUP raises turn-owner indicator in human color; turn.h and docs say no indicator in SETUP. L
37. turn.c:2728 — `turn_run_european_ai_stubs` dead code with superseded (wrong) phase order, still exported. L
38. docs/turn_between_players.md:66-83 — stale: TURN_PROC_KING split absent, King/REF etc. still shown inside FINISH. DOC

## Units / movement (beyond MP cluster)

39. turns_worked multi-role collisions: units.c:478 + turn.c:166 — Sentried/Fortified Treasure Train outside colony double-incremented, despawns ~4 turns not 8; units.c:554 + turn.c:166 — anchored damaged/building ship repairs ~2× fast; units.c:7474 — `units_wake` zeroes it, wiping repair progress / treasure clock / **trade-route stop index**; units.c:7284 vs 7255 — sentry doesn't reset it but fortify does (stale value = same-turn MP refund). M — FIXED: new runtime-only `park_nights` (units.h) carries the wake-refund nights count; turn.c refresh bumps it (not turns_worked), units_set_orders zeroes it on any park, units_wake reads it for SENTRY **and** FORTIFIED (promotion-turn wake now 0 MP = DOS FUN_479b_0b6c spend, closes #103) and no longer zeroes turns_worked at all (DOS activate writes the order byte only, 42717/42781). turns_worked is again purely the DOS +0x16 clock: treasure despawn back to 8 turns (FUN_3844_0004 confirmed DOS-real, viceroy 58268-58301), ship repair single-rate, repair/route/cower state survives wake. units_order_fortify's turns_worked=0 kept (DOS 2b5a_1112 tail).
40. units.c:7467 — wake `parked` set excludes CLEAR_PLOW/BUILD_ROAD: woken pioneer left at 0 MP though turn.c:158 zeroed its allotment for the same reason as Fortified. M — REFUTED (user recollection confirmed): DOS clears moves_spent for every unit at day top (viceroy 6356-6358) but the plow/road work body spends the full allotment via FUN_281f_0934 (76761/76889) including the completion turn; no wake path writes +0x3149 (activate handler 42717/42781 writes order byte only). Fortified/Sentry wake with full MP only because they never spent. Port asymmetry is DOS-real. Side finding FIXED: aborted work order (bad tile/no tools, viceroy 76753/76886 return before spend) kept full MP in DOS but was zeroed by turn.c — turn.c now pre-restores `units_max_mp` and lets the tick's own spend (units.c:9140) zero it. Two new entries below: #103 (FORTIFIED promotion-turn wake), #104 noted timing nuance not ported (DOS ticks standing orders lazily at selection, port eagerly at turn start — clearing orders before the cycle reaches the pioneer keeps full MP in DOS; high golden risk, low value).
41. units.c:7813 — flood neighbour pick `best_score = 99` but costs are thirds (up to 39/tile); mountainous paths exceed 99 and flood falsely fails. M — REFUTED: 99 is DOS-literal (FUN_OVL20_L0000__0015bc neighbour-pick tail `uStack_30 = 99`, viceroy_overlays.c 86761-86762); DOS too drops to greedy tier when path prices above 99. Citation comment added at site. Note: DOS flood costs are a byte array (would wrap past 255); port int + UNITS_FLOOD_INF, unreachable on real maps (~126 max), not emulated.
42. units.c:7641 — `units_flood_edge` omits the `>100 → 1` sentinel clamp both map.c cost helpers apply; classes 30/31 cost 765 in flood vs 3 in real move. M — FIXED (port-internal alignment): flood edge now folds `>100 → 1` like map_move_spent_thirds/map_move_cost_step. DOS has NO clamp anywhere (viceroy_overlays 86722/86818 bare `*0x2f76 table *3`); map.c's clamp is itself port defence — DOS terrain table only runs classes 0-28, port's `&0x1f` can reach 30/31 on corrupt maps. Zero behaviour change on valid maps (AMER2.MP: classes 0-23,25,26).
43. units.c:8013 — greedy fallback tier has no owner term (flood tier has); goto can route through foreign tiles / into peer colonies. M — FIXED: DOS greedy tier (6662_0f74 scored 8-neighbour tail, viceroy 104678-104690) rejects candidates occupied by a foreign nation/tribe (281f_06d2, settlement bit first) with one escape: mover's own colony when it is the goto goal; reroll/wiggle tail re-applies 06d2 with no escape (104724-104726). Ported as `units_greedy_owner_ok` on both greedy loops + wiggle. Flood's second arm (88d6 +8 peer-improved-land) has no greedy counterpart — asymmetry DOS-real, not copied. RNG stream unchanged (draw before check); goldens stayed green.
44. units.c:6254-6259 — enter probe: empty foreign colony bypasses war/treaty gate entirely for AI and goto stepper; peer colony captured while formally at peace (human protected only by game_loop @HAVETREATY). M — REFUTED (static + user field observation of DOS capture at formal peace): DOS FUN_465b_0000 sets attack flag from settlement owner alone (viceroy 75467-75482, FUN_281f_06be), empty colony = attack target; NO war-state test on path. Only gates: @HAVETREATY popup human-only (`control == 0`, viceroy 75545-75551) and the −0x77b8 treaty-timer cooldown (75553-75557). War declared as SIDE EFFECT of attacking under treaty (75567-75593, 0x40 cleared both ways). 20e6's 0x46 colony-seize stamp has no relation check either. Port correct here; see #105/#106 for the real divergences found alongside.
45. units.c:9652 vs 9566 — `units_find_boardable_ship` ignores goods holds; probe says BOARD, board fails as bogus BLOCKED_DOMAIN. M — FIXED: finder room test = `units_ship_free_passenger_slots > 0` (capacity − passengers − goods holds), matching units_board/units_board_stacked. DOS-backed: +0x3150 = goods-hold count; FUN_1427_10be board budget = capacity − holds_occupied; 0a60 hull-full test `0x5237[type] == +0x3150` (see original_sources_annotated/ai/move_scoring_20e6_full.md). Also fixes stacked-tile case: laden ship first by pool index no longer blocks boarding the ship with room. No pathfinding shift (flood/greedy accept only OK/DOCK).
46. units.c:5759 — `strstr(name, "Missionary")` but type name is "Missionaries"; squat test never fires, missionary classified illegal squatter in `units_can_enter`/pathfinding. Compare units.c:8722 "Missionar". M — FIXED: needle "Missionar".
47. pedia.c:72-73 — claims sentried units wake when enemies approach; no such mechanic exists anywhere. M — FIXED (text): DOS neither claims it (PEDIA.TXT @MISC pages are title-only; blurb was port-authored) nor implements it (every DOS write of order byte +0x314c enumerated — none is a hostile-adjacency scan; day-top refresh 6355-6357 clears only moves-spent). Wake clause cut, boarding half kept (real, 4720_0006/015c); negative-evidence enumeration recorded in the pedia.c comment.
48. game_loop.c:12054-12066 — goto to a farther lane tile sails to Europe from first high-seas tile touched, not ordered destination. L — FIXED: DOS suppresses the sail prompt entirely under Go To / Trade Route orders (4720_015c reason 5, viceroy 76047-76052); DOS's only order-3 Europe departure is the goto menu's "Europe" entry (dest 999 → 2b5a_1dfc, 42798-42819). Port now sails only when standing on the ordered lane destination; move_enter.md updated.
49. map.c:1941 — `map_move_cost_step` road-only pair rule vs `map_move_spent_thirds` road-or-settlement (bugs 352 fix); helpers now disagree (dead outside tests). L

## Europe / market / trade routes

50. europe.c:2478-2482 — human harbor buys/sells pass `col1=NULL`: nation `trade.tons/tons2/gold` ledger never written, so player's own trading is invisible to the long-run price pool (Custom House + AI sales do count). H — FIXED: all five direct channels (sell_hold, sell_hold_partial, sell_unit_hold, buy_cargo, buy_unit_cargo) route through europe_apply_trade_volume with col1 + real nations; buy entry points grew col1/nation params (game_loop callers rewired). human_nation from head.human_player, so AI transport dump-sells now take the non-human 1d44 term (part of #58's 6405 leg). Residual on the wrapper: arm rows (europe.c dock menu, no DOS arm located — #62) and ai_euro 5d04 (#61).
51. europe.c:3042-3058 — sell credits gold but never credits tax to `royal_money` (DOS `nation+0x22 += tax`); all Europe tax revenue vanishes, REF systematically underfunded. Custom House arm does credit it. H — FIXED: europe_credit_sale_tax helper; europe_sell_hold + europe_sell_unit_hold credit gross−net to seller nation royal_money (trade-route auto-unload fixed for free; #52/#53 direct paths closed in batch 2).
52. game_loop.c:2861-2874 — shift+drag partial sell: no boycott check, no price movement. Boycott exploit + zero-market-impact channel. H — FIXED: new `europe_sell_hold_partial` (full path: boycott gate, tax credit, sale status, volume price move); both game_loop sites rewired. Also closes the #51 residual tax leak on these paths.
53. game_loop.c:13429-13435 — `-` key single-unit sell: same two omissions; 99 presses liquidate a hold with zero price movement. H — FIXED: same helper (amount 1).
54. game_loop.c:9418, 13337, 13399 — all three @HOWMUCH4 purchase prompts quote `europe_sell_price()` (bid) instead of ask; off by burden+1 (Food quoted 0 not 8). H — FIXED: all three quote `europe_buy_price` (ask).
55. europe.c:725/729 — @CARGO `start_hi` discarded; DOS rolls RNG(lo..hi) per cargo per campaign; every game opens at bottom of every price band. H — FIXED: europe_seed_campaign_prices = FUN_38fd_6024 seed (viceroy_unpacked.c 68645-68654): bid = RNG(start_lo..start_hi) inclusive, fixed 16 draws in cargo order (hi==lo still draws), one roll shared by all nations, no [low,high] clamp; called from the new-campaign block after move_rng is established. Load path untouched (save euro_price wins; DOS load skips the seed at 75c2:121921).
56. europe.c:589-631 — missing DOS Spain override: nation 2 recruit-pool slot 0 forced to Jesuit Missionaries after seeding. M — FIXED: DOS 38fd_6024 tail LAB_38fd_6161 (viceroy 68731-68733): `if (0x9e12 == 2) record[+2] = 0x18` (job 24 = Jesuit Missionaries). Ungated (stomps even the Discoverer easy-opener), seed-time only (no reroll case), load path untouched (save bytes win). Ported in `europe_seed_pool` keyed on `bound_nation == 2`; col1_bridge legacy-save fallback hoists `europe_set_nation` above the pool restore so the binding is fresh.
57. europe.c:2392-2394 — Dutch (term*2)/3 damping keyed on `human_nation == 3` which the caller can never satisfy (passes −1), and applied to buys too (DOS: sells only). Dutch player gets no damping on own harbor trades. M — FIXED: damping now `!is_buy && human_nation == 3` — DOS FUN_38fd_1dfa sell walk damps the Dutch record slot (viceroy 60265-60266), FUN_38fd_1d80 buy walk has no Dutch case (60204-60245). Caller fixed via new runtime-only `EuropeScreen.bound_nation` stamped by `europe_set_nation` (= DOS FUN_38fd_0000 binding DS:0x9e12/0x84fc, viceroy 58696-58702); `europe_apply_volume_price` passes it instead of (−1,−1). Dutch human buys now move prices undamped (~1.5× faster) — intended.
58. europe.c:2387/3392 + ai_euro.c:6405 — `seller_is_human` hardcoded true for AI dump-sells; at difficulty >2 volume-term sign flips, AI sales push market wrong direction. M — MOSTLY PRE-FIXED by #50 rework (3392 leg + ai_euro 6405/20e6 tail already pass real human_nation); residual FIXED: `europe_apply_volume_price` (−1,−1) accidentally read as human — now passes `bound_nation` (see #57); plus DOS's `0x9e12 < 4` guard (viceroy 60193): seller_is_human requires 0 ≤ seller < 4.
59. col1_bridge.c:1444 vs 1821-1853 + game_loop.c:6287 — boycott `0xFFFF` heal applied only to Europe mirror; nation copy stays poisoned and the render mirror copies it back over the healed value first frame. M — FIXED: capture-side export heals nation word 0xFFFF→0 (nation copy stays authoritative); render mirror applies the same map so a poisoned word can't overwrite the healed UI copy on frame 1. Live-load heal already existed (game_loop 4723).
60. europe.h:222-223 + col1_bridge.c:2512-2600 — `trade_route_plus1`/`trade_stop` not exported for Europe-lane ships; saving mid-Atlantic silently drops route automation. M — FIXED: Europe-lane ships export orders=2 + profession=(stop<<4)|slot when route valid; import decodes via new col1_bridge_europe_ship_route_load on both lane and harbor paths (stale/emptied route drops automation like the map-unit decoder). DOS: 48d3_015e/007a preserve the orders byte when +0x314c==2, so the profession-byte cursor survives the Atlantic round trip; 479b_0bd0 gates on unit+0x08==2.
61. ai_euro.c:7421-7422 — 5d04 AI Europe sale writes tons/gold but not tons2 (price-pool input) nor royal_money. M — FIXED (tons2) / half-REFUTED (royal_money): DOS 5d04 sale = 38fd_1f0c (60320-60337) → 1dfa ledger (tons+tons2+taxed gold+pools) with treasury += untaxed gross via 15eb_0556; nation+0x22 tax credit is HUMAN-harbor-only (60557-60575), so no royal_money write. Port now routes through europe_apply_trade_volume with sell price = euro_price−1 (291f_09ea), treasury takes gross. test_ai_euro_expand expectations were calibrated to the old formula — re-derived.
62. europe.c:1521 — `europe_arm_sell_gain` untaxed while every other sale routes through tax; no DOS arm located either way. L — REFUTED: @ARMOPTIONS handler 38fd_3746 located (switch bodies recovered from viceroy_ndisasm.asm, jump table 38fd:3c5a); sell rows credit raw gross to treasury via 1dfa ledger — no tax split, no royal_money (contrast harbor sale 38fd_23c4 viceroy 60557-60575). Untaxed port correct; citation block added. Side defect FIXED: port ran the 0058 price-threshold pass on arm trades (DOS arm rows call the ledger bare) — spurious @PRICEUP/@PRICEDOWN on arming, now immediate_threshold=0.
63. reports.c:2038-2039 — Economic report fallback ask missing `+burden`. L
64. game_loop.c:2870 vs europe.c:3045 — empty-hold sentinel 255 vs 0. L

## Indian / King AI / diplomacy

65. ai_contact.c:8292-8305 — post-ambush raid pulse adds +2 alarm and `attacks++` on **every** tribe of nation, immediately after DOS's negative vent + attitude zero in `units_resolve_land_combat`; last surviving retired-drip-class caller (`ai_contact_alarm_bump_amount`); attacks word now live state (hostility gate + tier-3 map alarm). H — FIXED: both blocks deleted (bump + all-tribes attacks++); helper's remaining caller is the 4657 prelude (#72).
66. ai_contact.c:642, 1259 — @INDIANSHUN reject and open-hostilities also `attacks++` across all tribes; DOS's only attacks-writer is the per-settlement trespass bump. M — FIXED: both bumps deleted (mirror of #65); friction floor 80 kept. Sole remaining writer verified = 465b trespass bump at units.c:6781.
67. ai_contact.c:7718-7748 — SHIP raid kind: −16 alarm charged for zeroing MP + dumping 1 ton; DOS row is "unit at the colony killed". Raider pays alarm for nothing. M — FIXED (premise corrected): DOS 5fef_0f14 kind 3 cripples THE SHIP ON THE COLONY TILE (stack walk to type 0xd-0x12, no nation filter; raw 99989-100003), applied via 5fef_0352 with param_2<0 = always-damaged: holds+passengers zeroed, bit7, repair preset (Privateer 0x10 column), relocate to nearest repair port; @RAIDSHIP chrome. Shipless port demotes to kind 0 = no loot AND no −16 vent (LAB_5fef_123a). New units_raid_damage_ship + ai_contact_raid_port_ship; −16 alarm row itself was correct.
68. ai_contact.c:4595-4604 — WoI defection status line says tribe "declares for the rebel cause" while the mechanic (per its own comment) makes it Tory (+100 rebel alarm, −100 Crown). H — FIXED: line now "declares for the Crown!"; stale rebel-side header comment corrected too.
69. docs/difficulty.md:130-146 — King tax table (`first_year = 1536 - diff` etc.) matches nothing in ai_king.c; real gate is `ai_king_audience_roll` (turn ≥30, band 18/15/12/9, −2*(diff−2)). DOC-H — FIXED: section rewritten from ai_king.c:973-1099 (38fd_5be8 viceroy 68420): turn ≥30, interval base−2*(diff−2) by year band, tax>85 skip, favor score + delta ladder, 0..75% clamp; full 5×4 interval table.
70. docs/difficulty.md:151-166 — REF seed/intervention table stale vs `8*diff+15 / 5*(diff+1) / 3*diff+2 / 6*diff+2` and census-derived backups. DOC-H — FIXED: section rewritten in four parts: seed at NEW GAME not declare (75c2:360b, viceroy_unpacked_2 112436-112443), per-turn royal-purse growth (43f7_1d42: diff*8+10 doubled at 1600/1700/1750, unit per 1800 gold), wave/Tory difficulty terms, intervention pool seed + trigger/landing chain. Effects-summary + conflicts tables updated; dead constants flagged.
71. ai_king.c:3013-3015, 2372-2374 — comments reference retired constant/stand-ins. L
72. ai_contact.c:4624-4657 — alarm-prelude escalation latches `unknown31_flags |= 0x20` forever (fires once per nation per game), plus undocumented `alarm < 30` gate; makes difficulty escalation table nearly meaningless; repurposes unmapped DOS save byte. M — FIXED (invented; deleted): 4d56_1816 §2 (viceroy 81558-81599) is the WoI defection (already ported), touches no alarm/friction and has no alarm<30 term; DOS indian record +6 (the byte `unknown31_flags` aliases) is never accessed by any decompiled export (+0/+2/+3/+5/+7/+8/+10 only). Escalate block deleted; `ai_contact_alarm_bump_amount`/`ai_contact_bump_u8_cap100` retired (Pocahontas/French halving lives inside 4cc6_00f2 alarm_delta — helper double-counted it). Prelude is now clamp + mission-pacify only. col1_save.h comment corrected. Deleted draw was on a function-local rng — no shared-stream shift.
73. ai_contact.c:3686 vs 5168 — beg gate `alarm >= 55 skip` vs gift gate `> 74`; DOS has one gate (≤ 0x4a) for the single 022e encounter; 55-74 window produces no visit at all. M — FIXED: beg arm now uses the identical `ai_diplo_indian_alarm > 0x4a` bail the gift arm uses (022e reads alarm once via FUN_281f_030c, viceroy 96615, bails whole encounter at 96716). Dead window gone; band 55-74 can now reach beg arm (RNG note in #75 entry).
74. ai_contact.c:3528-3537 — beg-food outcome binds *first tribe of nation*, not the visiting Brave's home settlement (gift half binds correctly); wrong village takes attitude zero + capital doubling. M — FIXED: `ai_contact_apply_beg_food` takes home_tribe param (DOS binds Brave's own settlement via FUN_281f_0a4c(unit+0x314a) before encounter body, viceroy 96706); try_village_beg_food records home_index (same village the 2154 econ scoring used) and packs it through popup payload `colony_id | (home_index+1)<<16`, unpacked at AI_POPUP_TAG_CONTACT_BEGFOOD resolve.
75. ai_contact.c:5242-5249 + 3962-4058 — convert rolled twice per nation-turn (mission_convert_visit with own RNG, then gift arm re-rolls and `continue`s without sending); visit also fires on standing adjacency (invented-pulse shape). M — FIXED: `ai_contact_mission_convert_visit` (invented adjacency pulse, own rng(0,0xf)) deleted with its meet-trade call; gift arm's roll — already at DOS site (viceroy 96996-97010: one draw rng(0,0xf) at 97002, popup 0x182a, spawn 0x1b, falls through to visit-resolved) — now sends convert, stamps cooldown, returns 1 skipping beg half (DOS fall-through to LAB_5bfb_1000). RNG: draws only removed, none reordered; surviving per-European order mood rng(1,0x148) → convert rng(0,0xf) → rank rng(1,3). golden_ai_turns may shift (pulse draw removal + #73 band widening).
76. ai_contact.c:7482-7487 — raid threshold 35-not-40 for euro nation 2 "Spain conquest bias": invented, and backwards (e is the *target* — natives raid Spain earlier). M — FIXED: uniform `alarm < 40 → continue`. No DOS per-nation raid threshold exists; every nation-2 special is Euro-aggressor-side (5952_035e war shifts viceroy 94172-94186; tribute ×1.5). fandom "Spanish pushed toward conquest" = that Euro-side bias. test_ai_contact Spain fixture rewritten (35 closed / 40 open).
77. ai_diplo.c:633-639 — capital surrender forces PEACE bit + `relation_by_indian = 0x60` uncited (only the alarm clamp has DOS citation); razing capital rewards attacker with enforced peace. M — FIXED (user-confirmed DOS behavior, mechanism located): DOS forces peace via the alarm DELTA, not a flag store — capital-razed arm (5fef_1b0e viceroy 101289-101298) applies −(alarm−15) through 0d6c→4cc6_00f2, whose negative arm clears the WAR bit both ways when new alarm <0x4b (clamp lands at 15, always). Port hand-stored alarm=15, bypassing the war-clear — now routed through ai_diplo_indian_alarm_delta under the same alarm>15 guard. PEACE bit + 0x60 kept and cited (DOS surrender idiom 43f7_0108 or-s 0x60; restating it reproduces DOS exactly). Dead first-contact clamp line removed.
78. ai_contact.c:3689-3699 — stale comment: contact_state is annually wiped, not a forever latch. L
79. ai_diplo.c:572-575 — stale comment bands (50/40 vs live 26/16). L
80. docs/indians.md:179-180 — pop cap 15 stale vs `3*tech+4` / `2*tech+3`, satellites don't grow. DOC
81. col1_save.h:778-781 — `woi_defect_resolved` comment wrong: set only on success (matches DOS); eligible tribes genuinely re-roll every turn. L

## Founding Fathers / SoL / scoring / save

82. founding_fathers.c:1352 — `elect_commit` writes `head.founding_father[idx]` unconditionally; DOS writes only when still unclaimed (write-once, verified against dutch-reports.SAV). Turns first-claimer record into last-claimer; corrupts the reports fallback. H — FIXED: head write guarded on `< 0` (matches FUN_4345_0342).
83. founding_fathers.c:289 — `nation_has` reads head equality, contradicting reports.c:717 and inconsistent with `ff_available_to` (bitmask-only); head/bitmask desync (zero-filled heads existed per bugs 288) grants false effects AND leaves father electable again → double `apply_effect` (Jones second Frigate, Magellan re-bump, Coronado re-reveal). M — FIXED: `nation_has` is bitmask-only, matching DOS FUN_15eb_3960 (the ownership test behind FUN_281f_07b4; head DS:0x53a9 is filled with -1 at init, written once by FUN_4345_0342, never read back). game_loop.c's private `game_nation_has_ff` copy of the same head-equality test now delegates. test_colonies.c Bolivar fixture sets the bitmask (it encoded the bug).
84. founding_fathers.c:171 — pool sync only adopts stash when `> 0`; legit zero pool (right after election) never round-trips — reload refunds a large `total−spent` estimate, can fund instant second election. M — FIXED: `last > 0` guard dropped (adopt stashed 0 when `<= need`; `<= need` sanity kept vs pad-byte 0xc1 collision on DOS saves).
85. col1_save.c:685/769 + col1_bridge.c:1457-1458 — pool stash permanently destroys real `liberty_bells_last_turn` for all 4 nations and leaks the FF pool into the Europe screen's "bells last turn". M — FIXED (leak half): new `founding_fathers_col1_last_turn_is_stash`; bridge load zeroes `europe->liberty_bells_last_turn` when the marker is set (next EOT tally rewrites the genuine value). On-disk overload itself stays — the DOS layout has no spare word, and the field self-heals in one turn; residual: election gate at founding_fathers.c:~1416 still reads the overloaded field (#94).
86. founding_fathers.c:225 — `FF_DESOTO_REVEAL_RADIUS` 1 vs doc's radius 2 (elect-time sweep tighter than ongoing effect; Coronado sibling correct). M-L — FIXED (both halves wrong, user guidance confirmed): DOS FF-apply 4345_0342 (viceroy 73044-73160) has NO de Soto elect-time case — invented sweep deleted; de Soto's real effect is the already-ported ongoing unit sight radius 2 (units_sight_radius, 13f1:0321). Coronado (case 6, 73155-73159) loops ALL colonies with no owner test, ±5 square reveal via 13f1_00a6 — port's radius-2 owned-only sweep replaced by colonies_reveal_all_for_nation reusing colonies_reveal_ring5. Both radius constants removed; founding_fathers.md rows rewritten; tests assert ±5/±6 boundary + no de Soto elect reveal.
87. founding_fathers.c:870/945/1099 (+ game_loop.c:564, reports.c:2618) — `i < colony_count` iteration but `colonies_abandon` leaves in-place holes and shrinks count: loops visit dead slot, miss live tail colony (La Salle sweep, Coronado reveal). M — FIXED: all listed loops + colony.c:458 colonies_can_found @TOONEAR ring (worst case: founding legal 1 tile from a live colony), reports.c reports_naval_location, game_loop game_colony_pos (now returns n-th LIVE colony) converted to full-pool + active-guard idiom (158 existing sibling instances).
88. reports.c:2335/2499 — Colony report pairs col1 and runtime colony arrays 1:1 by index; offset after any abandon/raze → SoL/bells attributed to wrong colony. M — FIXED: both report pages pair col1↔runtime colonies by (x,y) via new reports_pool_colony_for — same key col1_bridge_export uses; index pairing dropped a tail colony's row to the NULL fallback after any abandon besides misattributing the rest.
89. colony_production.c:416-418 vs turn.c:2170-2175 — rebel accumulator recomputes bells with sol_bonus=0 while EOT nation tally uses real bonus; two different "bells this turn" for same colony, zeroing rationale invented. M-L
90. col1_bridge.c:2734-2736/2747-2749 — `col1_contact_adjacent_tribe` on every human land action drips friction + alarm (fourth drip of the class bugs 297 removed), and increments low byte of a documented signed int16 attitude word with `< 11` on that byte (incoherent at values ≥256). M — FIXED: both drips deleted (fourth sibling of the retired fandom-drip class; DOS land-step tail is 5bfb_3180 = encounter scan, writes no attitude/alarm). Village-adjacency detection, first-welcome report, status string kept.
91. col1_bridge.c:1764 — `head.show_entire_map = 0` every capture; read as de Witt report gate; cheat/post-win state can't round-trip. M-L — FIXED: capture zeroed a live head field (save IS the live head via game_loop.c:4924); runtime stamps it (setview, victory, king) and reads it back (fog bypass, FA/de Witt gate = 281f_07b4(viewer,4) || DS:0x53a2). Now preserved, normalized 0/1.
92. col1_bridge.c:2198 vs 1223-1227 — spent-MP export via `units_max_mp` (Magellan +3 only if module global bound) vs import adding +3 unconditionally: +3 MP per round trip when capture runs unbound (tools/tests). M-L — FIXED: shared `col1_bridge_unit_max_mp(ut, nation, save)` (cites 1427_065a) derives Magellan from the save in hand on BOTH import and export; round trip lossless bound or unbound.
93. colony_production.c:194-206 + col1_bridge.c:1880-1894 — colony matched by (x,y) only; re-found colony on same tile inherits previous owner's SoL history. L — FIXED at the founding side: (x,y) pairing kept everywhere (owner check would break capture pairing when g_colonies_col1 unbound); `colonies_found` now wipes an orphan COL1 record on the tile via colonies_col1_forget_record_at (zeroes in place, keeps slot, reseeds rebel_divisor=100 per 364b_1ba8). Load path unaffected.
94. founding_fathers.c:1416 — election gate reads the stash-overloaded field as genuine EOT bells. L — FIXED: try_elect gate substitutes 0 when founding_fathers_col1_last_turn_is_stash (the #85 marker) says the field carries the FF pool.

## European AI

95. ai_euro.c:16728 — 20e6 sail-pick "doesn't need colonists" arm `−0x25` (−37); DOS SBB idiom gives −25 (`-0x19`). Hex/decimal slip; ferries over-avoid staffed colonies. H — FIXED: `-0x19`.
96. ai_euro.c:16744 — reads ai_flags bit 0x04 (`NEEDS_MILITARY`, which has **no writer** in the port) where DOS raw tests bit 0x08; war-cargo destination score always takes the −0xf arm on DOS-loaded games. M-H — FIXED: new `COLONIZE_COLONY_AI_SHORT_DEFENDERS` 0x08 (colony.h) tested at the site (DOS raw 89663). Bit pair confirmed at FUN_5952_035e 94195-94199: 0x08 = defenders below want, 0x04 = surplus; the 0x04 constant's `NEEDS_MILITARY` name is inverted but kept — its other read site (ai_euro.c ~12103) matches DOS raw 88756, which really does test bit 4. Neither bit has a port writer; both come from the save's +0x1b byte.
97. ai_euro.c:16749 — missing `(int8_t)` cast on `cargo_idle_turns`; sibling at 13452 casts; DOS reads signed. M — FIXED: `(int8_t)` cast at the 20e6 colony-sail shared idle term.
98. ai_euro.c:1592 vs 1546 — `clear_pre_stockade_build_queue` runs last in dispatcher and wipes exactly the young-colony Docks/Warehouse picks `prefer_young` just made (and the `colonies_found` Docks default). Comment/code disagree (Pop≥2 vs `< 3`). M — REFUTED: tried keeping pop-1 picks (`pop == 2` gate); golden TURN4→5 failed (DOS New Amsterdam pop2 bip=255 — DOS young colonies carry a project ONLY on their founding turn, via the colonies_found default that lands after this clear). `pop < 3` wipe is DOS-correct; comment rewritten to say so. prefer_young's per-turn re-pick is dead work but harmless.
99. ai_euro.c:248 — 0x08 relation bit double-booked: 0a60 continent-stance gate reads it as DOS amicable-negotiation latch, but Linux-only `AI_DIPLO_TREASURE_STRONGER` (ai_euro.c:3254, ai_king.c:1724) writes the same byte; treasure train passing a stronger peaceful rival can flip continent tier. ai_diplo.h:79 documents collision as unreconciled; this consumer post-dates it. M — REFUTED (wrong citation, not invented write): real DOS writer is 465b_0000 (viceroy 75527-75545): treasure actor on foreign-relation tile sets bit7, then rng(0,100) < diff+1 sets 0x02 (target stronger) or 0x08 (actor stronger) on nation[target].euro_relation[actor] — index order and comparison direction match the port. One latch, consumers 521d_6d8e war-relapse + 0a60 gate, cleared by 43f7_0108 0x0b mask. Flag renamed AI_DIPLO_AMICABLE, citations corrected at all sites, ai_diplo.h collision paragraph deleted. Behavior-neutral.
100. ai_euro.c:12003 — DOS's non-ship guard and `odds<1` clamp on the −999 attack penalty dropped; latent (sea units gated out upstream) but function branches on `is_ship` as if reachable. L
101. ai_euro.c:12098 — reads destination-tile colony where DOS reads mover's nearest own colony; also widens DOS's single owner gate. L
102. ai_euro.c:10855 — `s_20e6_hop_slot`/`hop_steps` get no despawned-id cleanup unlike siblings; reused unit id inherits foreign ring-hop latch. L — FIXED: the s_20e6_wagon_errand hygiene loop in ai_euro_dispatcher_turn generalized into a shared dead-id reset also covering hop_slot (slot+1 encoding, 0 == DOS 0xff unset) and hop_steps.

## Found during #40/#44 verification (2026-09-09)

103. units.c:7469-7508 + turn.c:147-150 — FORTIFIED wake over-restores on the promotion turn: DOS 5→6 promotion FUN_479b_0b6c (viceroy 77146-77153) spends MP (`FUN_281f_0934`), so waking that turn yields 0 MP in DOS but full MP in port (`prev == FORTIFIED` treated as fully parked). Needs a nights-fortified discriminator like the SENTRY `turns_worked > 0` one. M-L — FIXED with #39 (park_nights discriminator covers FORTIFIED promotion turn).
104. turn.c:152-165 — port ticks pioneer standing orders eagerly at turn start; DOS ticks lazily when unit becomes active in Move Pieces loop (FUN_2b5a_3ae6 jumptable, viceroy 46413): DOS player clearing orders before the cycle reaches the pioneer keeps full MP and skips that turn's work. Not ported by choice — moving the tick into selection is high golden risk for small fidelity. DOC/WONTFIX
105. units.c:8766-8801 — `units_advance_goto_one_step` has no branch for empty foreign colony: human goto ending on a treaty peer's undefended colony captures it without @HAVETREATY prompt; DOS routes goto steps through FUN_465b_0000 and prompts at viceroy 75545. M — FIXED: goto stepper treats an empty foreign colony tile like a foreign occupant (mid-path halt; dest only for attack>0 — DOS 465b attack flag from settlement owner alone); human pacer hands a hostile FINAL step to game_try_unit_move so the full @WHACKINDIANS/@HAVETREATY/@HALF chain runs (DOS prompt at 75545); attack-cancel resolves clear the Go To so the confirm can't re-fire every frame.
106. units.c:6171 + ai_euro.c:15984/16102 — port stricter than DOS on AI war opening: probe returns BOUNCE_PEACE for defended foreign tiles at peace and colony-seize/best-foe gates require `ai_diplo_at_war`; DOS requires only absence of signed treaty (0x40) and colony-seize has no relation check at all (20e6 0x46 stamp). Makes @SNEAK declare-war (ai_euro.c:13120) near-dead; port AI essentially never opens a Euro war on its own. M — FIXED: AI Euro×Euro fightable unless signed-treaty bit 0x40 stands (units_at_war_for_move; human mover keeps at-war test — the game_loop confirm chain declares first); best-adjacent-foe same gate; colony-seize relation gate deleted (DOS 0x46 stamp has none) with 465b side-effect war declared on capture (75567-75593). @SNEAK opening reachable again.

# AI accuracy bar (parallel campaign)

Paste into every parallel AI Task prompt:

```
Accuracy bar:
- Prefer real rules from Colonization.pdf + docs/fandom_col1994.md + NAMES/decomp
  before inventing stand-ins.
- "Rough" = incomplete wiring/UI, NOT made-up powers.
- If the real effect needs a missing hook: implement the closest real behavior
  or PARK with a comment naming the intended effect — no gold/crosses fiction.
- Cite the source effect in the code comment for each FF / rule change.
```

Authority order for effect semantics: original code/NAMES/decomp → Colonization.pdf → docs/fandom_col1994.md → documented temporary stub only if (1)–(3) give no usable rule.

Forbidden: inventing gold/tools/moves/crosses stand-ins when the manual/wiki already names the real power (e.g. Smith factory gate, Drake privateer +50%, Brewster dock filter).
